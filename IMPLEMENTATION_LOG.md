# 구현 학습 로그

## 컨셉 선택 기능과 최종 명칭 (2026-09-13)

생성/수정 모달에 표지 선택을 연결하고 SQLite `cover_concept` 저장/마이그레이션을 추가했습니다. 최종 명칭은 디자인1~4이며 초록 여행과 색상 선택 UI를 제거했습니다. 기존 초록 여행 값은 디자인1로 표시합니다. 원본과 이전 데이터는 보존합니다. Rust 테스트 8개 통과. 실행 중인 Tauri 앱은 재시작해야 새 명령 인자가 반영됩니다.

## 밝은 민트 표지 (2026-09-13)

표지 소재를 `album-mint-cover.png`로 교체하고 뒷표지와 기본 배경색을 민트 계열로 맞췄습니다. 사진/장식 배치와 기능은 유지했습니다. 디자인 테스트 4개, 빌드 통과. 에셋 편집 이유와 프롬프트는 학습 로그 최상단에 기록했습니다.

## 기존 표지 소재 교체 (2026-09-13)

- 기존 천/투명 장식 에셋 2개와 합성 스타일을 제거하고 `album-sage-cover.png` 하나로 교체했습니다.
- 표지색을 참고 사진의 세이지색으로 통일했습니다. 이전 색상 설정은 책갈피 리본 색상으로 전환했으며 기존 DB 값은 보존합니다.
- 두꺼운 내지, 리본, 아이보리 인화지, 종이 라벨 안의 제목으로 목록 표지를 재구성했습니다. 앨범 내용과 원본은 변경하지 않습니다.
- 디자인/앱 테스트 20개와 빌드 통과. 구현 이유와 생성 프롬프트는 학습 로그 최상단에 정리했습니다.

## 참고 이미지 기반 스크랩북 (2026-09-13)

- 앨범 목록 표지를 실제 사진 최대 4장, 작은 테이프, 지도/압화 장식, 한글 손글씨 제목으로 변경했습니다.
- `AlbumsView.tsx`에서 이미지 종류만 선택하고 사진 수별 배치를 지정합니다. `albums.css`에 표지 전용 스타일을 모았습니다.
- 장식 PNG와 Nanum Pen Script 폰트/라이선스를 `public`에 포함했습니다. 데이터 저장 구조, 원본, 선택/편집/삭제, 내부 앨범 넘김은 변경하지 않았습니다.
- 디자인/앱 테스트 18개 통과 후 사진 0~4장과 긴 제목 검사를 추가하여 디자인 테스트 4개도 통과했습니다. 빌드 성공, 기존 인식 모듈의 큰 청크 경고는 남아 있습니다.
- 생성 프롬프트, 폰트 출처와 구현 원리는 `PROJECT_LEARNING_LOG.md`에 기록했습니다.

## 사진 중심 표지 전환 (2026-09-13)

- `features/albums/albums.css`: 천 질감과 작은 사진 창을 제거하고 앞표지 전체 사진, 하단 왼쪽 제목, 12px 책등으로 변경했습니다.
- 기존 표지색과 내지 두께, 앨범 데이터 및 내부 감상 기능은 유지합니다.
- `tests/design.spec.ts`: 새 표지 사진 점유율과 제목 영역 검사로 교체했습니다. 디자인/앱 테스트 18개 통과.
- 구현 이유는 `PROJECT_LEARNING_LOG.md`의 같은 날짜 항목에 정리했습니다.

## 패브릭 앨범 표지 적용 (2026-09-09)

- 기존 격자 패턴을 로컬 생성 에셋 `public/textures/album-linen.png`로 교체했습니다.
- 정면 표지, 3:4 세로 사진 창, 아래쪽 작은 제목, 19px 책등과 얇은 내지를 구성했습니다.
- 표지색 설정, 선택/수정/삭제 및 내부 앨범 넘김은 유지합니다. 새로운 라이브러리는 없습니다.
- 구현 원리와 에셋 프롬프트는 `PROJECT_LEARNING_LOG.md`의 같은 날짜 항목에 기록했습니다.

## 이전 앨범 목록 디자인 잔여 코드 제거

- book-cover-3d가 package.json/lockfile과 실행 코드에서 제거된 상태 확인.
- 공통 CSS에 남아 있던 이전 savedAlbumGrid/Card/Open 배치·고정 높이·hover 규칙 삭제. 새 앨범 전용 CSS로 통합.
- 정면 표지, 모바일 배치, 내부 감상 및 데이터는 유지.

## 내 앨범 정면 표지 재구성

- 사선 BookCover 대신 frontAlbum 구성 추가: 뒤표지/내지/제본 홈/사진 창/하단 제목.
- 회전과 hover 회전 제거, 패브릭 질감과 가장자리 그림자만 사용. 표지 색상 유지 및 제목 대비 자동 조정.
- 사용하지 않는 book-cover-3d 의존성과 이전 표지 전용 CSS 제거. 앨범 내부는 유지.
- PC/모바일 캡처 확인. 앨범 및 기존 앱 흐름 테스트 18개 통과 후 정리 작업 재검증.

## 얼굴 모아보기 크기 조정 (2026-09-09)

- 얼굴 모아보기만 128px 정사각형 타일로 변경. 적은 사진이 넓게 늘어나지 않도록 고정 그리드 트랙 사용.
- 작은 모바일 화면은 3열, 중복 얼굴 썸네일은 숨기고 날짜 유지. 인물별 상세 사진 크기와 선택 체크 크기는 유지.

## 메뉴/로딩 배경과 반려동물 후보 확인

- 메뉴 16px, 아이콘 20px, 버튼 최소 높이 48px. 100px 메뉴바 유지.
- 등록 미디어의 임시 그라데이션을 종류별 중립 단색으로 교체.
- `petRecognition.ts`: 로컬 COCO-SSD 개/고양이 탐지, MobileNet 특징 비교, 순차 추론 및 실행 중 캐시.
- `PetMatchReview.tsx`: 등록 사진을 기준으로 후보 찾기, 진행/중단, 선택 확인 후 기존 SQLite 연결 저장. 자동 확정이나 모델 재학습은 아님.
- 모델 최초 준비를 predev/prebuild에 연결. 모델 약 35.6MB, Git 제외. 추가 패키지는 TensorFlow.js 4.22.0과 COCO-SSD 2.2.3.
- 실제 모델 개 탐지/특징 추출 및 외부 요청 없음 검사 통과. 개체 식별 정확도 검증과 실제 사용자 DB 테스트는 수행하지 않음.
- `npm run tauri:dev`로 재시작. 개/고양이가 한 마리 선명하게 나온 사진을 먼저 연결한 다음 후보 찾기 실행.

## 상단 메뉴바 높이 추가 조정

- 데스크톱 메뉴바 84px → 100px. 로고와 메뉴 크기는 그대로 두고 `align-items: center`로 세로 중앙 정렬 유지. 모바일은 기존 자동 높이와 줄바꿈 유지.

## 썸네일 로딩 후속 최적화 (2026-09-08)

- 사진별 DB 초기화·전체 경로 정리 대신 읽기 전용 단건 조회.
- 캐시 조회는 생성 잠금 없이 즉시 반환, 생성은 2개 구역으로 제한. 같은 ID 중복 생성 방지.
- 빠른 PNG 압축, 이미지 관련 의존성의 개발 빌드 최적화 추가.
- 프런트엔드 요청 공유: 30초/최대 512개. 목록 재진입 시 중복 명령 방지 테스트 PC·모바일 통과. 빌드 성공.
- 실제 사용자 사진의 전후 소요시간은 측정하지 않았으므로 속도 향상 배수는 제시하지 않음.

## 상단 띠와 목록 이미지 부하 개선 (2026-09-08)

- 데스크톱 상단 메뉴 띠 72px → 84px, 기존 중앙 정렬 유지. 모바일 줄바꿈 구조 유지.
- `thumbnails.rs`, `media_thumbnail` 명령과 image 의존성 추가. 640px 캐시, EXIF 회전, 순차 생성, 임시 파일 후 교체.
- 공통 MediaImage는 화면 근처에서 썸네일 요청. 상세·달력 메인·앨범 내부는 original 옵션으로 원본 유지. 인물별 사진 목록도 썸네일 사용.
- 기존 사진 재등록 불필요. 앱 종료 후 `npm run tauri:dev`로 재시작. 최초 썸네일 생성 시 대기 발생 가능.
- Rust 6개 테스트 통과: 축소 크기·캐시 재사용·원본 불변 검사 포함. 실제 대량 사진의 성능 수치는 측정하지 않음.

## 미확인 얼굴 직접 제외

- 미확인 얼굴 영역에 선택 모드와 선택한 얼굴 제외 버튼 추가. 여러 그룹 선택 가능, 해당 그룹의 모든 얼굴을 기존 제외 명령으로 처리.
- 원형 체크, 얼굴 수 표시, 확인창 및 마지막 제외 되돌리기 유지. 등록 인물과 원본 사진은 제외 대상이 아님.
- 인물 관련 PC/모바일 테스트 6개 통과, 기존 2개 생략. 빌드 성공, 기존 얼굴 인식 청크 크기 경고 유지.

## 인물 분류와 제외 진입 개선

- 이름 있는 인물은 상단 `등록된 인물`, 빈 이름은 하단 `미확인 얼굴`로 분리. 공백 이름도 미확인 처리, 각 영역 독립 페이지네이션 적용.
- 인물 첫 화면에 `얼굴 선택하기` 추가. 바로 다중 선택 후 `얼굴 제외` 가능하며 기존 원본 보존·제외 되돌리기 유지.
- 인물 관련 Playwright PC/모바일 6개 통과, 기존 2개 생략. 프로덕션 빌드 성공. DB 변경 없음.

## 반려동물 수동 사진 모음 (2026-09-07)

- 별도 반려동물 메뉴 대신 `인물 → 사람 / 반려동물` 탭으로 구성. 탭 전환 시 화면 상태 유지, 키보드 탭 전환 지원.
- 이름·대표 사진 등록, 사진 다중 선택/행 드래그, 사진 연결 해제, 이름·대표 사진 수정, 등록 삭제 구현. 사진 상세보기와 연결.
- `pet`, `pet_media` 테이블 및 `list_pets`, `save_pet`, `delete_pet` Tauri 명령 추가. 원본 파일 이동/복사/삭제 없음. AI 학습/자동 인식 없음.
- React 구성: `PeopleWorkspace.tsx`, `PetsView.tsx`, `petService.ts`, `pets.css`. Rust 저장과 검증은 `pets.rs`에 분리.
- 빌드 성공. 전체 Playwright 26개 통과/2개 기존 생략, Rust 5개 통과. 기존 얼굴 인식 라이브러리의 큰 청크 경고는 남아 있음.
- 실행 중인 앱을 종료하고 `npm run tauri:dev`로 재시작하면 새 명령과 테이블 적용.

## 수동 얼굴 제외와 행 선택

- 인물 화면에 얼굴 모아보기, 선택 모드, 얼굴 제외, 마지막 제외 되돌리기 추가. 인물별 상세에서도 선택·제외 가능.
- `excluded_face` 추가 및 `set_faces_excluded` 명령 등록. 원본·기존 분석 기록 유지, 조회·재비교·새 분석 기준에서 제외.
- 사진 모아보기의 elementFromPoint 기반 드래그를 공통 useRowSelection으로 교체. 행 사이 전체 범위 선택·해제, 빠른 이동 시 누락 방지.
- Rust 테스트 4개, 전체 브라우저 테스트 24개 통과/2개 기존 생략. 빌드 확인.
- Tauri 명령과 테이블 추가가 있으므로 앱을 닫고 `npm run tauri:dev`로 다시 실행.


## 미확인 얼굴 다시 비교

- 인물 화면에 재비교 버튼과 24개씩 표시하는 후보 검토 모달 추가. 선택한 얼굴만 적용.
- Rust `find_face_matches` 명령 등록. 같은 모델 버전의 저장된 특징 벡터로 이름 있는 인물과 미확인 얼굴 비교. 작업 스레드에서 실행.
- 이름 있는 얼굴·직접 확정된 얼굴 보호, 애매한 후보 및 같은 사진의 인물 중복 제안 제외.
- SQLite 단위 테스트 3개 통과, 프런트엔드 빌드 통과. 원본·DB 스키마 변경 없음.
- 새 Rust 명령이므로 실행 중인 Tauri 앱을 종료한 뒤 `npm run tauri:dev`로 다시 실행해야 함.


## 중복 이름 인물 병합

- PeopleView 이름 저장 시 공백 제거 후 같은 이름 검색. 중복이면 저장 대신 합치기 표시.
- 확인 후 기존 move_faces 트랜잭션으로 전체 얼굴 연결 통합, 병합 대상 상세로 이동.
- 이미 같은 이름인 경우도 버튼 활성화. 빈 이름은 병합하지 않음. 원본 파일과 DB 스키마 변경 없음.
- 브라우저 회귀 검사에 공백 이름, 취소, 병합, 재조회, 이전 버전 중복 이름 사례 추가. 실제 Tauri DB 실행 검증과는 구분함.


## 2차 관리 구조 개선

- 달력·일정, 앨범 목록·편집·감상, 공통 이미지와 모달 훅을 App에서 분리. App 913줄.
- 앨범 전용 디자인 규칙을 `features/albums/albums.css`로 이동. 기존 공통 CSS와 전역 테마 유지.
- 모달 스택으로 최상위 창에만 키보드 이벤트 전달. 리스너 반복 등록 감소.
- 앨범 타이머 취소 및 빠른 중복 입력 잠금 추가. 다시 섞기 중 이전 페이지가 뒤늦게 적용되는 문제 방지.
- 테스트에 넘김 중 다시 섞기 회귀 검사 추가. 얼굴 모델 요청 관찰 전 기존 웹폰트 로딩 대기.
- 빌드 통과. CSS 60.15KB 유지. 원본과 DB·Rust 코드 변경 없음.


## 1차 코드 정리

- 미사용 8개 클래스 계열, 이전 장식 가상 요소, 참조 없는 `turnNext`/`turnPrev` 애니메이션 제거.
- 숨겨진 중복 제목·앨범 캡션 JSX와 명조 폰트 로딩 제거.
- `mockMedia.ts`, `ImportJob`, `sameMonthItems` 제거. 빈 초기 상태를 App에서 직접 선언.
- TypeScript 미사용 지역 변수·인수 검사 활성화.
- 빌드 통과, 전체 브라우저 테스트 22개 통과/2개 기존 생략. CSS 65.39KB → 60.15KB. 얼굴 모델 청크 크기 경고 유지.
- 데이터·원본·Rust 스키마 변경 없음. CSS 전역 통합이나 컴포넌트 분할은 이번 범위에 포함하지 않음.


## 표지 재배치 및 앨범 방향키

- 책 모양 유지, 상단 중앙 제목과 아래 사진으로 표지 재배치. 부가 문구 없음.
- 좌우 방향키로 책장 이동. 입력 필드·키 반복·넘김 중 중복 입력 제외. 상세창 표시 중에는 뒤 앨범 키보드 조작 차단.
- PC/모바일 디자인 테스트 2개 및 빌드 통과. 기존 얼굴 모델 청크 크기 경고는 남아 있음.


## 앨범 목록 크기 조정

- 표지 축소 배율 0.72를 1로 변경해 약 39% 확대. 목록 행 높이도 360px로 늘려 겹침 방지.
- 표지의 사진 장수 문구 제거. 표지 사진과 제목, 기존 선택·수정 기능은 유지.


## 앨범 두께 보강

- 표지 라이브러리 두께 48px, 기본 각도 32도, hover 각도 24도로 변경. 같은 좌표계에 책등 면 추가.
- 펼친 앨범 아래 종이 단면 21px와 하드커버 층 추가. 중앙 제본 음영 보강.
- PC/모바일 디자인 흐름 테스트 2개 통과. 페이지 넘김·상세보기 흐름 유지. 원본과 DB 변경 없음.


## 앨범 표지와 감상 화면 개편

- `book-cover-3d`는 유지하고 표지를 큰 사진과 하단 제목 띠로 재구성. 사용자 지정 색은 책등·바깥 여백에 적용. 표지 밖 중복 제목이나 카드는 추가하지 않음.
- 감상 화면을 상단 제목, 중앙 책, 하단 페이지 이동의 세 영역으로 분리. 페이지당 사진 두 장과 기존 넘김 타이밍 유지.
- 책장 번호 계산을 펼침당 두 쪽씩 증가하도록 수정. 원본 파일과 DB 변경 없음.


## 배치 구조 재설계 (색상 개편 이후)

- 왼쪽 고정 메뉴를 상단 가로 메뉴로 이동해 사진 영역이 화면 전체 폭을 사용하도록 변경.
- 등록·통계 영역을 제거하고 검색과 파일/폴더 등록을 화면 제목 옆으로 이동. 통계용 JSX와 계산 코드도 제거.
- 모아보기 탭과 선택·필터를 같은 줄에 배치. 모바일은 두 줄로 나눔.
- 상세보기의 오른쪽 편집 패널을 제거하고 전체 화면 사진, 하단 날짜·별점, 접을 수 있는 댓글 영역 순으로 재구성. 즐겨찾기는 상단 아이콘으로 이동.
- `tests/design.spec.ts`에 배치 좌표, 댓글 접기 시 사진 높이 증가, 아이콘 최소 크기 검증 추가.
- 이번 변경 검증: 디자인·기존 앱 테스트 18개 통과, `npm run build` 통과. 얼굴 모델 청크 크기 경고는 남아 있음. Tauri 실제 창 검사는 미실시.

## 전체 디자인 개편

- 사용자 제공 Apple/Nike/Raycast 분석 문서를 조합해 `src/design-system.css` 추가, 전역 시각 규칙 통일.
- 짙은 탐색 영역, 밝은 사진 캔버스, 작은 반경, SUIT 제목으로 변경. 긴 설명·등록 제목·브랜드 보조 문구 제거.
- 등록/통계 카드 축소, 모아보기 카드 외곽 제거, 상세·달력·일정·앨범·인물 조작부 재정리.
- 테스트용 사진 18장·앨범 9개로 PC/모바일 캡처. 모바일 정렬과 도구 배치 수정 후 재확인.
- 전체 브라우저 검사 22개 통과, 중복 추론 검사 2개 생략. 빌드 확인. DB와 원본 파일 변경 없음.

## 로컬 얼굴 분류 첫 버전

- 인물 준비 화면을 얼굴 목록·이름 저장·사진 상세 조회·선택 분리·이동 화면으로 교체.
- 실제 얼굴 분석: `@vladmandic/face-api`의 SSD 탐지, 68점 정렬, 얼굴 특징 추출. 연령·성별·감정 추론은 사용하지 않음.
- 모델 파일과 라이선스를 앱에 동봉하는 `scripts/prepare-face-models.mjs` 추가. 생성된 모델 폴더는 Git 제외.
- Rust 명령: `list_face_index`, `save_face_scan`, `rename_face_person`, `move_faces`, `clear_face_index`.
- SQLite: `detected_face`, `face_scan` 추가 및 기존 `person` 활용. 얼굴 정보 제거는 원본 사진과 앨범에 영향을 주지 않음.
- 한 장씩 분석, 중단·재개, 실패 사진 재시도, 모델 지연 로딩, 얼굴 목록 24개 페이지 처리.
- 빌드 및 Rust DB 테스트 2개 통과. 전체 E2E 20개 통과, 중복되는 모바일 실제 추론 검사 2개 의도적 생략. 관리 화면은 PC/모바일 모두 검사.
- 실제 모델 검출과 외부 요청 부재 확인. UI 저장 명령은 모의 Tauri로 검사했으며 Windows WebView와 사용자 DB를 연결한 실사용 검증은 미실시.
- 얼굴 분석은 Tauri 앱에서 `인물 > 얼굴 찾기`로 시작. Rust 명령 추가로 앱 재시작 필요.

이 문서는 `오래담은`을 실제 앱으로 만드는 과정에서 어떤 기술을 어디에 썼는지 정리한 구현 중심 학습 노트입니다.

## 현재 앱 구조

```text
React 화면
  ↓
TypeScript 서비스 어댑터
  ↓
Tauri invoke 명령
  ↓
Rust 백엔드
  ↓
SQLite 데이터베이스
```

각 층의 역할은 다릅니다.

- React: 사용자가 보는 화면을 만든다.
- TypeScript: 화면에서 쓰는 데이터 모양과 호출 방식을 정리한다.
- Tauri: 웹 화면과 데스크톱 기능을 연결한다.
- Rust: 파일 읽기, 폴더 탐색, SQLite 저장을 처리한다.
- SQLite: 등록된 미디어의 정리 정보를 저장한다.

## 주요 파일

- `src/App.tsx`: 전체 화면, 메뉴, 탭, 달력, 앨범 페이지 흐름
- `src/styles.css`: 레이아웃, 색상, 반응형, hover 상태
- `src/types/media.ts`: 사진/영상 데이터 타입 정의
- `src/features/media/mediaService.ts`: 날짜 그룹화, 지원 확장자 판단 같은 순수 로직
- `src/features/media/mockMedia.ts`: 브라우저 실행용 샘플 데이터
- `src/services/tauriMediaService.ts`: React에서 Tauri 명령을 호출하는 연결부
- `src-tauri/src/lib.rs`: 실제 파일 등록, 목록 조회, 별점/코멘트 저장 명령
- `src-tauri/database/schema.sql`: SQLite 테이블 설계
- `tests/app.spec.ts`: 사용자 흐름 자동 테스트
- `scripts/capture-screenshots.mjs`: QA용 스크린샷 캡처

## 파일 등록 흐름

사용자가 `파일 선택` 또는 `폴더 선택`을 누르면 다음 순서로 동작합니다.

1. React 화면에서 버튼 클릭을 받는다.
2. Tauri dialog plugin이 운영체제의 파일 선택 창을 연다.
3. 선택된 파일 또는 폴더 경로를 Rust 명령 `register_paths`로 보낸다.
4. Rust가 경로를 확인한다.
5. 폴더라면 내부 파일을 재귀적으로 탐색한다.
6. 이미지/영상 확장자만 골라낸다.
7. SQLite에 파일 경로, 이름, 종류, 날짜 정보를 저장한다.
8. React가 `list_media`를 다시 호출해 화면 목록을 갱신한다.

이 구조의 장점은 화면 코드가 파일 시스템을 직접 만지지 않는다는 점입니다. 화면은 “등록해줘”라고 요청하고, 실제 로컬 권한 작업은 Tauri/Rust가 처리합니다.

## 왜 mock 데이터가 아직 필요한가

Tauri 앱으로 실행하면 실제 파일 등록을 사용합니다. 하지만 브라우저에서 `npm run dev`로 화면만 확인할 때는 운영체제 파일 접근 권한이 없습니다.

그래서 브라우저 모드에서는 mock 데이터를 보여줍니다. 이 덕분에 디자인과 사용자 흐름은 빠르게 확인하고, 실제 파일 등록은 Tauri 모드에서 검증할 수 있습니다.

## SQLite에 저장하는 것과 저장하지 않는 것

저장하는 것:

- 파일 경로
- 파일 이름
- 사진/영상 구분
- 촬영일 또는 기준 날짜
- 별점
- 코멘트
- 즐겨찾기 여부

저장하지 않는 것:

- 원본 사진 파일 자체
- 원본 영상 파일 자체
- 클라우드 계정 정보
- 서버 업로드 데이터

이렇게 나눈 이유는 원본 파일을 안전하게 유지하면서도 앱 안에서 정리와 검색을 가능하게 하기 위해서입니다.

## 현재 구현된 기능

- 한국어 기본 UI
- 앱 이름 `오래담은`
- `사진보기` 메뉴 안의 `모아보기`, `달력보기`, `앨범보기` 탭
- 연도/월 셀렉트 박스로 과거 날짜 이동
- 달력 칸에서 해당 날짜의 사진/영상 확인
- 실제 앨범처럼 좌우 책장 형태로 넘기는 앨범 화면
- 한쪽 페이지마다 사진을 위아래 2장까지 배치하는 앨범 레이아웃
- 앨범보기의 전체창 감상 모드
- 앨범 전체창의 정방향 사진 배치
- 파일 선택으로 실제 미디어 등록
- 폴더 선택으로 하위 미디어 등록
- 파일/폴더 등록 중 로딩 표시와 버튼 비활성화
- Tauri 런타임에서 이미지 파일을 실제 썸네일처럼 표시
- 앨범보기 사진 클릭 시 오른쪽 감상 패널 선택 변경
- 오른쪽 고정 감상 패널 제거
- 사진/영상 클릭 시 상세 모달에서 별점, 즐겨찾기, 코멘트 편집
- `선택` 모드에서 사진 다중선택
- 모아보기 화면에서 드래그로 여러 항목 선택
- 선택 모드 진입 버튼을 `모아보기` 패널 헤더 내부로 이동
- 선택 항목 삭제와 선택 항목으로 앨범 만들기 명령 연결
- 디자인 시스템을 `styles.css` 토큰 중심으로 재정리
- 설정 화면에서 등록 목록 전체 비우기
- 파일 내용 해시 기반 중복 등록 방지
- 브라우저 데모용 샘플 미디어 제거
- SQLite 저장
- 별점, 코멘트, 즐겨찾기 저장 명령

## 등록 중 로딩 상태를 넣은 이유

파일 하나를 등록할 때는 거의 즉시 끝날 수 있지만, 폴더 등록은 하위 폴더를 재귀적으로 살펴보고 지원 확장자를 걸러 SQLite에 저장해야 합니다. 사진과 영상이 많으면 몇 초 이상 걸릴 수 있습니다.

그래서 React 상태에 `importing`을 두고, 등록 작업이 시작되면 `files` 또는 `folder` 값을 넣습니다. 작업이 끝나면 `finally`에서 다시 `null`로 돌립니다. 이 방식은 성공, 취소, 오류 상황 모두에서 로딩 상태가 풀리게 해줍니다.

화면에서는 이 상태를 사용해 다음을 처리합니다.

- 등록 중 버튼 비활성화
- 회전 아이콘 표시
- `파일 등록 중`, `폴더 등록 중` 문구 표시
- `role="status"`와 `aria-live="polite"`로 보조 기술에도 상태 전달

## 실제 이미지 표시를 붙인 이유

처음 MVP에서는 `thumbnail`에 실제 이미지 경로가 아니라 CSS 그라디언트를 넣었습니다. 이 방식은 화면 배치와 디자인을 빠르게 확인하기에는 좋지만, 실제 사진 앱처럼 보이지 않는 한계가 있습니다.

Tauri에서는 로컬 파일 경로를 그대로 `<img src>`에 넣지 않고 `convertFileSrc`로 앱에서 읽을 수 있는 URL로 바꿔야 합니다. 그래서 이미지 항목은 `filePath`를 `convertFileSrc(item.filePath)`로 변환한 뒤 카드, 달력, 앨범, 감상 패널에 `<img>`로 표시하도록 바꿨습니다.

영상과 음원은 아직 프레임 추출 썸네일이 없으므로 기존 그라디언트와 아이콘을 유지합니다. 다음 단계에서 FFmpeg나 별도 썸네일 생성 로직을 붙이면 영상도 실제 장면 이미지로 보여줄 수 있습니다.

## 등록 목록 비우기와 원본 삭제의 차이

`clear_registered_media` 명령은 SQLite의 `media` 테이블을 비웁니다. 이 작업은 앱 안의 등록 목록만 지우며, 사용자의 실제 사진/영상 파일은 삭제하지 않습니다.

이 구분이 중요한 이유는 이 앱의 핵심 원칙이 원본 보호이기 때문입니다. 사용자가 “앱에서 지우기”를 눌렀을 때 원본 파일까지 사라지면 위험하므로, 현재 단계에서는 등록 해제만 지원합니다.

## 중복 등록 방지 방식

기존에는 `file_path`에 `UNIQUE` 제약을 걸어 같은 경로가 두 번 저장되지 않게 했습니다. 하지만 같은 파일을 다른 폴더에 복사해두면 경로가 달라져 중복으로 들어갈 수 있습니다.

이번 변경에서는 파일 내용을 읽어 간단한 해시값을 만들고 `content_hash` 컬럼에 저장합니다. 같은 내용의 파일은 경로가 달라도 같은 해시가 나오므로, SQLite의 unique index로 중복 저장을 막을 수 있습니다.

주의할 점:

- 지금 해시는 중복 방지를 위한 실용적 식별값이지 보안용 암호 해시가 아닙니다.
- 아주 큰 파일은 해시 계산 때문에 등록 시간이 더 걸릴 수 있습니다.
- 그래서 등록 중 로딩 표시가 더 중요해졌습니다.

## DB 마이그레이션 오류 메모

기존 사용자 DB에는 `content_hash` 컬럼이 없었습니다. 그런데 `schema.sql`에서 `content_hash` 인덱스를 먼저 만들려고 해서 앱 시작 시 `no such column: content_hash` 오류가 났습니다.

해결 방식:

- `schema.sql`은 새 DB의 기본 테이블을 만드는 역할로 둔다.
- 기존 DB 업그레이드는 Rust의 `migrate_database`에서 처리한다.
- `ALTER TABLE media ADD COLUMN content_hash TEXT`를 먼저 실행한다.
- 그 다음 `CREATE UNIQUE INDEX IF NOT EXISTS idx_media_content_hash`를 실행한다.

학습 포인트는 “새 DB 생성”과 “기존 DB 변경”을 같은 파일에 섞으면 순서 문제가 생길 수 있다는 점입니다.

## 샘플 데이터 제거 메모

초기 MVP에서는 화면을 빠르게 확인하기 위해 `mockMedia.ts`에 샘플 사진과 영상을 넣었습니다. 하지만 실제 앱 사용 단계에서는 샘플이 남아 있으면 “내가 등록한 사진”과 “데모 사진”이 섞여 보입니다.

이번 변경에서는 `mockMedia.ts`를 빈 배열로 바꾸고, 등록된 항목이 없을 때 각 화면이 빈 상태 안내를 보여주도록 수정했습니다. 이제 앱은 실제 등록한 파일만 보여주는 상태에서 시작합니다.

## Windows 경로와 썸네일 오류 메모

Tauri에서 로컬 이미지를 보여줄 때는 `convertFileSrc`로 파일 경로를 앱 내부 asset URL로 바꿉니다. 그런데 Rust의 `canonicalize()`가 Windows 경로를 `\\?\C:\...` 형태의 확장 경로로 반환할 수 있습니다.

이 값이 그대로 프론트엔드로 넘어가면 `http://asset.localhost/%5C%5C%3F%5CC%3A...` 같은 URL이 만들어지고, 이미지 로딩이 실패할 수 있습니다.

해결 방식:

- 새로 등록하는 파일 경로는 Rust에서 `\\?\` 접두사를 제거해 저장한다.
- 기존 DB에 남은 경로도 앱 시작 시 마이그레이션에서 정리한다.
- 프론트엔드에서도 `convertFileSrc` 직전에 한 번 더 경로를 정규화한다.

학습 포인트는 “운영체제 내부 경로 표현”과 “웹뷰에서 접근 가능한 URL”이 같은 것이 아니라는 점입니다.

## Tauri asset protocol 메모

`convertFileSrc`는 로컬 파일 경로를 `asset.localhost` URL로 바꿔주지만, Tauri 설정에서 asset protocol이 꺼져 있으면 실제 파일을 서빙하지 못합니다. 이 경우 DevTools에는 `net::ERR_CONNECTION_REFUSED`가 표시됩니다.

해결 방식:

- `src-tauri/tauri.conf.json`의 `app.security.assetProtocol.enable`을 `true`로 켠다.
- 로컬 앨범 앱 특성상 사용자가 직접 선택한 사진 경로를 보여줘야 하므로 `scope`를 허용한다.
- 브라우저의 `npm run dev`가 아니라 `npm run tauri:dev`로 실행해 Tauri asset protocol 안에서 확인한다.

## 상세 모달 구조 메모

오른쪽 고정 감상 패널은 항상 화면 공간을 차지해서 사진 목록과 앨범보기 영역을 좁게 만들었습니다. 사용자가 평소에는 많은 사진을 훑어보고, 필요할 때만 상세를 열어보는 흐름이 더 자연스럽기 때문에 상세 정보를 모달로 옮겼습니다.

변경 후 구조:

- 기본 화면은 목록/달력/앨범 영역을 넓게 사용한다.
- 사진이나 영상을 클릭하면 `DetailModal`이 열린다.
- 모달 안에서 큰 미리보기, 별점, 코멘트, 즐겨찾기를 편집한다.
- 바깥 영역 클릭, 닫기 버튼, `Esc` 키로 모달을 닫을 수 있다.

학습 포인트는 “항상 필요한 정보”와 “선택했을 때만 필요한 정보”를 분리하면 화면 밀도를 더 효율적으로 조절할 수 있다는 점입니다.

## CSS 디자인 토큰 메모

색상, radius, shadow처럼 반복되는 디자인 값은 CSS 변수로 빼두면 전체 톤을 바꾸기 쉽습니다.

이번 변경에서는 `--bg`, `--surface`, `--primary`, `--secondary`, `--border`, `--text`, `--radius-card` 같은 변수를 만들고, 기존 녹색/한지 중심 스타일을 따뜻한 크림/브라운 계열로 다시 정리했습니다.

학습 포인트는 디자인 시스템이 단순히 예쁜 색 목록이 아니라, 화면 전체에서 같은 규칙이 반복되도록 만드는 기준이라는 점입니다.

## 상단 레이아웃 재구성 메모

이번 변경에서는 `src/App.tsx`의 상단 구조를 `topbar → overviewGrid → contentGrid` 흐름으로 정리했습니다.

각 영역의 역할:

- `topbar`: 현재 메뉴 이름과 검색
- `overviewGrid`: 등록 행동과 앨범 현황
- `memoryIntro`: 파일/폴더 등록 버튼과 등록 중 상태
- `summaryStack`: 전체 기록, 사진, 영상, 즐겨찾기 수
- `contentGrid`: 모아보기, 달력보기, 앨범보기 같은 실제 감상 영역

React 쪽에는 `mediaStats`를 `useMemo`로 계산하도록 추가했습니다. 사진 수나 즐겨찾기 수는 `items`가 바뀔 때만 다시 계산하면 되므로, 매 렌더링마다 새로 계산하지 않아도 됩니다.

CSS 쪽에는 `overviewGrid`, `summaryStack`, `statCard`를 추가했습니다. 데스크톱에서는 등록 영역과 현황 카드를 나란히 두고, 태블릿과 모바일에서는 한 줄로 쌓이게 했습니다. 모바일 메뉴는 5개 항목이 두 줄로 내려가 화면을 과하게 차지하지 않도록 가로 스크롤 한 줄 구조로 바꿨습니다.

## 앨범 전체창과 정방향 배치 메모

앨범보기는 일반 패널 안에 있으면 사진 목록의 또 다른 표현처럼 보입니다. 사용자가 “앨범을 펼친다”고 느끼려면 주변 UI가 잠시 사라지고, 책장 자체가 화면의 중심이 되어야 합니다.

이번 변경에서는 앨범 탭 안에 `전체창으로 보기` 버튼을 두고, 누르면 `albumFullscreen` 레이어가 화면 전체를 덮도록 만들었습니다. 닫기는 우측 상단 버튼과 `Esc` 키로 처리합니다.

사용한 기술과 라이브러리:

- React: `readerOpen`, `pageIndex`, `turning` 같은 화면 상태 관리
- CSS: `position: fixed`, `perspective`, `rotateY`, `animation`, CSS Grid로 전체창과 책장 넘김 구현
- lucide-react: `BookOpen`, `Maximize2`, `Dices`, `ChevronLeft`, `ChevronRight`, `X` 아이콘 사용

새 앨범 전용 라이브러리는 추가하지 않았습니다. 현재 요구사항은 복잡한 물리 기반 페이지 넘김보다 “전체창”, “책장 넘김”, “정방향 사진 배치”가 핵심이므로 React와 CSS만으로 구현하는 편이 의존성을 늘리지 않고 유지보수하기 좋습니다.

정방향 앨범 배치는 두 단계로 만듭니다.

1. `makeAlbumPages()`에서 미디어를 섞고, 한 펼침에 최대 4개씩 나눕니다.
2. CSS Grid로 좌우 페이지에 각각 위아래 2장까지 정방향으로 배치합니다.

주의할 점:

- 사진 배치가 랜덤이기 때문에 매번 같은 화면을 보장하지 않습니다.
- 사진 자체는 기울이지 않고 정방향으로 둡니다.
- 모바일에서도 같은 방향성을 유지하되 화면 폭에 맞게 한쪽 페이지가 세로로 쌓입니다.

## 배경 이미지 제거 메모

초기 디자인에서는 한지와 오래된 사진첩 느낌을 주기 위해 배경 이미지를 사용했습니다. 하지만 실제 사진 앱에서는 배경 그림이 사용자의 사진보다 먼저 눈에 들어오면 감상 경험을 방해할 수 있습니다.

이번 변경에서는 `src/styles.css`의 `url("/assets/korean-memory-bg.png")` 참조를 제거하고, 따뜻한 크림색과 모카색 계열의 색 배경만 남겼습니다.

학습 포인트:

- 사진 앱에서는 사용자의 사진이 가장 중요한 시각 요소다.
- 배경 이미지는 분위기를 줄 수 있지만, 실제 콘텐츠와 경쟁하면 제거하는 편이 낫다.
- 색상만으로도 충분히 제품의 정서를 만들 수 있다.

## 앨범 사진 프레임 변경 메모

전체창 앨범의 사진 프레임은 처음에는 흰 여백과 파일명이 아래에 붙는 폴라로이드 카드에 가까웠습니다. 하지만 사용자가 원하는 방향은 “사진 두 장을 앨범 페이지에 붙여둔 느낌”이므로, 장식적인 종이 여백을 줄이고 일반 인화사진처럼 보이게 바꿨습니다.

수정 방식:

- 전체창 앨범 안의 `.albumPhoto` padding을 제거했다.
- 하단 파일명 캡션은 화면에서 숨겼다.
- 버튼 접근성은 유지하기 위해 `aria-label`에 파일명을 넣었다.
- 사진 프레임은 `16 / 9` 가로형 직사각형 비율로 바꿨다.
- 사진 바깥에는 `2px` 흰 테두리를 두고, 모서리는 `4px`만 둥글게 했다.
- 바깥 배경 그라데이션은 제거하고 단색 모카 배경만 남겼다.
- 앨범 안 사진 묶음은 위아래 중앙이 맞도록 `align-content: center`로 정렬했다.
- 하단 페이징은 앨범 두께 그림자에 가리지 않도록 책장 높이와 아래 간격을 조정했다.

학습 포인트:

- 같은 사진이어도 프레임이 두꺼우면 폴라로이드나 카드처럼 보인다.
- 일반 인화사진처럼 보이려면 사진 주변 장식을 줄이고, 사진 면적이 프레임보다 먼저 보여야 한다.

## 책장 넘김 애니메이션 보강 메모

처음 구현한 책장 넘김은 실제 페이지가 넘어간다기보다 페이지가 살짝 흔들리는 느낌에 가까웠습니다. 원인은 두 가지였습니다.

- 클릭하자마자 `pageIndex`가 바뀌어 넘김 중간 장면이 짧게 느껴졌다.
- CSS 회전 각도가 `10deg` 정도라 실제 종이가 넘어가는 운동처럼 보이지 않았다.

수정 방식:

- `turnPage()`에서 `pageIndex` 변경을 애니메이션 중간 이후로 늦췄다.
- 넘김 중에는 이전/다음 버튼을 잠시 비활성화해 중복 클릭을 막았다.
- `rotateY()` 각도를 거의 `180deg`까지 키워 좌우 페이지가 중앙 축을 기준으로 넘어가게 했다.
- `.bookSpread.immersive::before`, `.bookSpread.immersive::after` 가상 요소를 추가해 실제로 넘어가는 종이 레이어를 만들었다.
- `paperLeafNext`, `paperLeafPrev` 키프레임으로 종이 면의 밝기와 그림자를 함께 바꿨다.

학습 포인트:

- 화면 전환 애니메이션은 “상태 변경 시점”과 “시각 효과”가 같이 맞아야 자연스럽다.
- 실제 물체처럼 보이려면 회전뿐 아니라 그림자, 밝기 변화, 중심축이 함께 설계되어야 한다.

추가 보강:

- 실제 좌우 페이지 자체를 회전시키지 않고, 넘김 전용 `paperTurnLayer`만 회전하도록 바꿨다.
- `pageIndex` 변경 시점을 넘김 시작 직후로 당겨, 책장이 넘어간 뒤 새 페이지가 뒤늦게 리로드되는 느낌을 줄였다.
- 좌우 페이지 하단에 여러 겹의 얇은 그림자를 넣어 앨범 종이 두께감을 만들었다.
- 좌우 페이지 내용을 별도 상태로 분리해, 넘겨지는 페이지는 즉시 바꾸고 반대편 페이지는 애니메이션 중반에 바뀌도록 했다.

학습 포인트:

- 실제 콘텐츠 레이어와 전환 효과 레이어를 분리하면 애니메이션 중 콘텐츠가 사라지는 문제를 줄일 수 있다.
- 두께감은 큰 그림자 하나보다 얇은 선과 그림자를 여러 겹 쌓을 때 더 자연스럽게 보인다.
- 양쪽 페이지가 있는 UI에서는 전체 상태 하나보다 왼쪽/오른쪽 상태를 분리해야 전환 타이밍을 세밀하게 제어할 수 있다.

## 다중선택과 드래그 선택 메모

사진 앱에서는 하나씩 상세를 보는 흐름과 여러 장을 골라 정리하는 흐름이 다릅니다. 그래서 평소 클릭은 상세 모달을 열고, 상단의 `선택` 버튼을 누른 뒤에는 클릭이 선택 토글로 동작하게 분리했습니다.

드래그 선택은 `pointerdown`, `pointermove`, `pointerup` 이벤트로 구현했습니다. 드래그 중 현재 마우스 위치의 `[data-media-id]` 요소를 찾아 아직 처리하지 않은 항목이면 선택 상태를 바꿉니다.

선택된 항목으로 할 수 있는 일:

- 등록 목록에서 삭제
- 새 앨범 만들기

삭제는 원본 파일을 지우지 않고 SQLite 등록 목록에서만 제거합니다. 앨범 만들기는 `album`, `album_item` 테이블에 선택 항목의 순서를 저장합니다.

## 아직 구현하지 않은 것

- EXIF 촬영일 읽기
- 영상 메타데이터 읽기
- 실제 썸네일 생성
- 원본 파일 이동/삭제 감지
- 태그 관리
- 인물 자동 분류
- 중복 사진 정교한 판별

현재 날짜는 파일 수정일을 기준으로 저장합니다. 사진의 실제 촬영일과 다를 수 있으므로, 다음 단계에서는 EXIF 읽기를 붙이는 것이 좋습니다.

## Rust/Cargo 관련 메모

Rust는 언어이고, Cargo는 Rust 프로젝트의 빌드 도구이자 패키지 관리자입니다.

이 프로젝트에서 Cargo가 하는 일:

- Tauri 백엔드 코드를 컴파일한다.
- `rusqlite`, `walkdir` 같은 Rust 의존성을 내려받고 관리한다.
- `cargo check`로 Rust 코드가 빌드 가능한지 빠르게 검사한다.

Windows에서는 Rust 툴체인이 GNU와 MSVC로 나뉠 수 있습니다. 이 프로젝트는 Tauri Windows 빌드 호환성을 위해 `stable-x86_64-pc-windows-msvc`를 사용하도록 `src-tauri/rust-toolchain.toml`에 고정했습니다.

## 확인한 명령

```bash
cargo --version
rustc --version
cargo check
npm run build
npm run test:e2e
npm run tauri:dev
```

현재 `cargo check`, `npm run build`, `npm run test:e2e`, `npm run tauri:dev`까지 통과한 상태입니다.

## 다음 구현 순서 추천

1. EXIF 촬영일 읽기
2. 썸네일 생성 및 캐시 폴더 설계
3. 등록된 원본 파일이 사라졌을 때의 표시 방식
4. 태그/장소/인물 분류 테이블 추가
5. 검색과 필터 성능 점검

## 달력 보기 연월 선택과 모달 잠금

월간 달력과 사진이 있는 날 보기는 선택 기준이 다릅니다.

- `월간 달력`은 일반 달력처럼 사진이 없는 과거/미래 연월도 볼 수 있어야 하므로 연도 범위를 넓게 제공하고, 월은 1월부터 12월까지 모두 선택할 수 있게 했다.
- `사진 있는 날`은 실제 사진 기록이 있는 연월만 의미가 있으므로 등록된 사진의 `takenAt` 값을 기준으로 연도와 월 옵션을 제한했다.
- 사진 있는 날 보기에서는 선택된 연월 안에서 사진이 찍힌 날짜만 카드로 보여준다.

모달 동작도 정리했다.

- 달력 상세 모달과 사진 상세 모달은 배경 클릭으로 닫히지 않는다.
- 닫기는 닫기 버튼 또는 `Esc` 키로만 처리한다.
- 모달이 떠 있는 동안에는 본문 스크롤을 잠가 원래 페이지를 조작하는 느낌이 나지 않게 했다.

학습 포인트:

- 같은 `select` UI라도 보기 모드의 목적이 다르면 옵션을 따로 계산해야 한다.
- 모달은 단순히 화면 위에 띄우는 것뿐 아니라, 배경 화면의 조작 가능 여부까지 함께 설계해야 한다.

## 상세보기 화면 개편

상세보기는 사진 감상이 가장 중요한 화면이므로 사진 영역과 정보 편집 영역을 분리했다.

- 왼쪽은 큰 사진 감상 영역으로 두고, 오른쪽은 날짜, 용량, 별점, 코멘트, 즐겨찾기를 정리했다.
- 상세보기 사진에 있던 흰색 내부 테두리와 하단 그라데이션 장식을 제거했다.
- 확대 버튼을 추가해 사진을 더 크게 볼 수 있게 했다.
- 확대 상태에서는 사진 영역 안에서 스크롤하며 볼 수 있고, 버튼 상태는 `aria-pressed`로 표현한다.

학습 포인트:

- 상세보기는 목록 카드와 다른 목적을 가진다. 목록 카드는 구분이 중요하지만, 상세보기는 사진 자체에 시선이 가야 한다.
- 같은 이미지 컴포넌트를 쓰더라도 화면 목적에 따라 장식선, 그라데이션, 여백을 다르게 적용해야 한다.
- 확대 기능은 새 창을 열기보다 현재 감상 흐름 안에서 토글되게 만들면 사용자의 맥락이 덜 끊긴다.

추가 조정:

- 이전/다음 버튼은 상단 헤더가 아니라 사진 박스 안 좌우 중앙에 배치했다.
- 확대 기능은 아이콘 버튼만 남기고 별도 설명 문구는 제거했다.
- 닫기 버튼은 밝은 헤더 위에서 잘 보이도록 더 진한 배경색과 그림자를 적용했다.

## 상세보기 댓글형 코멘트

기존 상세보기 코멘트는 단일 textarea라 작성 후 확정하는 흐름이 없었다. 이를 댓글형 입력으로 바꿨다.

- 댓글 입력은 `작성자`와 `내용`을 따로 받는다.
- `확인` 버튼을 눌러야 댓글 목록에 추가된다.
- 댓글은 작성자, 작성일, 내용이 보이는 카드 형태로 표시한다.
- 기존 `MediaItem.comment`는 목록과 검색에서 쓸 대표 코멘트로 유지하고, 새 댓글을 추가하면 마지막 댓글 내용으로 갱신한다.
- 실제 댓글 목록은 `localStorage`의 `oraedameun.mediaComments`에 미디어 ID별로 저장한다.

학습 포인트:

- 입력 중인 값과 저장된 댓글 목록은 다른 상태로 분리해야 한다.
- 기존 단일 필드를 바로 없애면 목록/검색/기존 데이터가 깨질 수 있으므로 대표 코멘트로 남겨 호환성을 유지했다.

추가 조정:

- 댓글 작성자의 기본값은 빈값으로 둔다.
- 상세보기 모달에서 `ArrowLeft`는 이전 사진, `ArrowRight`는 다음 사진, `Esc`는 닫기로 동작한다.
- 입력칸이나 textarea에 포커스가 있을 때는 방향키를 사진 이동으로 쓰지 않아 글자 편집 흐름을 방해하지 않는다.
- 로컬 앱이므로 댓글 작성자와 상관없이 누구나 댓글을 수정하거나 삭제할 수 있게 했다.
- 댓글 수정은 카드 안에서 작성자와 내용을 다시 입력한 뒤 `저장`으로 확정한다.
- 댓글 삭제 후 남은 마지막 댓글을 대표 코멘트로 유지하고, 댓글이 없으면 대표 코멘트도 비운다.

## 앨범보기 진입 흐름

앨범보기는 별도 런처 화면을 거치지 않고 탭을 누르는 순간 전체창 앨범으로 바로 열린다.

- `전체창으로 보기` 버튼과 중간 안내 런처 UI를 제거했다.
- 앨범에 사진이 있으면 `Albums` 컴포넌트가 열릴 때 `readerOpen`을 바로 활성화한다.
- 사진이 없을 때는 기존 빈 상태 안내만 보여준다.
- 전체창을 닫으면 앨범 탭 화면으로 돌아오며, 다시 보려면 다른 보기로 이동했다가 앨범보기를 다시 선택하면 된다.

## 만든 앨범 목록

사진보기의 모아보기에서 `앨범 만들기`로 만든 앨범을 다시 볼 수 있는 `내 앨범` 메뉴를 추가했다.

- `앨범보기` 탭은 전체 사진을 책처럼 감상하는 보기다.
- `내 앨범` 메뉴는 사용자가 만든 앨범 목록을 보는 곳이다.
- Tauri 환경에서는 SQLite의 `album`, `album_item` 테이블을 읽는 `list_albums` 명령을 추가했다.
- 브라우저 개발 환경에서는 앱 상태에 임시 앨범을 저장해 같은 UI 흐름을 테스트할 수 있게 했다.
- 앨범 카드를 누르면 해당 앨범에 담긴 사진만 전체창 책장으로 열린다.

학습 포인트:

- “보기 방식”과 “사용자가 만든 데이터 목록”은 역할이 다르므로 메뉴를 분리하는 편이 명확하다.
- 이미 만든 책장 UI는 공통 리더 컴포넌트로 분리해 전체 사진 보기와 저장된 앨범 보기에서 함께 재사용했다.

디자인 조정:

- `내 앨범` 카드는 단순 사진 카드가 아니라 실제 앨범 표지처럼 보이게 책등과 페이지 두께를 표현했다.
- 한 화면에서 9개 정도를 훑어볼 수 있도록 데스크톱 기준 3열 그리드로 조정했다.
- 참고 이미지처럼 세워둔 하드커버 앨범에 가깝게 표지 비율을 세로형으로 바꾸고, 중앙에는 작은 사진 창을 배치했다.
- 목록 화면에서는 외곽 카드 테두리, 하단 제목, 사진 수, 생성일, 색상 스와치를 제거하고 앨범 표지만 보이게 했다.
- 앨범 제목은 표지 안에 이미 있으므로 같은 정보를 하단에 반복하지 않는다.
- 표지색 변경은 목록에서 바로 처리하지 않고, 이후 앨범 수정 화면에서 다루는 기능으로 분리한다.
- 앨범을 더 크게 키우고 약간 옆을 보는 각도로 회전시켜 실제 책장에 세운 앨범 같은 인상을 강화했다.
- 과한 3D 회전은 표지와 책등을 함께 비틀어 부자연스러워 보일 수 있어 제거했다.
- 표지는 거의 정면 직사각형으로 두고, 왼쪽 책등과 아래 페이지 층을 따로 표현해 참고 이미지처럼 세워 둔 앨범의 각도를 만들었다.
- 중앙 사진 창은 표지 대비 너무 작아 보이지 않도록 키웠다.
- 하단 페이지 묶음은 표지 밖에 떠 있는 받침처럼 보이지 않도록 표지 뒤쪽으로 넣고, 왼쪽 책등과 시작 위치를 맞췄다.
- 책등은 폭과 내부 그림자를 키워 실제 하드커버 앨범의 두꺼운 좌측 대처럼 보이게 조정했다.
- 이후 책등이 너무 넓은 색 덩어리처럼 보여 폭을 다시 줄이고, 왼쪽 옆면 레이어와 접합 그림자로 두께감을 표현했다.
- 하드커버 앨범의 두께는 단순한 폭보다 가장자리 그림자, 옆면, 표지와의 겹침으로 만드는 편이 더 자연스럽다.
- 앨범 전체를 하나의 3D 좌표계로 다시 잡아 표지, 책등, 페이지가 같은 각도로 움직이게 했다.
- 오른쪽 뒤판처럼 보이던 색 레이어는 제거하고 그림자 레이어로 낮춰, 별도 조각처럼 튀어나오는 느낌을 줄였다.
- 최종적으로 `book-cover-3d` 라이브러리를 도입해 앨범 표지의 3D 각도와 두께 표현을 맡겼다.
- 직접 만든 `savedAlbumSpine`, `savedAlbumPages` 구조는 표지와 각도가 따로 놀기 쉬워 제거했다.
- 현재 CSS는 라이브러리 안에 들어가는 앞표지 디자인, 제목, 작은 사진 창만 담당한다.
- Tauri 환경에서는 `album.cover_color` 컬럼과 `update_album_cover_color` 명령으로 표지색을 저장한다.
- 기존 DB에는 `ALTER TABLE album ADD COLUMN cover_color` 마이그레이션으로 기본 연녹색 표지를 추가한다.

학습 포인트:

- 목록 화면은 “빠르게 찾고 여는 곳”이고, 수정 화면은 “속성을 바꾸는 곳”이다.
- 두 역할을 한 화면에 섞으면 사용자는 무엇을 눌러야 하는지 헷갈리기 쉽다.
- 같은 정보가 화면 안에서 반복되면 시선이 흩어지므로, 표지 안 제목처럼 이미 보이는 정보는 다시 표시하지 않는 편이 낫다.
- 실물 사물 느낌을 낼 때는 전체 요소를 한 번에 3D 변형하기보다, 표지/책등/페이지처럼 실제 구조를 나눠 표현하는 편이 더 자연스럽다.

## 달력 일정 등록 MVP

달력보기 화면에 일정 등록 기능을 추가했다.

- 일정 분류는 `생일`, `기념일`, `기일`, `약속`으로 시작했다.
- 각 분류에는 Lucide 아이콘을 붙여 달력 칸과 상세 화면에서 빠르게 구분할 수 있게 했다.
- 일정은 날짜별로 `localStorage`에 저장한다. 아직 SQLite 테이블로 영구 저장하지는 않았다.
- `디데이 표시` 체크박스를 두어 일정별로 `D-day`, `D-5`, `D+3` 같은 표시를 켜고 끌 수 있게 했다.
- 달력 칸에는 메모, 일정 분류, 디데이가 함께 보인다.
- 새 일정 등록은 날짜 상세 모달 안이 아니라 달력보기 화면의 `일정 등록` 버튼에서 시작한다.
- 일정 등록 모달에서 날짜, 분류, 내용, 디데이 표시 여부를 선택한다.
- 날짜 상세 모달에서는 그날 등록된 일정을 확인하고, 디데이 표시를 켜고 끄거나 삭제만 할 수 있다.

윈도우 달력/알림 연동 메모:

- 윈도우 기본 달력 앱에 직접 쓰는 기능은 별도 시스템 연동이 필요해서 MVP 범위에는 넣지 않았다.
- 현실적인 다음 단계는 Tauri 알림 플러그인으로 윈도우 알림을 띄우거나, `.ics` 캘린더 파일 내보내기를 지원해 사용자가 윈도우/구글/아웃룩 달력에 가져가게 하는 방식이다.
- 알림을 “날짜가 다가오면 자동으로” 띄우려면 앱이 백그라운드에서 실행되거나 시작 프로그램으로 등록되어 있어야 한다.

## Git 저장소 용량 정리

문제:

- 프로젝트가 너무 커져 GitHub에 올리기 어려운 상태였다.
- 확인 결과 `src-tauri/target/` 안의 Rust/Tauri 빌드 산출물 7,660개가 Git 추적 대상에 포함되어 있었다.
- `src-tauri/target/`은 소스 코드가 아니라 Cargo가 빌드 중 자동으로 만드는 결과물이므로 저장소에 올릴 필요가 없다.
- 화면 QA용 `screenshots/`도 다시 생성 가능한 작업 산출물이므로 기본적으로 Git에서 제외하는 편이 좋다.

정리한 내용:

- `.gitignore`에 `src-tauri/target/`, `screenshots/`, 로컬 DB 파일, 환경 파일, 로그 파일을 추가했다.
- `git rm --cached`로 기존에 추적 중이던 빌드 산출물과 QA 스크린샷을 Git 인덱스에서 제거했다.
- 이 명령은 로컬 파일을 삭제하지 않고 Git 추적 목록에서만 제거한다.

학습 포인트:

- `.gitignore`는 “앞으로 새로 추가될 파일”을 무시한다.
- 이미 Git이 추적하고 있는 파일은 `.gitignore`에 추가해도 자동으로 빠지지 않는다.
- 이미 추적 중인 파일을 저장소에서만 빼려면 `git rm --cached`를 사용한다.
- 빌드 산출물은 소스보다 훨씬 커질 수 있으므로, Tauri/Rust 프로젝트에서는 `src-tauri/target/`을 반드시 무시해야 한다.

## 메뉴 설명 및 앨범 장식 정리

## 앨범 전체창 복귀 및 내 앨범 관리

## 일정 매년 반복

- 일정 등록에 `매년 반복` 체크 추가. 체크하면 연월일 입력을 월·일 셀렉트로 교체.
- 연도 없는 반복 일정 저장 및 모든 연도 달력 조회 적용. 디데이는 조회 연도의 날짜 기준.
- 반복 일정의 디데이 토글·삭제는 ID로 원본을 찾아 반영. 기존 일회성 일정 호환 유지.
- 빌드 통과. 기존 테스트 14개 통과 및 추가 반복 일정 테스트 PC/모바일 2개 통과(셀렉터 보정 후 재실행).
- 추가 검증: 월별 말일 보정, 새로고침 보존, 다른 연도 조회·디데이 변경·삭제.

### 앨범 관리 구현

- 전체창 닫기와 Escape에서 모아보기 복귀. 빈 앨범도 닫을 수 있는 빈 상태 제공.
- 내 앨범 선택 모드: 한 개 선택 시 수정, 여러 개 선택 시 삭제.
- 수정 모달: 제목, 표지 색상, 사진 다중 선택 및 앨범에서 제거, 24장 페이지 단위 표시, 저장/취소.
- Tauri `update_album`, `delete_albums` 연결. 트랜잭션으로 저장 및 삭제하며 원본과 미디어 등록 목록 유지.
- 검증: `npm run build`, `cargo +stable-x86_64-pc-windows-msvc check --manifest-path src-tauri/Cargo.toml`, Playwright 14개 통과.
- Rust 명령이 추가되었으므로 실행 중인 Tauri 앱은 재시작해야 함.

### 이전 메뉴 문구 정리

- 앨범 표지 상단 줄 요소와 관련 CSS 제거.
- 모아보기, 달력보기, 앨범보기, 내 앨범, 지난 추억의 설명 문구 제거.
- 사이드바 설명 및 상단 보조 문구 제거, 파일 등록 영역 제목을 `사진 등록`으로 축약.
- 빈 상태, 등록 진행 상태, 삭제 결과 안내는 유지.

## 컨셉별 앨범 내부 전면 교체

- 기존 `bookSpread/bookPage/leftPage/rightPage/albumPhoto` JSX와 CSS 삭제.
- `AlbumFullscreenReader`를 `scrapbookStage/scrapbookSheet/scrapbookPrint` 구조로 재작성.
- 디자인1=두 번째 참고 이미지, 디자인2=첫 번째, 디자인3=세 번째, 디자인4=네 번째로 연결.
- 디자인별 내부 배경 에셋 4개를 Imagegen으로 생성해 `public/textures/`에 추가.
- 디자인별 한 책장 용량을 각각 4장, 6장, 6장, 7장으로 분리.
- 상세 앨범 사진은 eager loading, 갤러리 썸네일은 기존 lazy loading 유지.
- 검증: `npm run build`, Playwright 핵심 22개 테스트 통과. 네 컨셉을 PC/모바일에서 각각 캡처해 레이아웃 확인.

### 양면 4:3 펼침 통일

- 디자인1~4 내부 배경을 모두 중앙 책등과 페이지 두께가 있는 1448x1086 `4:3` 에셋으로 교체.
- 모든 컨셉의 사진을 중앙선을 넘지 않도록 좌우 페이지에 분리 배치.
- 화면 높이에 따라 앨범 너비를 제한해 상단 메뉴와 하단 페이징에 가리지 않도록 조정.
- 좌우 페이지 번호를 각각 표시하고, 페이지 전환 레이어를 진행 방향의 한쪽 페이지로 제한.

### 페이지 넘김 단계 분리

- 초기에 사용했던 `scrapbookTurnGap`은 전체 페이지가 새로고침되는 인상을 줘 삭제.
- 회전 종이의 뒷면은 `backface-visibility: hidden`으로 숨김.
- 별도 전환 가림막을 삭제하고 현재 컨셉 종이 배경을 전환 내내 고정.
- 다음 장은 오른쪽 사진 즉시 제거, 왼쪽 사진 100ms 지연 제거. 이전 장은 좌우 반대.
- 300ms 시점에 다음 사진 데이터로 교체.
- 새 사진은 넘기는 쪽 40ms, 넘어간 쪽 150ms 지연 후 순차 표시. 페이드는 140ms로 단축.
- 전체 전환 잠금은 820ms 뒤 해제.

## 인물 대표 썸네일 변경

- `person.cover_face_id` 열과 기존 DB 자동 마이그레이션 추가.
- `set_person_cover_face` Tauri 명령 추가. 선택 얼굴이 해당 인물 소속인지 검증 후 저장.
- 인물 상세에 `대표 사진 변경` 모드와 현재 대표 배지 추가.
- 인물 목록은 저장된 대표 얼굴을 우선 표시하고 없으면 첫 얼굴로 대체.
- 대표 얼굴을 다른 인물로 이동하면 이전 대표 설정을 자동 해제.
- 검증: 프론트엔드 빌드, Rust 단위 테스트 10개, PC·모바일 대표 사진 선택·재실행 테스트 통과.

## 앨범·시간순 hover와 인물 사진 앨범 생성

- 내 앨범 표지에 좌우 흔들림, 상승, 밝기·채도 강조 hover/focus 효과 추가.
- 시간순 사진에 짧은 상승, 테두리, 그림자 hover/focus 효과 추가.
- 인물 상세 사진을 모아보기와 같은 `210px` 최소 카드 및 동일 모바일 2열 규격으로 변경.
- 인물 상세 다중 선택 도구에 `앨범 만들기` 추가.
- 선택 얼굴 ID를 중복 없는 원본 미디어 ID로 변환한 뒤 기존 앨범 생성 모달과 Tauri 저장 명령 재사용.
- 검증: `npm run build`, Playwright 관련 데스크톱 회귀 7개, 인물 앨범 생성 PC·모바일 2개 통과.

## 모아보기 정렬·필터

- 정렬: 날짜 최신순, 날짜 오래된순, 댓글 많은순, 별점 높은순, 조회수 많은순, 이름순.
- 필터: 사진/영상/음원, 최소 별점, 즐겨찾기만, 댓글 있는 사진만.
- 적용 필터 개수와 결과 장수 표시, 전체 초기화, 결과 없음 상태 추가.
- 정렬·필터 변경 시 페이지네이션을 1페이지로 초기화.
- 상세보기 조회수를 `media.view_count`에 저장하는 Tauri 명령과 기존 DB 자동 마이그레이션 추가.
- 상세 메타데이터에 조회수 표시. 목록은 기존대로 이미지와 날짜만 유지.
- PC·모바일 정렬 순서, 복합 필터, 초기화, 조회수 저장 테스트 추가.

## 패브릭 색상 앨범 및 링 바인더 내부 개편

- `AlbumConcept`, `coverConcept`, 디자인1~4 선택 UI 및 컨셉별 페이지 분기를 제거.
- 앨범 생성·수정에서 6개 기본 색상과 직접 색상 선택을 제공하고 `album.cover_color`에 저장.
- 앨범 목록 표지를 CSS 패브릭 커버, 책등, 종이 두께, 중앙 사진창 구조로 전면 교체.
- 앨범 내부를 고정 `4:3` 양면 링 바인더와 페이지당 사진 4장 구조로 전면 교체.
- 조회수 변경 등 동일 사진의 메타데이터 갱신 시 앨범 쪽수가 초기화되지 않도록 ID 순서 기준으로 동기화.
- 화면 높이 제한을 조정해 앨범과 하단 페이지 조작부가 겹치지 않도록 수정.
- 사용하지 않는 이전 앨범 표지·내부 PNG 9개 삭제.
- DB `cover_concept` 열은 기존 설치본 호환을 위해 스키마에만 유지하고 앱 모델과 UI에서는 사용하지 않음.
- 검증: `npm run build`, Rust 단위 테스트 11개, 앨범 관련 Playwright PC·모바일 10개 통과.

## 확대 보기·지난 추억·달력 일정 개선

- 상세 화면의 단순 확대 토글을 전용 전체 화면 `PhotoZoomViewer`로 교체.
- 축소·확대 버튼, 100~400% 슬라이더, 현재 배율, 100% 복원, 닫기 기능 추가.
- 확대 화면에서는 사진과 배율 조작부만 표시하고 상세 메타데이터와 댓글은 제외.
- 지난 추억을 현재 월·일과 같은 과거 연도 사진으로 계산하고 메뉴에 개수 배지 표시.
- 달력 일정 포스트잇을 셀 하단에서 날짜 아래 상단으로 이동하고 사진 영역과 분리.
- 모바일 달력은 일정 텍스트 대신 분류 아이콘을 표시하고 접근성 이름으로 제목 유지.
- 검증: `npm run build`, 관련 Playwright PC·모바일 6개 통과.

## 앨범 강조와 내부 스프링 제거

- 앨범 표지 hover의 `albumCoverRock` 반복 애니메이션과 키프레임 삭제.
- hover/focus는 위치 변화 없이 밝기·채도·그림자만 강조하도록 변경.
- `binderRings` DOM과 데스크톱·모바일 관련 CSS 전부 삭제.
- 중앙에는 얇은 접힘선과 좌우 페이지 안쪽 그림자만 유지.
- 내 앨범과 사진보기 앨범이 동일한 `AlbumFullscreenReader` 무스프링 구조를 사용하도록 회귀 테스트 추가.
- 검증: `npm run build`, 앨범 관련 Playwright PC·모바일 6개 통과.
