# 기술 용어 정리

`오래담은` 프로젝트에서 나온 용어를 공부용으로 정리한 문서입니다.

## Tauri

웹 화면을 데스크톱 앱으로 감싸고, 운영체제 기능을 Rust로 연결하는 프레임워크입니다. 이 프로젝트에서는 파일 선택 창, 폴더 탐색, SQLite 저장을 위해 사용합니다.

## React

화면을 컴포넌트 단위로 만드는 JavaScript 라이브러리입니다. 탭, 달력, 앨범 페이지처럼 상태가 바뀌는 화면을 구성하는 데 씁니다.

## TypeScript

JavaScript에 타입을 더한 언어입니다. 사진 데이터의 구조를 미리 정해 실수를 줄이는 데 도움이 됩니다.

## Rust

성능과 안전성을 중시하는 시스템 프로그래밍 언어입니다. Tauri 앱의 백엔드에서 파일 시스템과 데이터베이스 작업을 맡습니다.

## Cargo

Rust의 빌드 도구이자 패키지 관리자입니다. Rust 코드를 검사하고 빌드하며, 필요한 Rust 라이브러리를 관리합니다.

## SQLite

서버 없이 파일 하나로 동작하는 데이터베이스입니다. 로컬 앱에서 등록 목록, 별점, 코멘트 같은 정보를 저장하기에 적합합니다.

## Vite

프론트엔드 개발 서버와 빌드 도구입니다. React 화면을 빠르게 실행하고, 배포용 파일로 묶는 역할을 합니다.

## Playwright

브라우저를 자동으로 조작해 사용자 흐름을 테스트하는 도구입니다. 버튼 클릭, 탭 전환, 화면 표시 여부를 검사할 수 있습니다.

## invoke

React 쪽에서 Rust 쪽 Tauri 명령을 호출할 때 쓰는 방식입니다. 예를 들어 화면에서 `register_paths`를 호출하면 Rust가 실제 파일 등록을 처리합니다.

## command

Tauri에서 React가 호출할 수 있도록 공개한 Rust 함수입니다. 이 프로젝트의 예시는 `list_media`, `register_paths`, `update_media_details`입니다.

## schema

데이터베이스 테이블 구조를 정의한 설계도입니다. 어떤 컬럼을 저장할지, 기본값은 무엇인지 정합니다.

## migration

앱이 업데이트되면서 데이터베이스 구조를 바꿔야 할 때 적용하는 변경 절차입니다. 지금은 기본 schema 중심이고, 기능이 늘어나면 migration 관리가 필요합니다.

## EXIF

사진 파일 안에 들어있는 촬영 날짜, 카메라 정보, 위치 같은 메타데이터입니다. 현재 앱은 아직 EXIF를 읽지 않고 파일 수정일을 기준으로 날짜를 저장합니다.

## thumbnail

원본 사진이나 영상을 작게 미리 보여주는 이미지입니다. 실제 원본을 매번 크게 읽으면 느릴 수 있으므로, 보통 별도 캐시로 만들어 둡니다.

## cache

자주 쓰는 결과를 다시 계산하지 않도록 저장해두는 공간입니다. 썸네일 캐시는 사진 목록을 빠르게 보여주기 위해 필요합니다.

## hash

파일 내용을 짧은 식별값으로 계산한 값입니다. 이 프로젝트에서는 같은 사진이나 영상이 다른 폴더에 있어도 중복 등록되지 않게 확인하는 데 사용합니다.

## unique index

데이터베이스에서 특정 값이 중복 저장되지 않게 막는 장치입니다. `content_hash`에 unique index를 걸면 같은 해시를 가진 파일은 한 번만 등록됩니다.

## mock data

실제 데이터 연결 전 화면을 만들기 위해 쓰는 임시 데이터입니다. 브라우저에서 UI만 확인할 때 사용합니다.

## MVP

Minimum Viable Product의 줄임말입니다. 완성품이 아니라 핵심 흐름이 실제로 동작하는 최소 기능 버전을 뜻합니다.

## responsive layout

화면 크기에 따라 레이아웃이 자연스럽게 바뀌는 설계입니다. 데스크톱과 모바일에서 모두 사용할 수 있게 만드는 것이 목표입니다.

## hover state

마우스를 버튼이나 카드 위에 올렸을 때 나타나는 시각적 반응입니다. 사용자가 클릭 가능한 요소를 알아차리게 도와줍니다.

## pointer event

마우스, 터치, 펜 입력을 하나의 방식으로 다루는 브라우저 이벤트입니다. 이 프로젝트에서는 드래그로 여러 사진을 선택할 때 사용합니다.

## modal

현재 화면 위에 잠시 떠서 특정 작업에 집중하게 만드는 UI입니다. 사진 상세보기처럼 선택한 항목의 정보를 크게 보고 편집할 때 적합합니다.

## design token

색상, 간격, 둥근 정도, 그림자처럼 반복해서 쓰는 디자인 값을 이름 붙여 관리하는 방식입니다. CSS에서는 `--primary`, `--radius-card` 같은 변수로 만들 수 있습니다.

## web font

웹 화면에서 외부 폰트 파일을 불러와 사용하는 방식입니다. 이 프로젝트에서는 제목용 `ChosunMyeongjo`와 본문용 `SUIT`를 CSS `@import`로 불러옵니다.
