# Warm Journal v2 구현 검토

final result: blocked

## 기준

- 기준 브랜치: `feature/warm-journal-v2`, `d6834b2`.
- 사진 기록: 대화에서 승인한 Warm Journal 시안. 왼쪽 월 목록, 가운데 날짜별 사진, 오른쪽 내 앨범을 유지하고 그리드·달력·전체 앨범 세 탭을 사용한다.
- 앨범: 승인한 책등·종이 단면이 있는 아이보리 앨범 표지와 양면으로 펼친 앨범 시안.
- 참고 이미지: `exec-02205604-d335-42a2-9e63-d83e2655051e.png`, `exec-6c275f34-dd92-4b31-8b99-7ae3fa3ae2d6.png`, `exec-c1928b4e-68b1-4524-a73a-64916b61de50.png`.
- 원본 위치: `/workspace/scratch/f372df73a598/generated_images/`. 세 이미지의 실제 픽셀 크기는 모두 1487×1058이다.

## 이어서 수정한 항목 (2026-09-20)

- 앨범은 저장된 사진 순서를 유지하면서 최신 미디어 정보를 참조한다. 댓글·별점·즐겨찾기·조회수가 앨범 사본의 이전 값으로 돌아가는 경로를 제거했다.
- 내 앨범과 사진 기록의 바로가기는 열린 앨범의 ID를 보관한다. 사진 상세를 닫으면 같은 펼침에 수정된 기록이 표시되도록 연결했다.
- 등록 해제·전체 비우기 후 앨범의 삭제된 사진 참조도 정리한다. 미디어 목록을 아직 불러오지 못한 상태를 빈 목록과 구분해 초기 로딩 중 사진을 제거하지 않는다.
- SQLite에서 표지 사진을 등록 해제하면 `FOREIGN KEY constraint failed`가 발생하는 것을 재현했다. 삭제 전에 각 앨범의 다음 사진을 저장 순서대로 표지에 연결하는 트리거로 수정했다. 남은 사진이 없으면 표지를 비우고 앨범 제목·색상은 유지한다. 기존 DB에도 스키마를 다시 적용할 때 설치된다.
- 추억 검색 결과에서 사진 상세를 열면 이전·다음도 해당 검색 결과 안에서 이동하도록 범위를 맞췄다.
- 상세 사진 이동 시 ID별 조회를 사용해 전체 사진 목록을 반복 검색하는 비용을 줄였다.

이번 수정은 상태·데이터 연결 보완이며, 새 화면 캡처를 통한 디자인 검증 결과는 아니다.

## 영상·음원 상세 재생 보완 (2026-09-20)

- 영상·음원 상세에 로컬 소스를 사용하는 native video/audio 컨트롤을 연결했다. 재생 실패 안내·재시도, 창 닫기·기록 이동 시 일시정지, 플레이어 방향키와 사진 이동의 충돌 방지를 구현했다.
- 브라우저 파일 선택 시 MIME 정보가 없어도 확장자로 종류를 판별하고 같은 파일을 다시 선택할 수 있도록 입력값을 초기화한다.
- 사진 이동 시 댓글 초안·편집 상태를 초기화한다.
- 재생기 내부는 원본 시안에 없는 기능 상태다. 기본 사진 레이아웃은 유지하며, 재생기와 오류 안내의 실제 크기·배치는 화면 검증이 남아 있다.
- 브라우저 연결과 빈 탭을 확인했다. 이전 로컬 접근 거절의 해제가 확인되지 않아 해당 페이지에 다시 접근하거나 다른 포트·브라우저로 우회하지 않았다. 새 화면 비교 근거는 없다.

## 구현 내용

- 70px 공통 메뉴, 작은 글자, 따뜻한 흰색과 올리브 색상, 사진이 중심인 날짜별 배치.
- 월 제목과 표시 기록을 일치시키고 모든 기록·날짜 없음으로 돌아가는 경로 추가. 검색 시 전체 기록으로 범위 전환.
- 생성한 린넨 책 표지 이미지 위에 실제 앨범 사진과 제목 표시. 기존 표지 색상 선택 유지.
- 책 내부의 날짜·기록, 부분적으로 채워진 마지막 장, 사진 목록 전환과 페이지 슬라이더 구현.
- 저장 순서 유지. 섞기와 원래 순서는 별도 메뉴에서 명시적으로 선택.
- 사진 상세의 이전·다음이 현재 앨범 또는 검색 결과 안에서 이동.
- 사람·반려동물의 이름 검색 연결. 기존 분류·앨범 편집·달력 기능 유지.
- 브라우저에서 가져온 사진은 세션 중 실제 파일 미리보기 사용.

## 확인한 항목

- `npm run test:unit`: 18개 통과. 앨범 로직 12개에 파일 형식 판별, 파일 소스·Windows 경로, 재생 태그·조작부·복구 안내에 대한 6개 검사를 추가했다. 재생기 검사는 서버 렌더링한 HTML 구조에 대한 것으로 실제 브라우저 디코딩 검증은 아니다.
- `python3 tests/test_album_storage.py`: 실제 `schema.sql`을 사용하는 SQLite 검사 5개 통과. 여러 앨범의 표지 교체, 일반 사진 등록 해제, 전체 비우기, 기존 DB에 반복 적용, 롤백을 검사했다. 외래 키 검사도 통과했다.
- `npm run build`: TypeScript 검사와 Vite 프로덕션 번들 통과. 기존 인식 라이브러리의 큰 청크 경고는 남아 있다.
- `git diff --check`: 공백 오류 검사.
- 표지 이미지 자체의 질감·구도·투명 배경을 확인했다. 앱에 렌더링한 화면의 검증은 아니다.

## 아직 확인하지 못한 항목

로컬 미리보기의 브라우저 접근이 이전 자동 승인 검토에서 거절되어 새 화면 캡처를 확보하지 못했다. 다른 포트나 별도 브라우저로 우회하지 않았다. Product Design의 시각 검증 조건에 따라 결과를 `blocked`로 둔다.

- 승인 시안과 같은 1487×1058 화면에서 실제 사진 비율, 표지 오버레이, 책 중앙 접힘과 여백 비교.
- 1440×960, 1080×720 데스크톱 창에서 메뉴·도구·책장 넘김 확인.
- 수정·추가한 Playwright 시나리오의 실제 실행과 콘솔 오류 확인. 테스트 검색만으로 실행 통과를 주장하지 않는다.
- 추가한 앨범 수정 반영·펼침 유지·삭제 후 빈 앨범 시나리오는 TypeScript 검사만 통과했으며, 브라우저에서 실행하지 않았다.
- 무음 WAV 재생, 플레이어 방향키, 닫기·이동 후 정지, 잘못된 영상의 재시도, 댓글 초안 초기화 시나리오 역시 TypeScript 검사만 통과했다.
- Tauri 데스크톱에서 파일 대화상자, 원본 파일, 전체화면 및 기존 저장·인식 기능 확인.
- 이 환경에는 Rust/Cargo가 없어 Tauri 빌드와 실제 Windows 앱 검증은 수행하지 못했다. 변경된 SQL은 Rust의 `include_str!`로 포함되므로 데스크톱 앱을 다시 빌드해야 반영된다.

## 시각 검증 상태

- 원본: 위 1487×1058 PNG들. 이번 작업에서 사진 기록과 앨범 내부 원본을 열어 확인했다.
- 구현 스크린샷: 확보하지 못함. 실제 구현의 픽셀 크기·CSS 뷰포트·deviceScaleFactor는 미확인이다.
- 비교할 상태: 사진이 있는 그리드, 앨범 목록, 양면 앨범, 상세 편집 후 복귀, 마지막 펼침, 빈 앨범.
- 전체 화면 비교 및 부분 비교: 구현 캡처가 없어 수행하지 못함. 밀도 정규화도 미수행이다.
- 글꼴·타이포그래피, 간격·레이아웃, 색상·토큰, 이미지 품질·크롭, 문구: 모두 실제 렌더링 비교가 남아 있다.
- 시각 비교 이력: 이전 작업과 이번 작업 모두 구현 캡처 단계에서 차단되어 새 비교 회차나 시각 수정의 통과 근거는 없다.
- 상호작용·콘솔: 브라우저 검증 미실행. 위 단위 검사와 SQLite 검사는 화면 검증을 대체하지 않는다.

코드와 정적 검증 결과를 검토할 수 있는 상태이며, 시안과의 시각적 일치가 검증된 완성본은 아니다.
