# 프로젝트 학습 로그

## 앨범별 컨셉 저장과 선택지 정리 (2026-09-13)

앨범 생성의 `window.prompt`를 제목과 표지 미리보기가 있는 모달로 교체했습니다. 생성/수정/목록 모두 `AlbumCover`를 재사용하므로 미리보기와 실제 표지의 배치가 일치합니다. `albumConcepts.ts`에서 에셋과 사진 수를 정의합니다. 최종 선택지는 디자인1(mint), 디자인2(ivory), 디자인3(leather), 디자인4(linen)이며 초록 여행은 제거했습니다. 색상 선택 UI와 전용 CSS도 제거했습니다.

표시 이름 대신 안정적인 식별자를 `album.cover_concept`에 저장합니다. SQLite 마이그레이션은 기존 행을 보존하며 기본값 mint를 추가합니다. 예전 sage 또는 알 수 없는 값은 화면에서 디자인1로 표시합니다. 이전 색상 DB 필드는 호환성을 위해 남기되 UI에서 변경하거나 표지에 적용하지 않습니다.

생성은 표지와 사진 참조를 하나의 트랜잭션으로 저장합니다. 중간 실패 시 전부 되돌려 부분 저장을 방지합니다. 수정도 같은 원칙이며 원본 사진은 이동하거나 삭제하지 않습니다. Rust 테스트는 기존 DB의 반복 마이그레이션, 컨셉 읽기/쓰기, 잘못된 사진 ID의 롤백을 검증합니다. 브라우저 테스트는 Tauri 명령을 모의하여 선택값 전달과 새로고침 후 복원을 검증하며, 실제 DB 검증과 구분합니다.

새 표지 소재는 내장 Imagegen 신규 생성 모드로 만들었습니다. `public/textures/album-ivory-cover.png`, `album-leather-cover.png`, `album-linen-cover.png`에 포함했습니다. 공통 생성 조건은 '정면 4:5 전체 소재, 사진/프레임/문자/주변 배경 없음'입니다. 각각 '아이보리 수제 종이, 양쪽 작은 갈색 압화와 오른쪽 아래 빈 라벨', '체스트넛 가죽, 얇은 음각 테두리와 하단 작은 카메라', '밝은 오트밀 린넨, 오른쪽 아래 작은 식물 드로잉'으로 요청했습니다. 사진은 생성하지 않고 사용자의 실제 미디어를 최대 4/3/1/3장 표시합니다.

## 밝은 민트 표지 (2026-09-13)

천 소재만 밝은 민트색으로 편집하고 뒷표지를 `#A9CEBF`, 소재 아래 기본색을 `#D8EEE5`로 맞췄습니다. 전체 이미지에 CSS 색조 필터를 걸면 지도와 종이까지 변색되므로 내장 Imagegen 편집 모드로 천 영역만 바꿨습니다. 사진, 프레임, 배치와 기능은 그대로입니다.

저장 에셋: `public/textures/album-mint-cover.png`. 원본 소재는 `album-sage-cover.png`로 보존합니다. 편집 프롬프트:

```text
Edit only the cotton cloth background color of this scrapbook texture asset from dull gray olive sage to a noticeably lighter fresh pastel MINT GREEN, target #C9E7DC with subtle natural fiber shadows. Preserve all existing paper scraps, map, dried flowers, label, stamp and their exact shapes, locations and original cream colors. Keep dimensions/composition, straight-on flat view, texture sharpness unchanged. No new elements, no text, no overall tint on the paper or flowers. Cloth should feel airy, luminous mint, not gray, not yellow-green, not turquoise saturated.
```

## 기존 표지 스타일 폐기와 참고 색감 통일 (2026-09-13)

이전 결과는 기존 표지색 위에 장식을 합성하여 참고 이미지와 다른 인상을 만들었습니다. 이번에는 천, 지도, 종이, 압화를 하나의 소재 이미지로 새로 제작했습니다. 모든 표지에 같은 회녹색 소재를 사용하고, 인화지는 아이보리색, 글씨는 회갈색, 내지는 따뜻한 종이색으로 맞췄습니다. 사용자 사진 자체에는 색 필터를 적용하지 않습니다.

기존 `album-linen.png`, `album-scrapbook.png`와 이들을 합성하던 CSS, 표지색에 따라 글자색을 계산하던 `coverInk`는 제거했습니다. 과거 로그의 해당 에셋 경로는 변경 이력을 설명하는 기록이며 현재 사용하지 않습니다. 사진·앨범 데이터와 내부 감상 기능은 유지했습니다.

### 색상 설정의 역할 변경

참고 이미지의 표지색을 유지하기 위해 기존 색상 선택은 작은 책갈피 리본에만 적용합니다. 수정 화면 명칭도 '책갈피 색상'으로 바꿨습니다. DB의 `coverColor` 필드는 호환성을 위해 그대로 읽고 쓰므로 기존 저장값을 삭제하거나 DB 마이그레이션하지 않습니다. CSS에서는 `--album-ribbon`으로 역할을 구분합니다.

### 배치와 두께

책등, 앞표지, 내지, 뒷표지, 리본을 같은 부모 안에 배치합니다. 내지가 표지 아래로 약 21px 드러나게 하고 리본 공간을 버튼 아래 여백에 포함해 다음 행과 겹치지 않게 했습니다. 제목은 소재 이미지의 종이 라벨에 맞춰 정렬하고 두 줄까지만 표시합니다. 전체 제목은 버튼의 접근성 이름으로 유지됩니다.

### 새 소재 에셋

내장 Imagegen 신규 생성 모드. 프로젝트 저장 경로는 `public/textures/album-sage-cover.png`입니다. 생성 이미지는 장식과 표지 소재만 포함하고 실제 앨범 사진은 별도 HTML 요소로 표시합니다. 프롬프트:

```text
Generate a photorealistic flat full-bleed 4:5 portrait scrapbook COVER MATERIAL AND DECORATION asset. Precisely straight-on orthographic, no perspective, no book edges or pages, no surrounding scene. Entire background is soft pale muted gray sage green cotton bookcloth (#c1c5af), subtle fine natural fibers, NOT bright yellow-green, NOT blue, NOT orange. Tactile nostalgic handmade Korean travel memory album. Torn faded vintage map covers upper right corner 32% width 26% height. Soft torn ivory paper at left middle, with tiny dried baby's breath sprig attached on it, confined to leftmost 18%. Small blank ivory vintage luggage label with thin double line outline at left 18%-45%, vertical 68%-78%. Small faded botanical postage stamp near bottom left 33%, vertical 83%. Cream graph paper and a tiny pressed daisy on lower right edge, slender dried stems along rightmost 12%. Broad quiet sage green empty areas must remain at top left, across middle, and lower right for real user photos to be overlaid by code. Bottom left title area empty at x10%-42% y80%-92%. NO photos, NO polaroid frames, NO words or letters anywhere, NO shadows of outside objects, NO artificial grid on cloth. Soft even daylight and believable subtle contact shadows of paper scraps, very refined reference-quality handmade physical material, not vector illustration.
```

## 스크랩북 표지와 실제 사진 분리 (2026-09-13)

새 참고 이미지에 맞춰 전면 인쇄형 포토북을 천 표지 위에 인화사진을 붙인 스크랩북으로 변경했습니다. 책은 정면을 유지하고 사진만 소폭 회전시킵니다. 목록 표지에만 적용하며 내부 감상 화면의 두 장 배치와 원본 사진은 변경하지 않습니다.

### 데이터와 장식을 분리하는 이유

생성 이미지에 사진까지 넣으면 사용자 사진으로 교체할 수 없습니다. 따라서 Imagegen은 지도 조각, 종이, 압화만 투명 PNG로 만들고 실제 사진은 기존 `MediaVisual`로 표시합니다. 표지에는 이미지 미디어 중 앞의 최대 4장만 사용합니다. 매 렌더마다 무작위로 고르지 않으므로 별점이나 선택 상태가 변해도 표지 사진이 바뀌지 않습니다. 사진이 적으면 중복으로 채우지 않고 수량별 배치를 사용합니다.

### CSS 구성

표지의 `aspect-ratio: 4 / 5`를 기준으로 사진 위치를 백분율로 지정합니다. `data-slot`은 사진 배치 위치를 명시하므로 장식 요소가 추가되어도 `nth-child` 순서에 영향을 받지 않습니다. `data-photo-count`는 0~4장의 배치 차이를 표현합니다. 제목 공간과 사진 영역은 따로 확보하고 긴 제목은 두 줄까지만 표시합니다. 버튼 접근성 이름에는 전체 제목이 유지됩니다.

천 질감, 뒷표지, 내지, 책등은 기존 레이어를 재사용합니다. 지도와 압화는 `pointer-events: none` 및 빈 대체 텍스트로 클릭과 스크린 리더 탐색을 방해하지 않습니다. 새 UI 라이브러리는 추가하지 않았습니다.

### 한글 손글씨와 오프라인 지원

표지 제목에만 [Google Fonts의 Nanum Pen Script](https://github.com/google/fonts/tree/main/ofl/nanumpenscript)를 사용합니다. 파일은 `public/fonts/NanumPenScript-Regular.ttf`, 라이선스는 `public/fonts/NanumPenScript-OFL.txt`에 포함했습니다. 메뉴는 기존 SUIT를 유지합니다. 표지 폰트와 장식은 앱에 포함되므로 런타임 CDN 연결이 필요하지 않습니다.

### 생성 에셋 기록

내장 Imagegen 신규 생성 모드 사용. 저장 경로: `public/textures/album-scrapbook.png`. 기존 `album-linen.png`는 천 질감으로 재사용합니다. 새 에셋 프롬프트:

```text
Generate a UI texture asset, not a book mockup: flat orthographic full-bleed portrait 4:5 scrapbook decorating layer, with genuinely transparent background. Delicate photorealistic pressed white wildflowers only along lower right edge, a small torn vintage map fragment attached at extreme upper right corner, small torn ivory handmade paper behind middle left region, small pale graph paper fragment lower right. Sparse delicate Korean analog memory scrapbook aesthetic, natural paper fibers and restrained soft contact shadows. Keep most of canvas transparent: center completely empty for real user photos, bottom left empty for a title, top left empty. No photos, no photo frames, no text, no letters, no book, no table, no overall background or fabric. All fragments seen directly from the front, not perspective. Map confined to top right 25% of canvas, flowers confined to rightmost 16% below middle. These are quiet supporting decorations, no large objects.
```

## 사진 중심 포토북 표지 (2026-09-13)

천 표지 안의 작은 사진 창을 없애고, 책등 12px를 제외한 앞표지 전체를 사진으로 채웠습니다. 색상만 바꾸는 대신 사진과 여백의 비중을 바꾼 구성 변경입니다. 기존 표지색은 책등과 뒷표지에 사용하므로 색상 설정도 유지됩니다.

`position: absolute`와 `inset`으로 사진을 표지에 맞추고 제목을 왼쪽 아래에 겹쳐 배치합니다. 사진을 원본 비율 그대로 모두 보여주는 방식이 아니라 표지에 맞춰 일부 잘라 표시하며, 원본 파일은 변경하지 않습니다. 흰색 제목이 밝은 사진에서도 읽히도록 하단에만 반투명 음영을 두었습니다. 사진 전체에 어두운 필터를 적용하지는 않습니다.

새 라이브러리나 생성 이미지는 필요하지 않습니다. 기존 사진 썸네일과 CSS를 재사용하고 천 질감 이미지 로딩 규칙은 제거했습니다. 정면 구도, 내지/뒷표지 레이어, 선택/수정/삭제 및 내부 책장 넘김은 유지합니다.

검증은 모양에 종속된 과거의 3:4 사진 창 검사 대신, 사진 높이가 표지와 같고 폭은 90% 이상이며 제목이 사진 범위 안에 있는지 검사하도록 바꿨습니다. 이렇게 테스트도 변경된 사용자 경험을 검증해야 합니다.

## 참고 사진 기반 패브릭 표지 재구성 (2026-09-09)

두 번째 참고 사진의 정면 패브릭 표지와 세로 사진 창을 기준으로 구성했습니다. 기존 CSS 격자 질감 대신 Imagegen으로 만든 중성 회색 천 이미지를 `public/textures/album-linen.png`에 포함했습니다. `mix-blend-mode: soft-light`는 천의 밝고 어두운 부분을 사용자 지정 표지색과 섞으므로 색상별 이미지를 따로 만들 필요가 없습니다. 런타임 외부 이미지 요청도 없습니다.

앨범 외형은 HTML/CSS이며 새 라이브러리를 추가하지 않았습니다. 뒷표지, 내지, 앞표지, 좁은 책등을 같은 부모 기준으로 배치해 두께 표현이 따로 움직이지 않게 했습니다. 사진 창은 `aspect-ratio: 3 / 4`, 제목은 아래쪽 16px로 고정해 사진을 먼저 보도록 했습니다. 책장 넘김과 저장된 앨범 데이터는 변경하지 않았습니다.

에셋 생성 모드: 내장 Imagegen, 신규 이미지 생성. 프롬프트: `Create a seamless tileable photorealistic bookbinding linen cloth material texture, square flat orthographic scan, neutral medium gray monochrome, fine natural irregular woven flax fibers, subtle low contrast thread variation, very small weave, no checkerboard grid, no folds, no shading gradient, no objects, no text, no border. This will be a subtle multiply overlay on user-colorable fabric photo album covers in a web app. Only the flat fabric swatch, full bleed edge to edge.`

## 이전 앨범 스타일 완전 분리

라이브러리를 제거해도 공통 CSS에 이전 카드 높이와 배치 규칙이 남으면 새 스타일이 계속 이를 덮어써야 합니다. `styles.css`, `design-system.css`의 앨범 목록 전용 규칙을 삭제하고 `features/albums/albums.css`에서 레이아웃과 표지 스타일을 모두 정의하도록 정리했습니다. 선택·편집·삭제와 책장 넘김에 쓰는 스타일은 기능에 필요하므로 유지했습니다. 현재 정면 표지는 외부 표지 라이브러리 없이 HTML/CSS로 표현합니다.

## 정면 앨범 표지 디자인 (2026-09-09)

### 참고한 실물 제품

[Rosemood Debossed Fabric Photo Books](https://www.rosemood.co.uk/p/fabric-photo-books-debossed-photo-cover/)의 표지 안쪽으로 눌러 넣은 사진 창과 패브릭 소재, [Artifact Uprising Signature Layflat](https://www.artifactuprising.com/stories/signature-layflat)의 사진 창과 두꺼운 내지 구성을 참고했습니다. 판매량 순위를 검증한 것은 아니며, 공개된 실물 제품의 제본·표지 표현을 디자인 참고로 사용했습니다. 해당 업체의 사진이나 로고를 앱에 복제하지 않았습니다.

### 정면인데도 두께가 보이는 이유

사선 원근은 제거하고 뒤표지 → 내지 단면 → 앞표지 순으로 2D 레이어를 겹쳤습니다. 앞표지보다 아래로 6px 내려오는 내지와 10px 내려오는 뒤표지가 깊이 단서가 됩니다. 왼쪽은 넓은 원통형 책등 대신 14px 제본 홈으로 연결하고, 사진 창은 안쪽 그림자로 눌린 느낌을 표현합니다. 외곽 UI 카드를 덧붙이지 않습니다.

표지는 4:5 비율, PC 최대 너비 276px로 고정합니다. 사진 창과 제목은 같은 표지 안에서 배치하며 제목은 아래쪽에 한 번만 표시합니다. 긴 제목은 최대 두 줄로 제한하고 전체 제목은 앨범 버튼의 접근성 이름에 남습니다. 표지 색의 상대 밝기에 따라 제목 잉크색을 밝게/어둡게 바꿉니다. 마우스를 올려도 회전하지 않고 그림자만 변화합니다.

원근 회전용 `book-cover-3d`와 사용하지 않는 표지 CSS를 제거했습니다. 이번 표현은 3D 장면이 아니라 CSS 레이어 기반이며, 앨범 내부 책장 넘김에는 변경이 없습니다. 기존 데이터와 표지 색상 변경·제목 수정·선택 삭제 기능도 유지합니다.

## 메뉴 가독성과 반려동물 후보 검색 (2026-09-08)

### 메뉴와 로딩 배경

100px 메뉴바에 맞춰 메뉴 글씨를 16px, 아이콘을 20px, 버튼 높이를 48px로 조정했습니다. 이미지 로딩 전 배경은 사진 `#ECEEF1`, 영상 `#E5E9ED`, 음원 `#E9EDEB` 단색으로 바꿨습니다. 대체 사진을 넣으면 실제 등록 사진으로 오해할 수 있어 단색을 선택했습니다.

### 탐지와 개체 식별은 다르다

COCO-SSD는 사진에서 개/고양이 영역을 찾아내는 객체 탐지 모델입니다. 이 결과만으로 '우리 반려동물'인지는 알 수 없습니다. 검출 영역을 잘라 MobileNet V1의 분류 직전 특징값 1024개를 추출하고, 등록 사진의 특징과 코사인 유사도를 비교하는 후보 검색을 추가했습니다. 이는 모델 재학습이나 검증된 개체 식별 기술이 아닙니다. 같은 품종의 다른 동물, 조명·자세·배경에 따라 잘못된 후보 또는 누락이 생길 수 있습니다.

탐지는 0.55 이상, 외형 유사도는 0.7 이상을 초기 후보 기준으로 사용합니다. 이 값은 개체 식별 정확도를 검증한 임계값이 아니며, 유사도를 확률처럼 화면에 표시하지 않습니다. 사진에 작게 나온 동물이나 일부만 나온 동물은 놓칠 수 있습니다. 현재 지원 대상은 개와 고양이입니다.

### 안전한 확인 흐름

`인물 → 반려동물 → 등록한 대상 → 후보 찾기`에서 명시적으로 시작합니다. 연결된 사진은 기준, 아직 연결되지 않은 사진은 검색 대상으로 사용합니다. 기준 사진에서 0마리 또는 여러 마리가 검출되면 기준에서 제외합니다. 개와 고양이가 기준에 섞여 있으면 진행을 중단합니다. 후보 사진에 여러 마리가 있는 경우 적어도 하나가 기준과 유사하면 후보에 포함될 수 있습니다.

후보는 유사도 순으로 표시하지만 선택은 모두 해제된 상태로 시작합니다. 사용자가 확인한 사진만 기존 `save_pet` 명령으로 연결하며 원본은 이동하지 않습니다. 잘못 연결한 사진은 사진·이름 편집에서 해제합니다. 진행 표시·중단·닫기·24장 페이지네이션을 제공합니다. 닫으면 현재 한 장의 추론을 마친 뒤 멈춥니다.

### 성능과 로컬 실행

TensorFlow.js와 COCO-SSD를 사용합니다. MobileNet 모델은 TensorFlow.js LayersModel로 불러와 `global_average_pooling2d_1`의 출력을 사용합니다. `scripts/prepare-pet-models.mjs`가 Google 저장소에서 약 35.6MB 모델을 최초 준비하고, 이후에는 로컬 파일을 재사용합니다. 새 PC에서 첫 dev/build 실행에는 모델 다운로드용 인터넷이 필요합니다. 분석 시에는 로컬 썸네일과 로컬 모델만 사용하며 사진을 외부로 보내지 않습니다.

모델과 추론 코드는 후보 찾기를 눌렀을 때 지연 로딩합니다. 640px 썸네일을 사용하고 한 번에 한 사진씩 분석합니다. 앱 실행 중 최대 1024개 사진의 특징을 재사용하며, 앱을 재시작하면 특징은 다시 분석합니다. 저장되는 것은 확정한 사진 연결뿐입니다. 대량 사진의 최초 검색은 시간이 필요하며 실제 사용자 사진에서 속도·개체 정확도는 아직 측정하지 않았습니다.

공식 자료: [COCO-SSD](https://github.com/tensorflow/tfjs-models/tree/master/coco-ssd), [MobileNet](https://github.com/tensorflow/tfjs-models/tree/master/mobilenet). 모델 파일은 Git에서 제외하고 재현용 다운로드 스크립트를 남겼습니다.

## 썸네일 로딩 대기 줄이기 (2026-09-08)

첫 구현은 `open_database`를 사진마다 호출해 스키마 검사·마이그레이션과 전체 경로 정리를 반복했습니다. 썸네일 명령은 등록된 사진을 읽기만 하므로 읽기 전용 SQLite 연결로 바꿨습니다. 초기 DB 생성은 기존 앱 초기화/목록 조회가 담당합니다.

또한 전역 잠금 안에서 캐시를 검사하면 이미 만들어진 사진도 큰 원본의 디코딩이 끝날 때까지 기다립니다. 캐시 존재 검사를 잠금 앞으로 옮겨 이 대기를 제거했습니다. 생성은 ID에 따라 2개 작업 구역으로 나누고, 같은 ID는 같은 잠금을 사용합니다. 잠금을 얻은 뒤 캐시를 다시 확인해 중복 생성도 막습니다.

PNG 저장에는 빠른 압축을 적용하고, 개발 모드에서도 image/png/zune-jpeg만 최적화하여 디코딩·축소 연산 비용을 줄입니다. 첫 재컴파일은 더 오래 걸릴 수 있습니다. 기존 캐시는 계속 재사용하며 삭제하지 않습니다.

화면에서는 동일 사진의 진행 중 요청과 응답을 30초 동안 공유합니다. 최대 512개로 제한해 메모리가 계속 늘지 않도록 했고 실패 요청은 제거하여 재시도가 가능하게 했습니다. 30초 안의 외부 원본 수정은 잠시 이전 썸네일로 보일 수 있습니다. 원본 파일 접근 자체가 느리거나 OneDrive 다운로드가 필요하면 최초 생성 지연은 남을 수 있습니다.

## 고해상도 목록과 썸네일 캐시 (2026-09-08)

작은 카드로 표시해도 원본 JPEG를 그대로 img에 넣으면 브라우저는 원본 해상도로 디코딩합니다. CSS 크기나 lazy loading만으로 이 메모리 비용을 없앨 수 없습니다. 목록에는 긴 변 640px 이하의 별도 이미지를 사용하고, 상세보기·확대·앨범 감상에는 원본을 사용하도록 경로를 분리했습니다.

React의 IntersectionObserver는 화면에서 200px 이내로 접근한 이미지에만 `media_thumbnail` 명령을 보냅니다. Rust는 DB에 등록된 ID로 원본 경로를 조회하며 별도 작업 스레드에서 생성합니다. Mutex로 동시에 한 파일만 디코딩해 초기 메모리 부하를 제한합니다. 화면을 떠나면 응답은 무시하지만 이미 시작한 생성 작업은 캐시를 완성합니다.

`image` 크레이트로 EXIF 회전 정보를 적용한 뒤 축소하여 PNG로 저장합니다. PNG는 투명도를 유지하며 재압축 손실이 없는 대신 JPEG보다 캐시 용량이 클 수 있습니다. API 근거: [image 공식 문서](https://docs.rs/image/latest/image/), [EXIF 방향 처리](https://docs.rs/image/latest/image/metadata/enum.Orientation.html).

앱 캐시 디렉터리의 `thumbnails-v1`에 저장하며 ID·원본 경로 해시·파일 크기·수정시각으로 캐시를 구분합니다. 임시 파일 저장이 끝난 후 이름을 변경해 완성되지 않은 캐시가 노출되지 않도록 합니다. 원본이나 DB를 수정하지 않으며, 기존 등록 사진도 다시 등록할 필요 없이 처음 조회할 때 생성됩니다. 오래된 캐시 자동 청소는 아직 구현하지 않았습니다.

지원하지 않는 이미지 형식이나 생성 오류는 원본 표시로 돌아갑니다. 이 경우에는 목록 성능 개선을 보장할 수 없습니다. 최초 생성 시간과 디스크 캐시 공간이 필요하며 실제 사용자 사진 수천 장의 스크롤 성능은 별도 측정해야 합니다. 새 Tauri 명령을 사용하려면 앱을 재시작해야 합니다.

## 미확인 그룹에서 직접 제외하기

미확인 얼굴 카드 하나는 여러 사진에서 발견한 얼굴들을 묶은 그룹입니다. 따라서 그룹을 선택하면 그 인물 ID에 연결된 얼굴 ID 전체를 조회해 기존 `set_faces_excluded` 명령으로 제외합니다. 선택한 카드 수와 실제 제외할 얼굴 수가 다를 수 있어 화면과 확인창에는 얼굴 수를 표시합니다. 처리 직전 이름이 여전히 빈 그룹인지 다시 확인하여 등록된 인물은 이 작업의 대상에서 걸러냅니다.

별도 삭제 명령이나 DB 구조를 만들지 않고 기존 제외 기능을 재사용하므로 원본 사진은 유지되고 얼굴 비교에서도 빠집니다. 마지막 작업은 제외 되돌리기로 복구할 수 있습니다. 이는 앞으로 추가될 모든 유사 얼굴을 자동 차단하는 기능이 아니라 현재 선택한 그룹의 얼굴 기록을 제외하는 기능입니다.

## 등록된 인물과 미확인 얼굴 분류

이름을 `trim()`한 결과가 있으면 등록된 인물, 비어 있으면 미확인 얼굴로 표시합니다. AI의 인식 확신도와 사용자 이름 등록 여부는 서로 다른 개념이므로 이번 화면 분류는 이름만 기준으로 삼습니다. 기존 DB나 얼굴 묶음을 변경하지 않고 조회 결과에서 두 영역을 파생합니다. 이름을 저장하면 재조회 후 등록된 인물 영역으로 옮겨집니다.

등록된 인물을 위에 배치하고 두 영역은 각각 24개씩 페이지를 나눕니다. 전체 목록을 먼저 페이지로 자르면 등록된 사람이 미확인 얼굴 뒤 페이지로 밀릴 수 있으므로 분류를 먼저 수행합니다. 인물 첫 화면의 `얼굴 선택하기`는 기존 얼굴 모아보기를 선택 모드로 열어, 같은 제외·되돌리기 기능을 재사용합니다. 제외는 원본 사진 삭제가 아닙니다.

## 반려동물 사진 모으기와 인물 탭 (2026-09-07)

### AI 학습 대신 사진 연결을 선택한 이유

이번 요구는 강아지를 자동 판별하는 것이 아니라 사용자가 사진을 모으는 것입니다. 따라서 얼굴 특징값이나 AI 모델을 추가하지 않고, 이름과 대표 사진을 등록한 뒤 선택한 사진을 연결합니다. 사람 얼굴 분석과 반려동물 사진 모음은 서로 독립적입니다. 사진을 많이 등록해도 자동 인식 모델이 학습되지는 않습니다.

### 연결 테이블과 원본 보존

SQLite의 `pet`에는 이름과 대표 사진 ID를, `pet_media`에는 반려동물 ID와 사진 ID의 쌍을 저장합니다. 이것을 연결 테이블이라고 합니다. 한 사진에 여러 반려동물이 함께 있으면 같은 사진을 각각의 모음에 연결할 수 있는 다대다 관계입니다. 사진 파일을 복사하거나 이동하지 않습니다.

`pet_media`의 복합 기본키는 같은 반려동물에 같은 사진이 중복 연결되는 것을 막습니다. 반려동물 삭제 시 연결 행만 연쇄 삭제되고 `media`와 원본 파일은 유지됩니다. 사진 자체가 삭제되면 그 사진에 대한 연결이 제거되며, 대표 사진 참조는 NULL이 됩니다.

### 트랜잭션이 필요한 이유

이름 변경과 사진 연결 교체를 하나의 트랜잭션으로 저장합니다. 도중에 존재하지 않는 사진 ID가 들어오면 전체 작업을 되돌려, 이름만 바뀌거나 연결이 일부 사라지는 상태를 방지합니다. 서버 역할을 하는 Rust에서도 이름 길이, 이미지 여부, 대표 사진이 선택 목록에 포함되는지 검사합니다. 화면 검증만 믿지 않는 이유는 잘못된 명령 호출도 데이터베이스에 도달할 수 있기 때문입니다.

### 화면 구조와 재사용

`PeopleWorkspace`는 인물 메뉴 내부의 `사람 / 반려동물` 탭을 관리합니다. 반려동물 화면은 처음 탭을 선택할 때 생성하며, 이후 탭 전환에서는 화면을 숨겨 선택 상태를 유지합니다. 사람 분석을 반려동물 자동 인식으로 연결하지 않습니다. 탭은 ARIA tablist/tabpanel 관계와 좌우 화살표, Home/End 포커스 이동을 제공합니다.

사진 선택에는 기존 `useRowSelection`, 이미지 표시는 `MediaVisual`, 모달은 `useModalBehavior`를 재사용했습니다. 사진 선택 목록은 페이지당 24장만 렌더링합니다. React 화면 → Tauri invoke → Rust 검증/트랜잭션 → SQLite 저장 → 목록 재조회 순서로 동작합니다. 새 라이브러리는 추가하지 않았습니다.

### 검증의 범위

Playwright는 모의 Tauri 응답으로 등록·수정·삭제·재조회 및 원본 목록 유지 흐름을 검사합니다. 실제 SQL 동작과 롤백은 Rust의 인메모리 SQLite 테스트로 별도 검사합니다. 이는 실제 사용자 DB와 Windows 앱에서 직접 테스트한 것과는 구분해야 합니다. 새 명령이 추가됐으므로 데스크톱 앱은 `npm run tauri:dev`로 재시작해야 합니다.

## 수동 얼굴 제외와 행 단위 드래그 선택

### 얼굴 제외는 사진 삭제와 다르다

`인물 → 얼굴 모아보기 → 얼굴 선택하기 → 얼굴 제외`에서 사용자가 직접 제외합니다. 그림 여부를 자동 판정하지 않습니다. 원본 사진은 모아보기와 앨범에 그대로 남습니다.

`excluded_face` 테이블에는 제외할 얼굴 ID만 저장합니다. 원래 얼굴 특징값·사진 연결·분석 완료 기록을 삭제하지 않으므로 같은 사진의 다른 얼굴은 유지되고, 재실행 후에도 제외가 유지됩니다. 인물 목록 조회, 미확인 얼굴 재비교, 새 사진 분석의 비교 기준에서 모두 제외 ID를 걸러야 단순히 숨겨진 얼굴이 다시 영향을 주는 문제를 막을 수 있습니다.

여러 얼굴의 제외는 트랜잭션으로 처리합니다. 하나라도 잘못된 ID면 전부 되돌립니다. 화면을 떠나기 전 마지막 제외 작업은 `제외 되돌리기`로 복구할 수 있습니다. `얼굴 분석 정보 지우기`는 제외 설정도 초기화하는 별도 전체 초기화입니다.

### 드래그가 지나간 카드만 선택했던 이유

기존 코드는 `document.elementFromPoint(x, y)`로 포인터 바로 아래 카드 하나를 찾아 토글했습니다. 따라서 첫 번째 열에서 아래로 드래그하면 같은 행의 다른 열은 선택되지 않았고, 빠르게 움직일 때 중간 카드도 빠졌습니다.

새 `useRowSelection` 훅은 현재 페이지 카드의 `getBoundingClientRect()`로 행 위치를 계산합니다. 같은 행에서는 시작 카드와 현재 카드 사이를 선택하고, 다른 행으로 이동하면 시작 행부터 현재 행까지의 모든 카드를 처리합니다. 셀 간격 위에서도 가장 가까운 카드로 범위를 판단합니다. 일반 사각형 영역 선택이 아니라 사용자가 요청한 행 범위 선택입니다.

시작 카드가 미선택이면 범위를 선택하고, 선택된 카드라면 범위를 해제합니다. `seen` 집합으로 같은 제스처에서 한 카드를 두 번 토글하지 않습니다. 포인터 캡처로 카드 밖에서도 이동을 받고, 포인터 클릭은 캡처 단계에서 막아 드래그 직후 click 이벤트가 다시 토글하는 문제를 방지합니다. 키보드 클릭은 유지합니다. 이 훅을 사진 모아보기와 얼굴 목록이 공유합니다. 드래그는 현재 페이지 안에서 적용되며 화면 가장자리 자동 스크롤은 별도 구현하지 않았습니다.


## 등록된 인물을 기준으로 미확인 얼굴 재비교

이름을 50장에 붙여도 신경망을 새로 학습하는 것은 아닙니다. 현재 방식은 얼굴마다 저장한 128개 숫자(특징 벡터)를 서로 비교합니다. 등록된 얼굴이 다양해지면 기준으로 삼을 표정·각도도 늘어납니다.

기존 분석은 당시의 전체 인물 후보 사이에서 판단하고 이미 분석한 사진을 다시 처리하지 않았습니다. 새 `find_face_matches` 명령은 현재 이름 있는 인물들의 저장된 특징값을 기준으로 이름 없는 인물의 미확인 얼굴만 다시 비교합니다. 사진 읽기나 얼굴 검출을 반복하지 않습니다.

같은 인물의 여러 얼굴과 비교한 최소 거리를 인물별 점수로 사용합니다. 거리가 0.45 미만이고 다음 후보와 차이가 0.06을 넘을 때만 제안합니다. 이는 확률이나 정확도 퍼센트가 아닙니다. 이름 있는 인물이나 직접 이동·확정한 얼굴은 재분류 대상에서 제외합니다. 같은 사진에 같은 인물이 이미 있으면 중복 제안을 막습니다.

비교 명령은 읽기 전용입니다. 사용자가 후보 체크 후 적용할 때만 기존 `move_faces` 트랜잭션으로 얼굴 연결을 변경합니다. 한 인물로 이동하는 묶음마다 트랜잭션이 적용되므로 여러 대상 중 실패가 나면 앞서 성공한 대상은 저장될 수 있습니다. 오류 시 재조회하고 다시 비교하도록 했습니다.

오분류된 얼굴이 기준 인물에 섞여 있으면 제안도 틀릴 수 있어 자동 일괄 적용하지 않습니다. 흐린 얼굴이나 기준과 거리가 큰 얼굴은 그대로 남습니다. 이름만 다른 인물의 중복 그룹은 먼저 합쳐야 후보 경쟁으로 인한 보류를 줄일 수 있습니다.


## 같은 이름의 인물 합치기

이름 변경은 표시 문자열을 수정하는 작업이고, 인물 합치기는 얼굴들의 `person_id`를 하나로 모으는 작업입니다. 이전에는 이름만 저장했으므로 같은 이름이어도 별개의 인물로 남았습니다.

이제 앞뒤 공백을 제거한 비어 있지 않은 이름으로 비교하고, 중복이면 확인 후 기존 `move_faces` 명령을 사용합니다. 해당 명령은 SQLite 트랜잭션으로 전체 얼굴 연결을 이동합니다. 선택된 얼굴이나 현재 페이지에 한정하지 않습니다. 기존에 이미 중복 이름으로 저장된 경우에도 합치기 버튼이 활성화됩니다.

동명이인 오병합을 막기 위해 자동으로 합치지는 않습니다. 빈 이름은 병합 대상이 아니며, 취소하면 변경하지 않습니다. 원본 사진·얼굴 특징값은 유지되고 얼굴 연결만 변경됩니다. 비어 있는 예전 인물 DB 행은 기존 정책대로 남지만 얼굴 없는 인물은 목록에서 제외됩니다.


## 2차: 기능별 책임 분리와 실행 중 작업 정리

`App.tsx`가 화면 전환뿐 아니라 달력·앨범·이미지 표시까지 모두 담당하고 있었습니다. 기능별 수정 범위를 좁히기 위해 약 1,800줄의 파일을 913줄로 줄이고 다음 책임을 옮겼습니다. 코드를 옮기는 작업 자체는 다운로드 크기나 실행 속도를 줄이지 않습니다.

| 파일 | 책임 |
| --- | --- |
| `src/features/calendar/Calendar.tsx` | 달력, 날짜 상세, 일정 등록, 날짜 메모와 일정 저장 |
| `src/features/albums/AlbumsView.tsx` | 앨범 목록, 편집, 감상, 페이지 넘김 |
| `src/features/albums/albums.css` | 앨범 표지와 감상 화면의 전용 디자인 규칙 |
| `src/components/MediaVisual.tsx` | 공통 이미지 출력, 로컬 경로 변환, 빈 화면 |
| `src/hooks/useModalBehavior.ts` | 최상위 모달 키보드 처리와 배경 스크롤 잠금 |

모달은 열린 순서를 스택으로 관리합니다. 가장 마지막에 열린 모달만 방향키·Esc를 처리하므로 앨범 위 사진 상세창을 닫을 때 앨범까지 닫히지 않습니다. 콜백은 ref로 최신 값을 읽고 이벤트 리스너는 활성 상태가 바뀔 때만 등록합니다.

페이지 넘김은 예약 작업입니다. 컴포넌트를 닫거나 다시 섞어도 `setTimeout`은 자동 취소되지 않습니다. 타이머 ID를 ref에 모아 새 작업 시작과 언마운트 때 취소하도록 했습니다. React 상태가 화면에 반영되기 전 빠른 중복 입력도 막기 위해 별도 ref 잠금을 사용합니다.

CSS는 이번에 앨범 전용 디자인 규칙만 분리했습니다. 기존 공통 치수와 애니메이션은 `styles.css`, 전역 테마는 `design-system.css`에 남아 있습니다. 모든 CSS를 한 번에 재배치하면 선택자 우선순위가 바뀔 수 있어 나머지 기능은 점진적으로 옮깁니다. 상세·댓글 컴포넌트와 테스트 fixture 전체 공통화는 후속 대상입니다.

얼굴 인식 테스트에서 늦게 도착한 CDN 폰트 요청을 모델 요청으로 오인하는 경우가 있어 `document.fonts.ready` 이후 네트워크 관찰을 시작하도록 수정했습니다. 모델의 로컬 실행 검증과 앱 전체 오프라인 검증은 다릅니다. 현재 웹폰트 CDN 의존성은 유지되어 있습니다.


## 1차 미사용 코드 정리

미사용 코드는 화면에 보이지 않는 코드와 다릅니다. 모바일 전용 스타일, 실행 중 생성하는 `turning-next` 같은 클래스, DB 이전 버전 호환 코드는 평소 보이지 않아도 필요합니다. 이번에는 JSX 참조와 CSS 선택자를 대조하고, 실제 제거된 화면에 해당하는 코드만 삭제했습니다.

- 예전 등록·통계 박스, 선택 도구막대, 책 양옆 버튼 등의 미사용 CSS 제거.
- `display: none`으로 감추던 중복 제목과 앨범 캡션은 JSX에서도 제거. 더 이상 필요 없는 숨김 스타일도 함께 삭제.
- 빈 샘플 데이터 파일을 제거하고 `useState<MediaItem[]>([])`로 초기화. 미사용 `ImportJob`, `sameMonthItems` 제거.
- 현재 SUIT로 덮어쓰는 명조 폰트 로딩과 참조 없는 이전 애니메이션 제거.

CSS는 PostCSS 파서로 규칙을 구분해 정리했습니다. 쉼표 선택자 중 일부만 미사용일 때는 해당 선택자만 제거해 공유 스타일이 사라지지 않도록 했습니다. 반응형·우선순위가 다른 규칙은 단순 중복으로 간주하지 않았습니다.

`noUnusedLocals`, `noUnusedParameters`를 켜서 앞으로 사용하지 않는 지역 변수와 인수가 빌드에서 드러나게 했습니다. 단, export된 미사용 함수나 CSS까지 자동 검출하는 옵션은 아니므로 참조 검색과 테스트도 필요합니다.

빌드 CSS는 65.39KB에서 60.15KB로 약 8% 감소했습니다. 이것은 파일 크기 감소이며 체감 속도 향상을 측정한 것은 아닙니다. 전체 브라우저 테스트 22개 통과, 2개 기존 의도적 생략. Rust·DB 호환 코드, 테스트·학습 자료와 수동 캡처 도구는 유지했습니다.


## 표지 여백과 키보드 입력의 범위

책의 크기·두께·각도는 유지하고 흰 제목 띠를 제거했습니다. 제목을 표지 상단 중앙, 사진을 아래쪽에 분리해 배치하면 배경색과 여백이 드러나고 제목을 독립적으로 읽을 수 있습니다.

앨범 방향키는 버튼과 동일한 `turnPage()`를 호출합니다. 애니메이션 중 입력과 키를 길게 눌렀을 때의 반복 입력을 막고, 입력 필드에서는 글 편집을 방해하지 않습니다. 사진 상세창이 위에 열려 있으면 앨범 핸들러가 입력을 처리하지 않아 두 화면이 동시에 넘어가거나 닫히는 것을 방지합니다.


## 목록 크기와 레이아웃 공간

CSS `transform: scale()`은 보이는 크기만 바꾸며 주변 요소의 배치 공간은 늘리지 않습니다. 앨범 확대와 함께 행 높이를 246px에서 360px로 조정해 다음 줄과 겹치지 않도록 했습니다. 표지 글자는 제목만 남겼습니다.


## 앨범 두께를 표현하는 방법

그림자만 진하게 하면 떠 있는 평면처럼 보일 수 있습니다. 목록은 `book-cover-3d`의 두께를 24px에서 48px로 늘리고 회전각을 32도로 조정해 실제 옆면이 보이게 했습니다. 라이브러리에 없는 왼쪽 책등 면은 같은 3D 좌표계에 추가했습니다. 종이 옆면에는 반복 선을 사용합니다.

내부는 펼친 종이, 종이 묶음의 단면, 하드커버를 별도 층으로 표현합니다. `isolation: isolate`로 쌓임 범위를 책 내부에 한정하고 가상 요소를 뒤에 배치해 사진·넘김 레이어를 가리지 않게 합니다. 이는 감상 화면의 CSS 입체 표현이며 새로운 3D 엔진 도입은 아닙니다.


## 앨범 형태와 감상 레이아웃 분리

책의 입체 형태는 기존 `book-cover-3d` 라이브러리에 맡기고, 표지 안 콘텐츠만 큰 사진과 제목 영역으로 바꿨습니다. 책등과 표지의 각도를 별도 CSS로 흉내 내면 서로 어긋날 수 있으므로 공통 변환은 라이브러리를 유지합니다.

감상 화면은 CSS Grid로 상단 64px, 중앙 가변 높이, 하단 72px로 나눕니다. 페이지 이동 버튼이 책 바깥 독립 영역에 있으면 책 크기가 달라져도 겹치지 않습니다. 모바일 캡처에서 기존 세로 방향 헤더 규칙이 남아 버튼이 넘치는 것을 발견해 가로 방향과 세로 중앙 정렬을 명시했습니다.


## 색상 변경과 구조 변경의 차이

색·폰트·테두리만 바꾸는 것은 시각 스타일 변경입니다. 이번 피드백은 사용자가 정보를 보고 조작하는 순서 자체를 바꾸라는 뜻이었습니다. 그래서 CSS뿐 아니라 `App.tsx`의 JSX 트리(화면 요소의 포함 관계)를 변경했습니다.

기존의 왼쪽 메뉴 → 등록·통계 → 사진 목록 구조를 상단 메뉴 → 검색·등록 → 보기 탭과 선택 → 사진 목록으로 바꿨습니다. 상세보기 역시 사진 옆 편집 칸 대신 사진 아래에 정보를 배치했습니다. 사진은 넓어지고 댓글은 필요할 때 접을 수 있습니다.

CSS Grid의 `minmax(0, 1fr)`는 남는 공간을 쓰면서 내용 때문에 영역이 무한히 커지는 것을 막습니다. 상세창은 사진 영역과 자동 높이의 댓글 영역을 행으로 나누고, 긴 댓글은 패널 내부에서 스크롤하도록 제한합니다.

스크린샷만으로는 회귀를 자동 탐지하기 어렵습니다. Playwright의 `boundingBox()`로 댓글 영역이 사진 아래인지, 댓글을 접었을 때 사진 높이가 증가하는지 검사했습니다. 캡처에서 발견한 작은 하트 아이콘은 기존 텍스트 버튼의 좌우 패딩이 원인이어서, 아이콘 버튼의 패딩을 0으로 명시하고 SVG 축소를 막았습니다.

## 세 디자인 문서를 조합한 이유

브랜드 디자인을 참고할 때는 색과 글꼴을 그대로 복사하기보다 우리 앱의 행동에 필요한 원칙을 선택해야 합니다. Apple에서는 사진을 돋보이게 하는 여백, Nike에서는 단순한 사진 그리드와 명암 대비, Raycast에서는 짙은 메뉴와 간결한 작업 영역을 가져왔습니다. 한국어 SUIT 글꼴, 사진·날짜 중심 목록, 상세 댓글, 달력과 앨범 흐름은 유지했습니다.

등록·통계 카드를 얇은 도구 영역으로 바꾸면 같은 화면에서 사진을 더 많이 볼 수 있습니다. 모달에만 높이·그림자를 주고 일반 섹션은 평평하게 만들면 정보의 위계도 분명해집니다. 스타일 토큰과 판단 근거는 [디자인 시스템](docs/design-system.md)에 정리했습니다.

## 얼굴 인식 첫 버전에서 배울 점

이번 기능의 정확한 역할은 얼굴을 찾아 비슷한 얼굴끼리 분류하는 것입니다. 모델이 가족의 이름을 알아내지는 않습니다. 사용자가 이름을 붙이고 잘못 묶인 얼굴을 분리하거나 이동합니다.

기존 Tauri WebView에서 동작하는 `@vladmandic/face-api`를 사용했습니다. 사진마다 얼굴 탐지 → 얼굴 위치 정렬 → 128차원 특징 벡터 추출 → 기존 인물과 비교 순서로 처리합니다. 특징 벡터는 얼굴의 모양을 비교하기 위한 숫자 배열입니다. 두 벡터의 거리가 가까우면 비슷한 얼굴로 판단하지만, 정확히 같은 사람임을 보장하는 인증 수단은 아닙니다.

모델은 npm 패키지에 포함된 파일을 빌드할 때 앱에 복사합니다. 따라서 앱 실행 중 모델 다운로드나 사진 업로드가 필요하지 않습니다. 실행 엔진은 동적 import로 얼굴 찾기 버튼을 눌렀을 때만 불러옵니다. 모델 같은 재생성 가능한 큰 파일은 Git에서 제외하고 설치 버전을 lockfile로 관리합니다.

SQLite의 `detected_face`에는 사진과 인물의 연결, 얼굴 벡터, 얼굴 미리보기를 저장합니다. `face_scan`에는 분석 완료된 사진을 기록합니다. 얼굴이 없는 사진도 완료이고, 실패한 사진은 미완료라는 구분이 있어야 중단·재개와 재시도가 올바르게 동작합니다. 한 사진의 결과와 완료 기록은 트랜잭션으로 함께 저장합니다.

자동 분류에서는 하나의 최적 후보만 보는 것보다 다음 후보와 얼마나 차이 나는지도 확인하는 편이 안전합니다. 현재 거리 0.45, 후보 간 차이 0.06을 사용하며 실사용 사진으로 검증·조정해야 하는 경험값입니다. 자세한 구조와 용어, 실행·검증 방법은 [얼굴 인식 학습 문서](docs/face-recognition.md)에 정리했습니다.

이 문서는 `오래담은` 프로젝트를 만들며 배운 내용을 복습하기 위한 최상단 학습 노트입니다. 단순히 “무엇을 했다”보다 “왜 그렇게 했는지”, “다음에는 무엇을 이해해야 하는지”를 남기는 용도입니다.

## 프로젝트 목표

`오래담은`은 사진과 영상을 인터넷 서비스에 올리지 않고, 내 PC 안에서 안전하게 등록하고 분류하고 감상하는 로컬 전용 앨범 앱입니다.

핵심 방향은 세 가지입니다.

- 원본 파일은 사용자가 가진 폴더에 그대로 둔다.
- 앱은 파일 경로, 날짜, 별점, 코멘트 같은 정리 정보만 데이터베이스에 저장한다.
- 화면은 개발 용어보다 사용자가 사진을 찾고 보는 흐름을 먼저 보여준다.

## 왜 로컬 전용인가

사진과 영상은 사적인 데이터입니다. 클라우드 서버를 쓰면 로그인, 업로드, 저장 비용, 개인정보 처리, 네트워크 장애 같은 문제가 따라옵니다.

이 프로젝트는 “내 컴퓨터 안의 추억 보관함”이 목적이므로, 서버 없이 동작하는 구조가 더 잘 맞습니다. 그래서 웹 서비스보다 데스크톱 앱에 가깝게 설계했습니다.

## 왜 이 기술 스택을 선택했나

### Tauri

Tauri는 웹 기술로 화면을 만들고, Rust로 운영체제 기능을 연결하는 데스크톱 앱 프레임워크입니다.

이 프로젝트에서 Tauri를 쓰는 이유:

- 로컬 파일 선택 창을 열 수 있다.
- 파일과 폴더를 실제로 읽을 수 있다.
- SQLite 같은 로컬 데이터베이스를 앱 내부에서 사용할 수 있다.
- Electron보다 앱 크기와 메모리 사용량이 작은 편이다.

### React

React는 화면 상태가 자주 바뀌는 앱에 적합합니다. `사진보기`, `달력보기`, `앨범보기`처럼 같은 데이터를 여러 방식으로 보여줄 때 컴포넌트 단위로 나누기 좋습니다.

이 프로젝트에서 React를 쓰는 이유:

- 탭 전환, 선택된 월, 선택된 앨범 페이지 같은 UI 상태를 다루기 쉽다.
- 카드, 달력 칸, 앨범 책장 같은 반복 화면을 컴포넌트로 만들 수 있다.
- 나중에 검색, 필터, 상세 보기 기능을 붙이기 쉽다.

### TypeScript

TypeScript는 JavaScript에 타입을 더한 언어입니다. 사진 데이터처럼 필드가 많은 객체를 다룰 때 실수를 줄여줍니다.

예를 들어 `MediaItem`에 `id`, `name`, `kind`, `takenAt`, `favorite` 같은 필드가 있다고 정해두면, 화면 코드와 저장 코드가 같은 구조를 바라보게 됩니다.

### SQLite

SQLite는 서버 없이 파일 하나로 동작하는 데이터베이스입니다.

이 프로젝트에서 SQLite를 쓰는 이유:

- 로컬 앱과 잘 맞는다.
- 설치형 DB 서버가 필요 없다.
- 사진이 수천, 수만 장으로 늘어나도 단순 JSON 파일보다 검색과 정렬에 유리하다.
- 앱을 다시 켜도 등록 목록, 별점, 코멘트가 유지된다.

### Rust

Rust는 Tauri의 백엔드 쪽 언어입니다. 파일 시스템, 데이터베이스, 운영체제 기능처럼 안전성이 중요한 부분을 맡습니다.

이 프로젝트에서는 React가 화면을 담당하고, Rust가 실제 로컬 작업을 담당합니다.

### Vite

Vite는 React 개발 서버와 빌드 도구입니다. 화면을 빠르게 띄우고 수정 결과를 즉시 확인하기 위해 사용합니다.

### Playwright

Playwright는 브라우저에서 사용자 흐름을 자동으로 클릭해보는 테스트 도구입니다. 메뉴가 보이는지, 탭이 전환되는지, 달력과 앨범 화면이 깨지지 않는지 확인하는 데 사용합니다.

## 지금까지의 진행 방식

1. 제품의 핵심 가치를 먼저 정했다.
   로컬 전용, 원본 보호, 한국어 기본 화면, 사진 감상 중심.

2. 화면 MVP를 먼저 만들었다.
   실제 저장 기능보다 사용자가 어떤 흐름으로 사진을 볼지 확인하는 것이 먼저였습니다.

3. 디자인을 사용자 관점으로 고쳤다.
   `Phase`, `DB`, `metadata` 같은 개발 용어는 화면에서 제거하고, `사진보기`, `모아보기`, `달력보기`, `앨범보기`처럼 행동 중심 이름으로 바꿨습니다.

4. Tauri와 SQLite를 연결했다.
   이제 Tauri 런타임에서는 파일 선택과 폴더 선택으로 실제 로컬 미디어 경로를 등록할 수 있습니다.

5. 학습 로그를 정리했다.
   이후 프로젝트를 다시 볼 때 “왜 이런 구조가 되었는지”를 따라갈 수 있게 문서 역할을 나눴습니다.

6. 실행 화면 기준으로 레이아웃을 다시 정리했다.
   참고 자료의 디자인 루프처럼 실제 데스크톱/모바일 화면을 캡처해 보고, 사용자가 먼저 해야 하는 행동과 상태 정보가 잘 보이는지 기준으로 상단 구조를 바꿨습니다.

## 이번 레이아웃 변경에서 배운 점

레이아웃은 단순히 예쁘게 배치하는 일이 아니라, 사용자의 다음 행동을 정하는 일입니다.

이번 화면에서는 `사진보기`가 핵심이므로 다음 순서로 읽히게 만들었습니다.

1. 지금 어떤 화면인지 확인한다.
2. 파일이나 폴더를 등록한다.
3. 등록된 기록 수를 빠르게 본다.
4. 모아보기, 달력보기, 앨범보기 중 원하는 방식으로 감상한다.

상단에 큰 설명만 있으면 감성은 생기지만 실제 앱처럼 쓰기에는 행동이 약해집니다. 그래서 등록 버튼은 크게 유지하고, 전체 기록/사진/영상/즐겨찾기는 작은 현황 카드로 분리했습니다.

모바일에서는 메뉴가 두 줄이 되면 실제 콘텐츠가 아래로 밀립니다. 이럴 때는 모든 메뉴를 억지로 한 화면에 끼워 넣기보다, 가로 스크롤 메뉴로 만들어 높이를 줄이는 방법이 실용적입니다.

## 중요한 설계 원칙

- 사용자가 보는 화면에는 사용자가 이해해야 하는 말만 둔다.
- 개발자가 알아야 하는 구현 설명은 문서나 코드 안에 둔다.
- 원본 파일은 건드리지 않는다.
- 앱 데이터베이스에는 정리 정보만 저장한다.
- 같은 사진 목록의 다른 표현은 별도 메뉴가 아니라 탭으로 묶는다.
- 한국적인 분위기는 과한 전통 장식보다 문장, 재료감, 여백, 차분한 색으로 만든다.

## 다음 학습 주제

- Tauri 명령이 React에서 어떻게 호출되는지 이해하기
- SQLite 테이블 구조와 쿼리 읽기
- 파일 등록 시 중복을 막는 방식 이해하기
- EXIF 날짜와 파일 수정일의 차이 이해하기
- 썸네일 생성이 왜 별도 작업인지 이해하기
- Playwright로 사용자 흐름을 테스트하는 법 익히기

## 앨범 표지 라이브러리 적용

`내 앨범` 목록의 앨범 표지는 직접 CSS로 만들다가 `book-cover-3d` 라이브러리로 바꿨습니다.

직접 CSS로 표지, 책등, 페이지 두께를 각각 만들면 각도와 그림자가 서로 따로 움직여 한 물체처럼 보이지 않는 문제가 생겼습니다. `book-cover-3d`는 표지 앞면, 책 두께, 뒷면을 하나의 3D 좌표계에서 계산하므로 앨범이 더 자연스럽게 보입니다.

선택 이유:

- React 컴포넌트라 현재 프로젝트에 바로 붙일 수 있다.
- `width`, `height`, `rotate`, `thickness`, `pagesOffset` 값으로 앨범 크기와 두께를 조절할 수 있다.
- `react-pageflip`처럼 페이지 넘김 전체 기능을 붙이는 라이브러리보다, 지금 필요한 “앨범 표지 모양”에 범위가 더 잘 맞다.

이번 작업에서 배운 점:

- 시각적으로 복잡한 사물은 CSS 조각을 계속 보정하는 것보다, 목적에 맞는 작은 라이브러리를 쓰는 편이 더 안정적일 수 있다.
- 라이브러리를 고를 때는 유명한지보다 “지금 화면의 문제를 정확히 해결하는지”를 먼저 봐야 한다.
- 라이브러리를 추가하면 `package.json`과 `package-lock.json`이 함께 바뀐다. 다른 PC에서 실행할 때는 `npm install`이 이 파일들을 기준으로 같은 패키지를 설치한다.

관련 문서:

- `IMPLEMENTATION_LOG.md`: 실제 구현 구조와 실행 흐름
- `DESIGN_QA_LOG.md`: 화면 설계와 QA 판단
- `TECH_TERMS.md`: 프로젝트에서 나온 용어 정리

## Git에 올리지 않아야 하는 파일

Git에는 사람이 직접 관리하는 소스 코드, 설정, 문서만 올리는 것이 좋습니다. 다시 만들 수 있는 결과물이나 개인 PC에서만 의미가 있는 파일을 올리면 저장소가 너무 커지고, GitHub 같은 원격 저장소에 업로드가 실패할 수 있습니다.

이 프로젝트에서 특히 조심해야 하는 파일은 다음과 같습니다.

- `src-tauri/target/`: Rust와 Tauri가 빌드하면서 만드는 결과물입니다. 용량이 매우 크지만 `npm run tauri:dev`나 빌드 명령을 실행하면 다시 생성됩니다.
- `node_modules/`: npm 패키지 설치 결과입니다. `package.json`과 `package-lock.json`만 있으면 `npm install`로 다시 만들 수 있습니다.
- `dist/`: Vite가 만든 프론트엔드 빌드 결과입니다. 배포 전에 다시 만들 수 있습니다.
- `screenshots/`: 화면 QA를 위해 찍은 이미지입니다. 학습 기록에는 유용하지만 Git 저장소에는 꼭 필요하지 않습니다.
- `*.db`, `*.sqlite`: 로컬 앱 데이터베이스입니다. 개인 사진 경로와 사용 기록이 들어갈 수 있으므로 저장소에 올리지 않는 편이 안전합니다.

이미 Git이 추적하기 시작한 파일은 `.gitignore`에 추가해도 자동으로 빠지지 않습니다. 이때는 `git rm --cached`를 사용합니다.

`git rm --cached`는 로컬 파일을 삭제하지 않고, Git의 추적 목록에서만 제거하는 명령입니다. 즉 내 컴퓨터에는 파일이 그대로 남고, 다음 커밋부터 저장소에 올라가지 않게 됩니다.

## 메뉴 설명과 장식 정리

## 앨범 수정과 삭제의 데이터 흐름

## 매년 반복 일정의 날짜 표현

일회성 일정은 `YYYY-MM-DD`, 매년 반복 일정은 `MM-DD`와 `yearly: true`로 저장합니다. 반복 행사에 임의의 연도를 넣지 않아도 되고, 모든 연도의 일정을 미리 복사할 필요도 없습니다. 기존 데이터에 `yearly`가 없으면 일회성 일정으로 처리합니다.

달력은 `eventsOnDate`에서 표시할 날짜의 월·일과 반복 일정의 월·일을 비교합니다. 디데이 계산에는 현재 보고 있는 달력 연도의 실제 날짜를 전달합니다. 화면에 표시한 날짜와 저장 키가 다르므로 수정·삭제는 일정 ID로 원본 저장 위치를 찾습니다.

반복 날짜 입력은 월·일 셀렉트를 사용합니다. 월을 바꿀 때 말일을 넘으면 자동으로 줄이고, 윤년인 2000년을 기준으로 선택지를 만들어 2월 29일도 등록할 수 있습니다. 이 일정은 윤년에만 표시합니다. 일정 저장소는 기존과 같은 로컬 `localStorage`이며 Windows 알림 연동 변경은 없습니다.

### 앨범 데이터 흐름

앨범보기는 전체창을 닫을 때 `photoMode`를 `grid`로 돌립니다. 전체창 뒤에는 모아보기를 렌더링하므로 빈 앨범 메뉴가 남지 않습니다.

앨범 편집에서는 원래 객체를 바로 변경하지 않고 `draft`라는 임시 상태에 제목, 색상, 사진 목록을 보관합니다. 저장을 누를 때만 부모 목록과 DB를 갱신하고, 취소하면 임시 상태를 버립니다. 이런 구조가 있어야 취소한 변경이 다른 화면에 반영되지 않습니다.

`media`는 등록된 사진이고 `album_item`은 앨범과 사진 사이의 연결입니다. 앨범에서 사진을 빼는 작업은 연결만 변경합니다. 앨범 삭제도 `album`과 연결된 `album_item`만 제거하므로 원본 파일과 `media`는 유지됩니다.

Tauri의 `update_album` 명령은 제목, 색상, 표지 사진, 사진 순서를 SQLite 트랜잭션으로 함께 저장합니다. 트랜잭션은 여러 SQL을 한 작업으로 묶어 중간에 실패하면 이전 상태로 되돌립니다. 사진 삭제 후 첫 번째 남은 사진을 표지로 지정하고, 모두 빼면 표지는 비웁니다. 여러 앨범을 삭제하는 `delete_albums`도 트랜잭션을 사용합니다.

사진 선택은 ID 목록으로 관리하며 편집 화면은 24장씩 표시합니다. 페이지를 바꿔도 선택한 ID가 남아 여러 페이지의 사진을 한 번에 뺄 수 있습니다. 새 라이브러리 없이 React 상태와 기존 Tauri/rusqlite를 사용했습니다.

검증: 프론트엔드 빌드, MSVC Rust 검사, 데스크톱/모바일 Playwright 14개 통과. 실제 Tauri 창의 DB 저장·재실행은 이번 검사에 포함하지 않았습니다. 기본 GNU Rust에서는 `dlltool.exe`가 없어 실패했지만 설치된 MSVC 도구 체인으로 검사했습니다.

### 메뉴 문구 정리 배경

메뉴 제목과 버튼만으로 동작을 이해할 수 있으면 반복 설명은 사진이 차지할 공간을 줄이고 읽을 내용을 늘립니다. 각 보기의 설명 문구와 상단의 긴 안내를 제거하고, 파일 등록 영역에는 짧은 제목만 남겼습니다. 빈 목록, 진행 상태, 삭제 시 원본 보존 안내는 현재 상태와 결과를 판단하는 데 필요하므로 유지했습니다.

앨범 상단 줄은 `albumCoverRule` 요소와 CSS를 함께 제거했습니다. CSS로 숨기기만 하면 불필요한 DOM과 스타일이 남으므로, 사용하지 않는 장식은 렌더링 구조에서도 지우는 것이 좋습니다.

## 앨범 내부를 컨셉별로 다시 설계한 방법

이번 변경은 기존의 좌우 흰 페이지 구조를 꾸미는 방식이 아니라 `bookSpread`, `bookPage`, `leftPage`, `rightPage`, `albumPhoto` DOM과 CSS를 삭제하고 새 구조로 교체했다.

- `scrapbookStage`: 앨범 종이와 하단 페이지 두께를 함께 배치하는 무대
- `scrapbookSheet`: 컨셉별 종이 질감 이미지를 표시하는 한 장의 작업면
- `scrapbookPrint`: 실제 사용자의 사진을 올리는 클릭 가능한 인화 사진
- `scrapbookTurnLayer`: 다음·이전 이동 중 종이가 회전하는 전환 레이어

| 컨셉 | 참고 이미지 | 내부 구성 | 한 책장 사진 수 |
| --- | --- | --- | --- |
| 디자인1 | 두 번째 이미지 | 밝은 민트 여행 스크랩북 | 4장 |
| 디자인2 | 첫 번째 이미지 | 아이보리 종이 콜라주 | 6장 |
| 디자인3 | 세 번째 이미지 | 오래된 갈색 가족 앨범 | 6장 |
| 디자인4 | 네 번째 이미지 | 크림색 양면 카페 앨범 | 7장 |

배경 소재는 Imagegen으로 사진 영역을 비운 종이·테이프·압화 중심의 PNG를 만들었다. 실제 등록 사진을 생성 이미지에 합성하지 않고 React가 별도로 올리므로, 앨범 내용이 바뀌어도 같은 컨셉을 재사용할 수 있다. 파일은 `public/textures/album-inside-design1.png`부터 `album-inside-design4.png`까지 저장했다.

`pagePhotos`를 컨셉 메타데이터에 두어 화면과 페이지 분할 로직이 같은 값을 사용한다. 앨범 상세의 원본 이미지는 화면 진입 즉시 요청하고, 목록 썸네일만 지연 로딩한다. 이렇게 해야 첫 책장이 열린 직후 빈 인화지가 잠깐 보이는 현상을 줄이면서 목록 성능은 유지할 수 있다.

### 모든 컨셉을 양면 4:3 앨범으로 통일

처음에는 디자인1~3이 한 장의 스크랩북 종이처럼 보이고 디자인4만 양면 앨범이었다. 이를 수정해 네 배경 모두 중앙 홈, 좌우 종이 곡률, 바깥 페이지 두께가 포함된 `4:3` 펼침 이미지로 다시 제작했다.

CSS의 `aspect-ratio: 4 / 3`만 바꾸면 화면 높이가 낮을 때 앨범이 잘릴 수 있다. 그래서 너비를 `calc((100dvh - 헤더·하단·여백) * 4 / 3)`로 제한해 가로와 세로 조건을 함께 만족시켰다. 사진 위치도 중앙 50%를 경계로 왼쪽 또는 오른쪽에만 들어가도록 다시 정의했다.

페이지 번호는 한 책장 번호가 아니라 왼쪽·오른쪽 번호를 따로 표시한다. 페이지 전환 레이어도 전체 펼침이 아니라 진행 방향의 절반만 회전하므로 실제 양면 책에서 한 장을 넘기는 구조에 가깝다.

### 페이지 넘김에서 배경과 사진을 분리하는 이유

종이 배경까지 교체하면 검거나 하얀 화면이 끼어 전체 페이지가 새로고침되는 것처럼 보인다. 따라서 컨셉 배경과 책등은 고정하고, 회전하는 빈 종이와 사진 레이어만 시간차로 처리한다. 회전 종이에는 `backface-visibility: hidden`을 적용해 90도를 지난 뒷면이 계속 남지 않게 했다.

검은색 가림막이나 고정된 흰색 종이는 현재 앨범과 다른 재질처럼 보여 제거했다. 종이 배경은 전환 내내 고정하고 사진 레이어만 교체한다. 다음 장은 오른쪽 사진을 즉시 숨기고 100ms 뒤 왼쪽 사진을 숨긴다. 300ms에 다음 사진 데이터로 바꾼 다음 오른쪽 사진은 40ms, 왼쪽 사진은 150ms 뒤 표시한다. 각 페이드는 140ms이며 이전 장은 이 순서를 좌우 반대로 적용한다.

## 인물 대표 썸네일 저장 방식

기존 인물 목록은 각 그룹에서 가장 먼저 조회된 얼굴을 무조건 썸네일로 사용했다. 사용자가 고른 얼굴을 유지하려면 화면 상태만 바꾸는 것으로는 부족하므로 `person.cover_face_id`에 대표로 선택한 `detected_face.id`를 저장한다.

`cover_face_id`는 외래 키다. 외래 키는 다른 테이블의 실제 행을 가리키도록 제한하는 DB 규칙이다. 대표 얼굴을 삭제하면 `ON DELETE SET NULL`에 의해 대표 설정만 자동으로 비워지고 인물 자체는 유지된다. 얼굴을 다른 인물로 옮길 때도 이전 인물의 대표 참조를 먼저 해제해 잘못된 얼굴이 남지 않게 했다.

목록을 읽을 때 선택된 대표 얼굴이 현재 인물에게 속하고 제외되지 않았다면 그 썸네일을 사용한다. 해당 얼굴이 없으면 첫 번째 유효 얼굴을 대신 사용하므로 기존 데이터와도 호환된다. 이전 DB에는 열이 없으므로 앱을 열 때 멱등 마이그레이션으로 `cover_face_id`를 한 번만 추가한다.

## 선택 출처가 여러 화면일 때 앨범 생성 흐름

기존 앨범 생성은 모아보기의 `selectedIds`에 직접 의존해 인물 화면에서 재사용할 수 없었다. 이번에는 모달에 전달할 원본 사진 배열을 `albumDraftItems`로 분리했다. 모아보기와 인물 상세가 각각 선택 결과를 이 배열로 넘기고, 생성 함수는 배열만 보고 같은 Tauri 명령을 호출한다. 기능의 시작 화면과 실제 저장 로직을 분리하면 선택 UI가 늘어나도 저장 코드를 복제하지 않아도 된다.

얼굴 선택값은 `detected_face.id`, 앨범 항목은 `media.id`이므로 그대로 전달하면 잘못된 ID가 저장된다. 선택된 얼굴에서 `media_id`를 `Set`으로 모은 뒤 원본 `MediaItem[]`으로 변환했다. 같은 사진 안의 얼굴이 여러 번 선택되어도 한 사진은 앨범에 한 번만 들어간다. 앨범 생성은 원본 파일 이동이 아니라 기존 미디어 ID를 `album_item`으로 참조하는 방식이다.

앨범 hover는 표지 전체에 `translateY + rotate` 키프레임을 적용해 좌우 흔들림과 떠오르는 강조를 함께 표현했다. 시간순 카드는 짧은 이동, 테두리, 그림자만 사용해 더 차분하게 구분했다. `prefers-reduced-motion` 환경에서는 반복 흔들림을 끄고 위치 강조만 남긴다.

## 정렬·필터와 조회수 저장 방식

정렬과 필터는 원본 `items` 배열을 직접 바꾸지 않고 `useMemo`에서 새로운 목록을 계산한다. 먼저 검색 결과에 미디어 종류, 즐겨찾기, 댓글 유무, 최소 별점 조건을 모두 적용한 다음 정렬한다. 여러 필터를 동시에 선택하면 조건을 `AND`로 결합하므로 모든 조건을 만족하는 사진만 남는다.

정렬 값이 같은 사진은 촬영 날짜를 보조 기준으로 사용한다. 이를 안정적인 정렬 기준이라고 한다. 예를 들어 별점이 같은 사진이 매 렌더링마다 자리를 바꾸지 않아 사용자가 목록의 위치를 기억하기 쉽다. 이름순은 화면에 원본명을 표시하지 않되 내부 파일명을 한국어 로케일과 숫자 인식 옵션으로 비교한다.

댓글은 기존 `media.comment` 한 칸만 세면 실제 댓글 개수와 달라질 수 있다. 따라서 로컬 댓글 목록을 미디어 ID별 개수로 계산해 댓글 많은순과 댓글 있는 사진 필터에 공통 사용한다.

조회수는 상세보기를 열거나 상세에서 이전·다음 사진으로 이동할 때 1씩 증가한다. 프론트엔드는 클릭 즉시 낙관적으로 값을 갱신하고, Tauri 명령은 SQLite의 `view_count = view_count + 1` 연산으로 저장한다. 기존 DB에는 `migrate_media_view_count`가 열의 존재 여부를 먼저 검사한 후 기본값 0으로 추가하므로 기존 사진 행을 잃지 않는다.

## 앨범 컨셉을 색상 기반 제품 구조로 단순화

기존 디자인1~4는 컨셉마다 표지 이미지, 내부 배경 이미지, 사진 수와 배치 규칙이 달랐다. 새 요구사항은 한 종류의 실물 포토앨범을 색상만 바꾸는 구조이므로 `AlbumConcept`와 컨셉 메타데이터를 제거하고 `coverColor` 하나를 디자인 상태의 기준으로 삼았다. 사용자가 고른 색은 React 임시 상태가 아니라 SQLite `album.cover_color`에 저장되어 앱을 다시 실행해도 유지된다.

표지는 별도 이미지나 앨범 라이브러리 없이 CSS로 만들었다. 단색 무광 커버 위에 얕은 책등, 하단 종이 두께, 중앙 사진창을 서로 다른 레이어로 구성한다. 처음 적용한 가로·세로 반복선은 작은 모눈처럼 보여 제거하고, 반복되지 않는 약한 표면 명암만 남겼다. 실제 첫 사진을 창 안에 사용하므로 장식 이미지보다 앨범을 구별하기 쉽고, 표지 파일을 추가 생성하지 않아도 된다. 이전 컨셉용 PNG 9개와 관련 렌더링 코드는 모두 제거했다.

내부는 고정된 `4:3` 양면 링 바인더다. 한 펼침면에 왼쪽 2장, 오른쪽 2장을 배치하고 중앙 금속 레일과 링을 별도 레이어로 둔다. 사진 수가 항상 4장이라 페이지 계산과 전환 시점도 컨셉 분기 없이 한 코드로 관리할 수 있다. 페이지 넘김은 진행 방향 사진을 먼저 비우고 반대쪽을 100ms 뒤 비운 다음, 다음 데이터의 사진을 순차 표시한다.

기존 DB의 `cover_concept` 열은 삭제하지 않았다. SQLite에서 기존 열을 물리적으로 제거하는 마이그레이션은 데이터 손상 위험과 복잡도가 큰 데 비해 얻는 이점이 작기 때문이다. 새 코드에서는 읽거나 노출하지 않고, 오래된 DB의 NOT NULL 제약을 만족시키기 위한 호환 값만 저장한다. UI와 도메인 모델에서는 완전히 제거된 레거시 필드다.

앨범 상세에서 사진을 열면 조회수가 바뀌어 `items` 배열 객체가 새로 만들어진다. 배열 참조 자체를 페이지 초기화 조건으로 쓰면 같은 사진 목록인데도 1쪽으로 돌아간다. 초기화 의존성을 사진 ID 순서 문자열로 좁혀, 사진의 추가·삭제·순서 변경 때만 페이지를 다시 만들고 조회수 같은 메타데이터 변경에는 현재 쪽을 유지하도록 했다.

## 사진 확대 뷰어와 날짜 정보의 우선순위

상세 화면 안에서 이미지 크기만 키우면 댓글과 별점 영역이 계속 남아 감상 공간이 줄어든다. 확대 보기를 별도의 중첩 모달로 분리해 사진과 배율 도구만 렌더링했다. 배율은 `1`부터 `4`까지의 숫자 상태 하나로 관리하며 버튼과 `range` 입력이 같은 변경 함수를 사용한다. 확대된 캔버스의 너비와 높이를 배율만큼 키우고 바깥 영역에 스크롤을 주어 원본의 원하는 부분을 볼 수 있다. 중첩 모달도 `useModalBehavior`를 사용하므로 첫 번째 `Esc`는 확대 화면만 닫고 상세 화면은 유지한다.

지난 추억은 고정 날짜가 아니라 현재 로컬 날짜의 월·일과 `takenAt`을 비교한다. 현재 연도 사진은 제외해 실제 과거 기록만 세고, 결과가 1개 이상일 때만 메뉴 숫자 배지를 표시한다. 내용이 없을 때 `0` 배지를 반복 노출하지 않아 메뉴의 시각적 잡음을 줄였다.

달력 칸은 날짜, 일정, 사진, 메모 순으로 위계를 정했다. 일정은 날짜 바로 아래 상단에 두고 사진 시작 위치를 일정 아래로 내렸다. 모바일의 7열 칸은 텍스트와 D-day를 함께 표시할 폭이 부족하므로 포스트잇 색과 분류 아이콘만 표시하고, 일정 제목은 접근성 이름과 툴팁에 유지한다.

## 모션 없는 앨범 강조와 공용 내부 구조

반복 흔들림은 클릭 가능한 앨범을 눈에 띄게 만들지만 여러 표지가 한 화면에 있을 때 시선이 계속 움직여 피로를 줄 수 있다. hover와 키보드 포커스에서 `transform`과 `animation`을 사용하지 않고 밝기, 채도, 표지 그림자만 짧게 전환하도록 바꿨다. 요소의 위치가 고정되므로 주변 레이아웃도 흔들리지 않는다.

내 앨범과 사진보기의 앨범보기는 모두 `AlbumFullscreenReader`를 사용한다. 중앙 스프링 DOM과 CSS를 이 공용 컴포넌트에서 제거했기 때문에 두 진입 경로가 자동으로 같은 스타일을 사용한다. 금속 레일 대신 2px 중앙선과 좌우 안쪽 그림자로 책의 접힘만 표현하고, 페이지 넘김 로직과 사진 4장 배치는 유지했다.

## Warm Journal 화면 구조로 전면 개편

이번 개편은 기존 화면의 색상만 교체하지 않고 정보 구조를 다시 정리했다. 왼쪽 사이드바를 제거하고 상단에 브랜드, 사진 기록, 앨범, 사람과 반려동물, 추억, 설정을 한 줄로 배치했다. 현재 메뉴는 배경색 카드 대신 하단 올리브색 선으로 표시해 사진보다 내비게이션이 강하게 보이지 않도록 했다. 검색과 가져오기는 각 화면 제목 옆의 작업 영역으로 이동했다.

사진 기록은 평평한 썸네일 목록 대신 촬영일별 `journalDay`와 `journalMosaic`으로 구성한다. 날짜 제목과 사진 수를 먼저 읽은 뒤 사진을 훑을 수 있고, 같은 날짜 사진이 네 장 이상이면 첫 사진을 크게 배치한다. 사진이 한 장뿐인 날은 두 열 너비를 사용해 넓은 여백 속에서 사진이 지나치게 작아지지 않게 했다. 기존 선택과 드래그 선택은 각 사진 버튼의 `data-selection-id`를 유지했기 때문에 날짜 섹션이 추가되어도 그대로 동작한다.

상세보기는 어두운 배경과 우측 정보 패널을 없애고, 상단에 촬영일과 도구를 둔 밝은 전체 화면으로 바꿨다. 사진은 흰 프레임 안에서 원본 비율로 표시하고 메타데이터, 별점, 댓글은 하단 영역에 둔다. 댓글 입력·수정·삭제, 확대 보기, 이전·다음 키보드 이동은 기존 상태와 이벤트를 재사용한다.

앨범 목록은 색상 전체 면을 강조하던 표지 대신 아이보리 하드커버, 색상 책등, 큰 표지 사진, 하단 제목으로 다시 구성했다. 사용자가 선택한 앨범 색은 표지 책등과 내부 하드커버 가장자리에 남아 기존 색상 설정 기능을 유지한다. 앨범 내부는 밝은 4:3 양면 책장으로 바꾸고 사진 테두리와 중앙 접힘을 최소화했다. 페이지 넘김 중 사진이 방향별로 비워졌다 다시 나타나는 기존 타이밍은 유지했다.

검증 중 상단 내비게이션의 `z-index`가 달력 모달보다 높아 모달 바깥 클릭을 가로막는 회귀를 발견했다. 일반 모달과 사진 상세의 레이어 값을 내비게이션보다 높게 조정했다. 또한 사진 기록과 인물 상세의 열 간격을 모두 7px로 맞춰 같은 화면 폭에서 썸네일 너비가 정확히 일치하도록 했다. 모바일에서는 큰 대표 사진의 `height: 100%`가 자동 높이 그리드와 순환 계산을 일으켜 첫 사진이 0 높이가 될 수 있어, 620px 이하에서 명시적인 `aspect-ratio`로 되돌렸다.

### 시안과 동일한 화면 구획으로 재구성

첫 번째 개편은 색과 타이포그래피는 시안에 가까웠지만 사진 기록의 본문이 여전히 한 열이었다. 두 번째 개편에서는 시안의 실제 화면 구획을 기준으로 사진 기록을 `monthRail / libraryView / quickAlbums` 세 영역으로 분리했다. 왼쪽에는 사진이 존재하는 연도와 월, 가운데에는 보기 방식·정렬·날짜별 기록, 오른쪽에는 저장된 앨범 두 개와 전체 보기 진입을 배치했다. 우측 앨범 표지는 정적인 미리보기가 아니라 선택한 앨범의 전체 화면 리더를 바로 연다.

상단 제목은 고정된 '사진 기록' 대신 최신 촬영 월을 사용해 `2026년 9월` 형식으로 표시한다. 월 목록은 처음 진입할 때 기존 정렬·필터 기능을 보존하기 위해 전체 기록을 보여주고, 사용자가 월을 직접 선택한 뒤부터 해당 월만 필터링한다. 좁은 화면에서는 월 목록을 가로 스크롤로 바꾸고 우측 앨범 열은 숨겨 사진 폭을 확보한다.

인물 상세는 인물을 선택하면 목록이 사라지던 구조를 바꿔, 이름을 지정한 사람과 미확인 얼굴 진입을 좌측 디렉터리에 유지한다. 우측에는 이름 수정, 대표 사진 변경, 선택 작업과 4열 사진 목록을 배치했다. 모바일에서는 디렉터리를 상단 가로 목록으로 변환한다. 얼굴 모아보기는 인물 상세와 목적이 다르므로 기존 정사각형 작은 썸네일 규칙을 유지한다.

앨범 화면의 검색 입력은 실제 앨범 제목 필터에 연결했고 최근 수정순, 오래된 순, 이름순 정렬을 추가했다. 상단의 '새 앨범'은 빈 앨범을 만드는 대신 사진 기록의 선택 모드로 이동시켜 기존의 원본 사진 선택과 앨범 생성 흐름을 그대로 사용한다. 상세보기 상단은 사진 기록 복귀, 촬영일, 즐겨찾기, 확대, 정보·기록, 닫기 순서로 정리했다.

### 사진 수와 비율에 따라 변하는 날짜별 모자이크

초기 날짜별 그리드는 `grid-row: span 2`만 지정하고 행 높이를 자동 계산했다. 이 상태에서 첫 사진이 세로 사진이면 사진의 고유 높이가 행을 밀어 올려 대표 사진이 화면 높이보다 커지고, 오른쪽 사진도 같은 높이에 끌려가는 문제가 생겼다. 해결할 때 단순 최대 높이만 두면 사진 한 장도 지나치게 넓게 잘리므로 사진 수별 레이아웃을 분리했다.

- 1장: 중앙 영역의 2/3 너비와 약 3:2 비율
- 2장: 같은 크기의 좌우 2열
- 3장 이상: 대표 사진은 왼쪽 2/3에서 두 행, 다음 두 장은 오른쪽 1/3에서 위아래 배치
- 4장 이후: 아래쪽 3열 그리드로 계속 배치

모자이크는 6개의 동일한 열과 명시적인 `grid-auto-rows`를 사용한다. 따라서 원본의 세로·가로 비율이 CSS Grid의 행 높이를 바꾸지 않는다. 날짜 안에 가로 사진이 있으면 첫 번째 가로 사진을 대표 위치로 옮기고, 세로 사진은 상단이 잘리지 않도록 `object-position: center top`을 적용한다. 모바일은 대표 사진만 전체 폭으로 두고 나머지는 2열로 바꿔 세로 스크롤 길이를 제한한다.

### 대표 사진 3장과 앨범 리더 구조 교체

날짜별 기록에 사진이 많을 때 한 번에 모두 렌더링하면 첫 화면이 지나치게 길어지고 고해상도 썸네일 디코딩도 한꺼번에 발생한다. 각 날짜는 정렬된 대표 사진 세 장만 먼저 렌더링하고, 남은 수를 `사진 N장 더보기`로 표시하도록 바꿨다. 펼침 여부는 날짜 문자열을 담은 `Set`으로 관리하므로 한 날짜를 펼쳐도 다른 날짜의 상태는 바뀌지 않는다. 세 장 배치는 첫 사진을 왼쪽 2/3, 다음 두 장을 오른쪽 위아래로 명시해 자동 배치 규칙과 사진 원본 비율에 영향을 받지 않게 했다.

앨범 내부는 구형 `albumFullscreen`, `binderStage`, `binderPage` DOM과 관련 CSS를 제거하고 `albumJournal` 구조로 교체했다. 새 구조는 상단의 목록 복귀·제목·사진 수·전체화면 도구, 중앙 4:3 양면 펼침, 바깥쪽 이동 화살표, 하단 펼침 번호와 진행선으로 구성한다. 데이터 페이지 계산과 키보드 이동은 검증된 로직을 유지하되 보이는 책 구조는 새 클래스만 사용한다. 페이지 색은 아이보리로 통일하고 사용자가 선택한 앨범 색은 종이 바깥 하드커버 가장자리에만 반영한다.

회귀 테스트는 같은 날짜의 사진 일곱 장이 최초 세 장만 렌더링되는지, 더보기 뒤 일곱 장으로 늘어나는지 확인한다. 대표 사진 너비가 보조 사진의 약 두 배인지와 두 보조 사진이 같은 열의 위아래에 놓이는지도 좌표로 검증한다. 앨범 테스트는 새 펼침면이 4:3인지, 양면 두 장과 사진 네 장이 존재하는지, 페이지 전환 중 진행 단계가 순서대로 바뀌는지를 확인한다.

### 이미지와 CSS를 결합한 앨범 두께 표현

CSS의 사각형과 그림자만으로 만든 펼침면은 종이 면은 표현할 수 있어도, 여러 장의 종이 단면과 하드커버가 페이지 뒤를 받치는 관계가 평면적으로 보였다. 이를 해결하기 위해 사진과 글자가 없는 빈 양면 앨범을 투명 PNG로 생성해 `src/assets/album-open-base.png`에 추가했다. 이 이미지는 뒤쪽 하드커버, 좌우와 하단의 겹친 종이 단면, 중앙 제본 굴곡, 바닥 접촉 그림자만 담당한다.

실제 사용자 사진과 페이지 번호는 이미지에 합성하지 않고 기존 React 요소로 유지한다. `albumBookBase`를 가장 아래에 놓고, 이미지의 빈 페이지 좌표에 맞춘 투명 `albumPaper` 두 장을 그 위에 절대 배치했다. 따라서 사진 상세 열기, 페이지 순서, 키보드 이동과 넘김 애니메이션은 그대로 동작하면서도 책의 물성은 래스터 이미지가 담당한다. 앨범 색상은 생성 이미지 전체를 변색시키지 않고 이미지 뒤의 얇은 하드커버 레이어에 적용해 가장자리에서만 드러나게 했다.

이미지의 투명 캔버스와 실제 책 경계는 일치하지 않으므로 백커버와 페이지 하단 좌표를 캔버스 끝에 맞추면 큰 색상 판이 노출된다. 캡처를 기준으로 책의 실제 경계인 위 13.5%, 아래 14.2%에 백커버를 제한하고, 사진 페이지와 넘김 면의 하단도 15%로 맞췄다. 이 좌표를 함께 관리해야 생성 이미지의 종이 두께와 인터랙션 레이어가 분리되어 보이지 않는다.

### 앨범 화면 크기와 사진 기록의 비율 조정

앨범 PNG의 투명 여백까지 화면 맞춤 계산에 포함되어 실제 책이 작게 보였다. 내부 이미지 캔버스의 4:3 비율은 유지하면서, 보이는 영역은 책이 차지하는 세로 72.3%로 제한했다. 해당 영역의 가로세로 비율로 가용 폭과 높이를 계산해 책을 확대하며 사진, 제본, 넘김 레이어가 함께 확대되도록 했다. 사진 안쪽 여백도 줄이고 왼쪽 위 사진과 오른쪽 위 사진의 높이를 다르게 배분했다.

앨범 리더는 상단 로고와 메뉴 아래에서 시작한다. App에서 ResizeObserver로 실제 메뉴 높이를 측정해 CSS 변수로 전달하므로 모바일에서 메뉴가 두 줄이 되어도 겹치지 않는다. 메뉴가 계속 사용 가능하므로 리더는 비모달 대화상자로 표시하며, 메뉴 클릭으로 앨범에서 다른 화면으로 이동하는 동작도 검증한다.

사진 기록의 대표 모자이크는 뷰포트 너비로 행 높이를 정하던 규칙 대신 자체 7:3 비율을 사용한다. 첫 날짜의 좌우 폭은 1.8:1이고 이후 날짜는 사진 세 장을 같은 크기로 배치한다. 더보기를 펼치면 일정한 크기의 3열 목록으로 전환한다. 일반 사진의 object-position은 상단 20%, 세로 사진은 맨 위를 기준으로 설정했다. 상세보기는 댓글을 기본으로 접어 원본 비율 사진의 공간을 확보하고 정보·기록 버튼으로 댓글을 연다.

페이지 넘김 회귀 테스트는 캡처 소요 시간이 820ms 애니메이션을 넘길 수 있어 중간 단계 검증이 불안정했다. Playwright clock으로 애플리케이션 타이머를 고정하고 300ms 데이터 교체와 820ms 완료 시점으로 직접 진행해, 캡처 속도와 무관하게 전환 단계를 검증하도록 수정했다.

### 사진 기록 크기 제한과 흰색 앨범 뒷판

사진 기록은 넓은 창에서 중앙 열과 이미지 높이가 계속 커지지 않도록 본문 전체 폭을 최대 1440px로 제한했다. 첫 날짜의 대표 모자이크는 최대 330px, 이후 날짜 및 더보기 목록은 한 행 최대 180px로 줄였다. 대표 사진 세 장과 더보기 동작, 상단을 우선 보여주는 크롭은 유지한다. 우측 앨범은 폭 196px를 유지하면서 비율을 4:5에서 2:3으로 바꿔 높이를 245px에서 294px로 늘렸고 사진창도 세로로 확장했다. 모바일의 전체 폭 대표사진과 2열 보조사진 구조는 유지한다.

녹색이 포함된 뒷판은 CSS 색상만으로 흰색이 되지 않아 내장 image_gen 편집 도구로 `src/assets/album-open-white.png`를 만들었다. 기존 PNG는 보존하며 앨범 리더만 새 이미지를 참조한다. 사진 색을 바꾸는 필터는 사용하지 않고 이미지 뒤쪽 CSS 레이어도 흰색으로 통일했다. 외부 앨범 표지의 색상 선택과 저장은 그대로 유지한다. 새 이미지의 실제 책 범위인 세로 13.5~88%에 맞춰 표시 범위를 조정해 하단 뒷판이 잘리지 않게 했다.

이미지 편집 프롬프트 (내장 도구, CLI 사용 안 함):
> Use case: precise-object-edit. Edit target: the provided transparent PNG of an open photo album, used as a UI background with separately overlaid photos. Change ONLY the green/gray cloth hardback backing board surrounding and supporting the pages to WHITE bookbinding cloth, like a white wedding photo album. White material with subtle light-gray shadows, not green, beige, or dark gray. Preserve realistic thickness, layered paper edges, cloth texture, center crease, natural shadows. Keep blank warm-white pages unchanged. Crucially preserve the exact framing, book outline, orthographic front/top view, geometry, book placement, margins and 4:3 canvas aspect ratio from the original because interactive photos are positioned over this asset. No photographs, text, rings, new objects or added background. Keep a truly transparent alpha background and shadow around the book. Output a single PNG suitable as a drop-in replacement.

Playwright 검증에는 2191px 창에서도 대표 모자이크 높이가 330px를 넘지 않는지, 우측 앨범의 높이/폭 비율이 1.5인지, 흰색 뒷판 이미지와 CSS 레이어가 적용되는지 확인을 추가했다. 데스크톱과 모바일에서 더보기, 앨범 열기 및 표지 색상 저장 흐름을 함께 검증한다.

### 흰색 반광 하드커버 소재 조정

기존 흰색 뒷판은 거친 직물 결, 누런 종이 음영, 짙은 갈색 그림자로 인해 오래된 책처럼 보였다. 내장 이미지 편집 도구로 뒷판을 매끈한 흰색 반광 코팅 소재로 바꾸고 `src/assets/album-open-white-gloss.png`로 별도 저장했다. 편집 지시는 "기존 구도와 투명 배경, 책 두께는 유지하되 거친 천 결과 낡은 색감을 없애고, 밝은 흰색 코팅 표지에 은은한 가장자리 반사광을 추가한다"이다. 이전 이미지는 보존하며 앨범 리더만 새 이미지를 사용한다.

CSS의 갈색 계열 그림자도 옅은 중성 회색으로 바꾸고 넘김 면을 밝은 흰색으로 맞췄다. 생성 이미지의 책 바깥 잔여 픽셀은 이미지 레이어의 clip-path로 제한한다. 필터를 사진에 적용하지 않으므로 사용자 사진의 색감은 변하지 않으며, 페이지 배치와 넘김 타이밍도 유지한다. 앨범 테스트는 새 이미지 경로와 실제 이미지 디코딩을 확인하도록 갱신했다.

검증 중 책 캔버스의 투명한 상단 여백이 뒤로 버튼을 가로막는 문제를 확인했다. overflow를 잘라 그림자를 없애는 대신 장식용 albumHardback은 pointer-events: none, 실제 페이지인 albumPaper는 pointer-events: auto로 분리해 책 두께 표현을 유지하면서 상단 도구와 사진 클릭이 모두 동작하도록 수정했다.

### 얇은 뒷표지와 정면 앨범 표지

펼친 앨범은 흰색 반광 소재를 유지하면서 페이지 바깥으로 드러나는 뒷판 폭과 두께를 줄인 `album-open-white-thin.png`로 교체했다. 이미지 편집 지시는 "기존 빈 양면 페이지와 배치를 유지하고, 측면과 하단으로 돌출되는 흰색 뒷판을 약 70% 줄여 얇은 테두리만 남긴다"이다. 이미지 아래에 겹치던 CSS 뒷판도 제거했다. 이미지가 얇아져도 더 큰 CSS 판이 남으면 여전히 두껍게 보이기 때문이다. 페이지 번호는 종이 단면이 아닌 안쪽 페이지에 놓고, 사진 배치와 넘김 로직은 유지했다.

목록 표지는 CSS의 기울기만 없애는 것으로 해결되지 않았다. 기존 비트맵 자체에 원근과 책 옆면이 포함되어 있어, "완전한 정면 직교 시점, 8:9 비율, 얇은 왼쪽 제본 홈, 흰색 반광 표지, 옆면과 아래쪽 종이 단면 없음, 사진과 글자 없음, 투명 배경"으로 `album-front-flat.png`를 생성했다. 비트맵의 바깥 투명 여백만 잘라 실제 사진과 제목을 수평으로 배치했다. 공용 AlbumCover를 사용하므로 앨범 목록과 사진 기록 우측 앨범에 함께 적용되며 기존 색상 저장도 유지된다.

앨범 더보기 메뉴는 참고 이미지처럼 표지 오른쪽 아래에 배치했다. 표지를 여는 버튼 안에 메뉴 버튼을 중첩하지 않고, 같은 위치 기준 요소 안의 형제 요소로 분리해 편집과 앨범 열기가 충돌하지 않도록 했다. 제목에는 메뉴 영역을 제외한 너비와 두 줄 제한을 적용했다.

### 인물 디렉터리의 미확인 얼굴 보기

기존에는 미확인 얼굴을 누르면 활성 인물만 해제되어 등록된 인물 전체 화면으로 돌아갔다. 보기 상태를 `people / all / unknown`으로 구분하고, 이름이 비어 있는 인물 그룹의 얼굴만 모은 전용 목록을 추가했다. 확인 여부 플래그가 아니라 실제 이름 등록 여부로 분류하며, 이미 제외한 얼굴은 서비스가 반환하는 색인에서 빠져 있으므로 다시 노출하지 않는다.

미확인 얼굴을 보고 있을 때도 왼쪽 인물 디렉터리를 유지하고 활성 항목과 얼굴 수를 표시한다. 인물과 보기 모드가 바뀔 때 선택, 이동 대상, 대표 사진 변경 모드, 페이지를 초기화한다. 얼굴 선택, 상세보기, 인물 이동, 제외와 되돌리기는 기존 저장 함수를 재사용한다. 회귀 테스트는 이름 있는 얼굴과 제외한 얼굴이 섞이지 않는지, 24개 단위 페이지 이동, 이동 후 수량 갱신, 모두 제외했을 때 빈 상태, 되돌리기 및 인물 전환을 데스크톱과 모바일에서 검증한다.

### 흰색 목록 표지와 사진 방향별 앨범 배치

목록과 사진 기록 우측 앨범은 저장된 색상과 관계없이 흰색 정면 표지를 표시한다. AlbumCover의 색상 레이어는 색상 선택기의 미리보기에서만 명시적으로 켜고 기존 저장값은 삭제하지 않았다. 목록 표지 비율은 8:9에서 4:5로 변경해 같은 너비에서 세로 길이를 약 11% 늘렸다. 펼친 책의 표시 비율은 약 1.79:1에서 1.95:1로 넓혔으며, 사진과 종이 레이어는 같은 좌표계를 사용한다.

기존 네 장 단위 분할은 세로 사진도 가로 칸에 넣게 되므로 `makeAlbumSpreads`에서 먼저 종이 한 면씩 구성하고 두 면을 한 펼침으로 묶는다. 세로 사진은 한 면을 혼자 사용하고, 이어지는 가로 사진은 최대 두 장까지 같은 면을 사용한다. 가로 사진 다음에 세로 사진이 오면 가로 사진을 혼자 남기더라도 원래 순서를 유지한다. 마지막 홀수 면은 반대쪽을 빈 페이지로 둔다.

세로 판정은 유효한 가로·세로 메타데이터를 사용하며 정사각형, 크기 누락, 잘못된 크기는 일반 배치로 처리한다. 세로 사진의 버튼은 원본 비율과 페이지 높이에 맞추고 object-fit: contain을 사용해 상하를 자르거나 이미지를 늘리지 않는다. 페이지 넘김, 슬라이더, 순서 섞기, 상세 사진 열기는 새 펼침 수를 기준으로 동작한다.

검증: 단위 테스트 21개 및 앨범 Playwright 테스트 4개(데스크톱·모바일) 통과. 혼합 방향 순서, 모든 사진이 세로인 앨범, 홀수 마지막 면, 빈 앨범, 크기 누락, 페이지 넘김 중 데이터 교체, 키보드·슬라이더 이동, 사진 상세와 목록 전환, 색상 데이터 보존 및 흰색 목록 표지를 확인했다.

### 넓은 책 비율과 책 가장자리 이동 버튼

펼친 책의 비율을 1.95:1에서 2.4:1로 넓혔다. 기존 화면 전체 3열 그리드에 놓이던 좌우 이동 버튼을 책과 같은 `albumBookStage` 안으로 옮기고, 실제 책 가장자리에서 6px 떨어진 위치와 책 높이의 중앙을 기준으로 배치했다. 창 높이 때문에 책 너비가 줄어들어도 버튼은 책과 함께 이동한다.

배경 PNG의 좌우 투명 여백은 각각 2.7%이므로 이미지 캔버스와 책의 실제 경계를 그대로 동일시하면 버튼 사이 간격이 넓어진다. 장식용 캔버스를 좌우로 약 2.8541% 확장해 보이는 책 경계를 stage 너비와 맞췄다. 버튼은 책의 필터·원근 효과 밖에 두어 페이지 넘김과 무관하게 클릭 영역을 유지한다. 빈 앨범에서는 책과 좌우 버튼 대신 기존 빈 상태를 표시한다.

검증: 데스크톱·모바일 앨범 테스트 4개 통과. 1440×1000, 2560×900, 1080×720 및 모바일 크기에서 책 비율, 책과 버튼의 6px 간격, 수직 중앙 정렬, 화면 안 클릭 영역, 헤더·하단 도구와의 비겹침을 검사했다. 좌우 버튼 이동과 기존 세로 사진 단독 배치도 확인했다.

### 사진 원본 비율과 촬영 시기 탐색

앨범의 가로 사진도 object-fit: contain으로 표시해 넓은 책 칸에 맞추면서 원본을 자르지 않는다. 두 사진 영역은 같은 높이를 사용하고 남는 부분은 투명하게 처리해 종이 여백으로 보이게 했다. 마우스 강조는 빈 칸 전체의 그림자 대신 실제 사진에만 적용하며, 세로 사진의 한 면 단독 배치는 유지한다.

사진 기록 왼쪽 탐색을 PhotoDateNavigation으로 분리했다. 모든 기록, 즐겨찾기, 접을 수 있는 연도별 월 목록과 실제 기록 수, 하단 날짜 없음으로 구성한다. 선택한 월에는 왼쪽 강조선을 표시하며 좁은 화면에서는 가로로 스크롤할 수 있다. 즐겨찾기는 실제 favorite 값으로 필터링하고, 검색 후에도 즐겨찾기 범위를 유지하며 상세 화면에서 해제하면 목록에 즉시 반영한다.

검증: 단위 테스트 21개 통과. 데스크톱·모바일에서 새 탐색 동작과 혼합 방향 앨범 회귀 테스트 4개 통과. 화면 캡처로 원본 비율과 여백, 탐색 배치를 확인했다.

### 참고 이미지에 맞춘 사진 배치 보정

원본 전체를 표시하니 가로 사진이 페이지 중앙에 작게 모이는 문제가 있었다. 사용자 피드백에 따라 가로 사진은 페이지 안쪽 너비를 채우는 cover 방식으로 변경했다. 왼쪽 면은 위아래 높이를 1.65:1, 오른쪽 면은 1.35:1로 두어 위쪽 사진을 더 크게 표시하고, 크롭 위치는 세로 35%로 맞췄다. 세로 사진은 한 면 단독 배치와 원본 비율을 유지하며 책 크기, 페이지 넘김, 왼쪽 촬영 시기 메뉴는 변경하지 않는다.

### 앨범과 인물 묶음 내보내기

앨범 카드와 앨범 상세 메뉴, 인물별 상세 화면에 내보내기를 추가했다. 공용 모달에서 기본 폴더명을 수정하고 상위 경로를 선택하면 그 아래에 새 디렉터리를 만든 뒤 묶음에 포함된 원본 파일을 복사한다. 인물은 얼굴 검출 수가 아니라 고유한 미디어 ID로 파일을 모아 같은 사진을 중복 복사하지 않는다.

Rust 명령은 원본을 이동하거나 수정하지 않으며, 같은 파일명이 있으면 `파일명 (2).확장자`처럼 구분한다. 기존 폴더를 덮어쓰지 않고 Windows 예약 이름과 경로 문자를 거부한다. 복사 도중 실패하면 이번 작업에서 새로 만든 디렉터리만 정리하고 오류를 모달에 표시한다.

검증: Rust 단위 테스트 13개, 데스크톱·모바일 내보내기 UI 테스트 2개 통과. 앨범과 인물 묶음의 폴더명, 경로, 중복 제거된 원본 경로 전달과 완료 화면을 확인했다.

### 참고 이미지에 맞춘 달력과 날짜 상세창

달력 화면은 가운데 제한된 너비의 본문, 연월 선택과 이동 도구, 월간 달력/사진 있는 날 탭 순서로 정리했다. 날짜 칸은 위쪽 날짜, 가로 대표사진, 아래쪽 사진 수로 구성하며 마지막 주의 빈 칸도 채운다. 오늘 이동과 일요일/토요일 색상 표시를 추가했고, 기존 일정은 사진 위쪽에 유지한다.

날짜 상세창은 날짜와 기록 수, 큰 사진과 좌우 이동 버튼, 가로 썸네일 목록, 그날의 메모, 대표사진 설정 순서로 변경했다. 썸네일 목록은 가로 스크롤과 이동 버튼을 지원하며 선택 사진이 화면에 들어오도록 조정한다. 메모는 기존 저장 키를 유지하면서 명시적인 메모 저장 버튼으로 저장한다. 날짜별 대표사진 ID는 `oraedameun.dayCovers`에 저장하며 사진이 삭제되면 해당 날짜의 첫 사진으로 대체한다. 저장 실패는 성공으로 표시하지 않고 오류를 알린다.

메모와 일정 갱신 때 새 items 배열 때문에 선택 사진이 첫 장으로 돌아가던 효과를 제거했다. 날짜별 모달 키와 사진 ID로 선택을 유지한다. 사진이 없는 날에도 메모와 일정은 사용할 수 있다. 스타일은 calendar.css로 분리해 앨범과 사진 기록 그리드에는 영향을 주지 않는다.

검증: 데스크톱·모바일 Playwright 10개 통과. 150장 사진의 이동, 썸네일 스크롤, 대표사진과 메모의 재접속 유지, 저장 공간 오류, 삭제된 대표사진 대체, 빈 날짜, 윤년과 주 완성, 오늘 이동, 기존 반복 일정 등록·수정·삭제를 확인했다. 참고 이미지와 같은 1694x928 및 모바일 화면 캡처로 배치도 확인했다. 내장 Browser 연결 오류로 기존 Playwright 실행 환경을 사용했다.

### Windows Rust 툴체인 고정 (2026-09-24)

`dlltool.exe: program not found`는 프로젝트의 `channel = "stable"`이 PC 기본값인 GNU 툴체인으로 해석되어 발생했다. 설치된 MSVC Rust와 Visual Studio C++ Build Tools를 사용하도록 `src-tauri/rust-toolchain.toml`의 채널을 `stable-x86_64-pc-windows-msvc`로 명시했다. PC 전체의 Rust 기본값과 사용자가 수정한 Cargo.toml은 변경하지 않았다.

검증: src-tauri 디렉터리에서 MSVC 활성화를 확인하고 `cargo check --locked --offline` 통과. 기존 unused_mut 경고 1개만 남아 있으며 실제 앱 실행과 설치 패키지 생성은 이 검사에 포함하지 않았다. 프로젝트 루트에서 `npm run tauri:dev`로 다시 실행한다.

### 앨범 펼침의 랜덤 장수와 잘림 없는 사진 배치 (2026-09-24)

앨범을 열면 목록 대신 책이 먼저 보인다. 수동 배치 탭 없이 양쪽 면을 합친 한 펼침에 1~3장을 자동으로 배치한다. 세로 사진은 한 면을 단독으로 사용하며, 세 장 배치는 인접한 가로 또는 정사각 사진 두 장을 한 면에 넣을 수 있을 때만 선택한다. 사진 순서는 저장된 순서를 유지하고 마지막 사진까지 중복이나 누락 없이 이어진다. 한 장인 펼침은 오른쪽 면을 빈 종이로 둔다.

앨범을 열 때 생성한 시드로 장수를 결정하므로 같은 앨범을 보고 있는 동안 앞뒤 이동, 목록 전환, 댓글·별점·즐겨찾기 변경으로 구성이 다시 섞이지 않는다. 명시적인 사진 순서 섞기와 원래 순서 복원은 기존 메뉴를 유지한다.

책 비율은 새 시안에 맞춰 2.4:1에서 2.2:1로 조정했다. 각 사진의 실제 가로·세로 메타데이터와 설명·날짜 높이를 합산하여 페이지 내부에 들어가는 공통 사진 너비를 계산한다. 고정된 위아래 높이 비율을 제거하고 object-fit: contain을 유지해 이미지를 자르거나 늘리지 않는다. 크기를 알 수 없는 사진은 안전한 기본 틀 안에 전체 이미지를 표시한다. 사진마다 설명과 날짜를 가운데 정렬하고 좁은 사진의 설명은 종이 여백까지 사용할 수 있게 했다. 사진과 설명은 함께 페이지 넘김 애니메이션을 적용한다. 흰색 책 비트맵, 책 가장자리 이동 버튼, 클릭해서 사진 상세보기는 유지한다.

검증: 단위 테스트 23개, 관련 Playwright 테스트 10개(데스크톱·모바일), 프로덕션 빌드 통과. 세로·가로 혼합, 1~3장 분포, 저장 순서 보존, 빈 앨범과 한 장 앨범, 파노라마·정사각·크기 누락, 긴 설명, 책과 버튼의 위치, 상세보기·목록 전환·내보내기를 확인했다. 화면 캡처로 잘림 없는 배치와 모바일 날짜 표시도 확인했다. 내장 브라우저 연결 오류로 프로젝트의 Playwright 환경을 사용했으며 실제 Tauri 앱 실행 검증은 포함하지 않았다.

### 미사용 코드 정리와 기능별 함수 분리 (2026-09-24)

사진 목록, 사진 상세·확대, 추억, 설정 화면을 기능별 컴포넌트로 분리해 App.tsx를 1,146줄에서 588줄로 줄였다. 정렬·필터·검색·지난 추억은 collectionModel, 댓글 저장은 mediaComments, 달력 계산·일정 인덱스·저장은 calendarModel로 옮겼다. 인물·반려동물 조회도 각각 peopleModel, petModel로 분리했다. 분리 자체가 아니라 반복 계산을 줄이는 부분을 실제 최적화 대상으로 삼았다.

정렬 비교에 쓰는 Intl.Collator를 재사용하고, 인물마다 전체 얼굴 목록을 훑던 동작은 Map/Set 인덱스로 바꿨다. 달력의 반복 일정도 날짜별 인덱스로 조회한다. 대규모 사진 목록을 Math.min/Math.max 인자로 전개하지 않도록 수정해 20만 건 입력을 테스트했다. Rust의 미디어 행 변환을 공통 함수로 분리하고 앨범마다 사진 쿼리를 실행하던 구조를 두 번의 일괄 조회로 바꿨다. 빈 앨범과 저장된 사진 순서, 최신 댓글·조회 수는 유지된다.

참조되지 않는 옛 앨범·배경 PNG 6개와 폰트·라이선스를 제거했다. 총 15,845,742바이트이며 현재 흰색 책 이미지와 인식 모델은 유지한다. 옛 날짜 상세창·타임라인 등의 미사용 CSS와 더 이상 호출하지 않는 고정 장수 배치 함수를 정리했다. 빌드 결과물은 약 63.7MB에서 54.5MB로 감소했다. 기존 사용자 데이터와 저장 키, 데이터베이스 스키마는 변경하지 않았다.

변경 전 전체 화면 테스트의 실패 16개는 예전 버튼명·고정 페이지 수 등의 기대값을 현재 동작에 맞춰 정비했다. Tauri 테스트 응답도 실제 IPC처럼 새 스냅샷을 반환하도록 수정했다. 검증 결과: 단위 테스트 35개, Playwright 74개 통과(기존 조건으로 4개 건너뜀), Rust custom-protocol 테스트 17개, SQLite 저장소 테스트 5개, 프로덕션 빌드 통과. 기존 인식 라이브러리의 큰 청크 경고는 남아 있다. 설치 패키지와 실제 사용자 라이브러리 검증은 포함하지 않았다.

전체 기능의 모든 핸들러 분리가 끝난 것은 아니다. 남은 App 상태 처리, 달력 모달 분리, 사용 중인 CSS 중복 정리 등 후속 범위와 검증 명령은 docs/code-cleanup.md에 기록했다.
