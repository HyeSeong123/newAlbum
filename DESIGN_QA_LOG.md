# 디자인 QA 학습 로그

이 문서는 `오래담은` 화면을 제품 디자이너와 QA 관점에서 검토한 기록입니다. 단순 수정 목록이 아니라, 다음 프로젝트에서도 다시 쓸 수 있는 디자인 판단 기준을 남깁니다.

## 디자인 목표

`오래담은`은 사진 관리 도구이지만, 사용자가 느끼는 첫인상은 “파일 관리자”보다 “내 추억을 편하게 꺼내보는 앨범”에 가까워야 합니다.

그래서 화면 원칙은 다음과 같습니다.

- 기본 언어는 한국어로 한다.
- 개발 용어를 화면에 노출하지 않는다.
- 사용자가 바로 사진을 등록하고 볼 수 있게 한다.
- 한국 정서는 과한 장식보다 차분한 색, 여백, 오래된 앨범의 물성으로 표현한다.
- 메뉴는 기능 이름보다 사용자 행동을 기준으로 정한다.

## 디자인 시스템 적용 메모

새 디자인 방향은 따뜻한 크림 배경, 포근한 브라운 포인트, 둥근 카드, 명조 제목을 중심으로 잡았습니다.

적용한 규칙:

- 제목, 앨범명, 날짜는 `ChosunMyeongjo`를 사용한다.
- 본문, 버튼, 메뉴는 `SUIT`를 사용한다.
- 배경은 `#FDFBF7`, 표면은 `#FFFFFF`로 둔다.
- 주요 포인트는 `#B88E68`, 진한 강조는 `#735944`를 사용한다.
- 카드와 컨테이너는 `20px` radius를 기본으로 한다.
- 버튼은 `9999px` radius의 pill 형태로 만든다.
- 사진 프레임은 `1:1` 비율로 맞춘다.
- 그림자는 `rgba(115, 89, 68, 0.06)` 계열로 부드럽게 둔다.

검사 결과:

- 데스크톱에서는 목록 영역과 등록 영역이 충분히 넓게 보인다.
- 모바일에서는 명조 헤드라인이 크지만 겹침 없이 읽힌다.
- 버튼 터치 영역은 충분히 크다.
- 샘플이 없는 빈 상태에서도 화면이 깨지지 않는다.

## 레퍼런스 기반 레이아웃 재구성 메모

참고한 글의 핵심은 “화면을 한 번에 예쁘게 만드는 것”보다 실제 실행 화면을 보고 위계, 간격, 반응형, 클릭 흐름을 반복 검증하는 방식입니다.

이번 변경에서는 그 관점을 현재 앱에 맞춰 적용했습니다.

- 상단을 `소개 문구만 있는 영역`에서 `등록 행동 + 앨범 현황` 영역으로 바꿨다.
- 사용자가 가장 먼저 해야 하는 `파일 선택`, `폴더 선택`을 왼쪽 큰 영역에 두었다.
- 전체 기록, 사진, 영상, 즐겨찾기 수를 오른쪽 요약 카드로 분리했다.
- 아래의 `사진보기` 패널은 실제 작업 공간처럼 더 독립적으로 보이게 했다.
- 모바일에서는 메뉴가 두 줄로 내려와 화면을 많이 차지하지 않도록 가로 스크롤 구조로 바꿨다.

검사 기준:

- 첫 화면에서 다음 행동이 바로 보이는가
- 등록 영역과 사진 보기 영역이 서로 경쟁하지 않는가
- 현황 정보가 기능 설명처럼 길지 않고 숫자로 빠르게 읽히는가
- 모바일에서 메뉴가 사진 보기 흐름을 밀어내지 않는가
- 빈 상태에서도 화면이 고장처럼 보이지 않는가

학습 포인트:

- 좋은 레이아웃은 장식보다 정보의 우선순위를 정리하는 일에 가깝다.
- 사용자가 자주 하는 행동은 크게, 상태 확인은 작고 빠르게 읽히게 둔다.
- 실제 화면 캡처를 보면 코드만 볼 때 놓치는 모바일 높이 문제를 쉽게 발견할 수 있다.

## 사용자가 볼 필요 없는 것

화면에서 제거하거나 숨긴 것:

- `Phase 1`
- `MVP`
- `DB`
- `metadata`
- `등록 대기열`
- A/B 테스트 토글
- 내부 구현 설명
- CSS 클래스명 같은 개발자 용어

이런 정보는 개발자에게는 중요하지만, 사용자가 사진을 보는 데는 방해가 됩니다. 대신 학습 로그와 구현 문서에 남깁니다.

## 메뉴 설계

처음에는 달력과 앨범이 별도 메뉴처럼 보였습니다. 하지만 둘 다 “사진을 보는 방식”입니다.

그래서 사이드 메뉴는 `사진보기`로 통합하고, 내부 탭으로 나눴습니다.

- `모아보기`: 전체 사진을 한 번에 훑어보기
- `달력보기`: 날짜 기준으로 찾아보기
- `앨범보기`: 실제 앨범처럼 넘겨보기

이 구조가 더 자연스러운 이유는 사용자가 “달력 기능을 쓰러 간다”기보다 “사진을 날짜별로 보고 싶다”고 생각하기 때문입니다.

## 달력보기 QA

문제:

- 현재 월 중심 달력만 있으면 오래된 사진을 찾기 어렵다.
- 이전/다음 버튼만 있으면 2020년, 2018년 같은 과거로 가기 번거롭다.

수정:

- 연도 셀렉트 박스를 추가했다.
- 월 셀렉트 박스를 추가했다.
- 이전 달/다음 달 버튼은 빠른 이동용으로 유지했다.

검사 기준:

- 과거 연월을 직접 선택할 수 있는가
- 날짜 칸의 사진 수가 읽히는가
- 모바일에서 셀렉트와 버튼이 눌릴 만큼 충분히 큰가
- 달력 칸 간격이 너무 빽빽하지 않은가

## 앨범보기 QA

문제:

- 카드 목록만으로는 실제 앨범을 보는 감각이 약하다.
- 모든 페이지가 같은 개수의 사진이면 손으로 만든 앨범 느낌이 줄어든다.
- 일반 화면 안의 작은 책장은 실제 앨범을 펼친다는 느낌이 약하다.

수정:

- 좌우 책장 형태의 펼친 앨범 레이아웃을 만들었다.
- 한쪽 페이지마다 사진을 위아래 2장까지 정방향으로 놓이게 구성했다.
- 이전/다음 버튼에 책장 넘김 애니메이션을 추가했다.
- `전체창으로 보기`를 추가해 앨범 감상 중에는 주변 UI보다 책장이 중심이 되게 했다.
- 콜라주식 기울기와 불규칙 배치를 제거해 사진을 더 안정적으로 감상하게 했다.
- 폴라로이드처럼 보이는 두꺼운 여백과 캡션을 줄이고, 일반 인화사진을 붙인 느낌으로 바꿨다.

검사 기준:

- 현재 페이지와 전체 페이지 위치를 알 수 있는가
- 이전/다음 버튼이 명확한가
- 사진이 1장일 때도 비어 보이지 않는가
- 사진이 4장일 때도 답답하지 않은가
- 모바일에서 책장 구조가 깨지지 않는가
- 전체창을 닫는 방법이 명확한가
- 사진이 정방향으로 보이고 클릭을 방해하지 않는가
- 사진 프레임보다 실제 사진 면적이 먼저 보이는가
- 사진은 `4px` 정도만 둥글고, `2px` 흰 테두리로 인화사진 느낌이 나는가
- 다음장으로 넘길 때 페이지가 잠깐 사라져 보이지 않는가
- 하단과 중앙 접힘에서 앨범의 종이 두께감이 느껴지는가
- 하단 페이징이 앨범 그림자나 접힘에 가리지 않는가
- 좌우 페이지의 사진 묶음이 위아래 중앙에 맞는가

## 한국 정서 적용 기준

사용하지 않는 방향:

- 과한 궁궐/전통 문양 장식
- 외국 잡지 같은 scrapbook 분위기
- 영어 메뉴 중심 화면
- 설명이 많은 SaaS 랜딩페이지 구성

사용한 방향:

- 따뜻한 크림색과 차분한 갈색 계열의 배경
- 차분한 갈색, 먹색, 종이색 계열을 보조로 사용
- `오늘 남겨둘 순간`, `지난 추억`처럼 한국어 문장 톤 사용
- 기능보다 감상 경험이 먼저 보이도록 구성

## 화면 QA 체크리스트

- 시각적 위계: 가장 중요한 등록/보기 흐름이 먼저 보이는가
- 간격: 카드, 버튼, 탭 사이가 답답하지 않은가
- 대비: 배경 위 텍스트가 충분히 읽히는가
- 텍스트 가독성: 긴 한국어 문장이 버튼이나 카드 밖으로 밀리지 않는가
- 반응형 레이아웃: 모바일에서도 메뉴와 탭이 사용 가능한가
- hover 상태: 클릭 가능한 요소가 마우스 오버 시 반응하는가
- 모바일 사용성: 손가락으로 누를 영역이 충분한가
- 클릭 가능한 요소의 명확성: 버튼과 정보 텍스트가 구분되는가
- 사용자 흐름의 자연스러움: 등록 후 바로 사진을 볼 수 있는가

## 등록 중 상태 QA

문제:

- 큰 폴더를 등록할 때 화면 변화가 없으면 사용자는 앱이 멈췄다고 느낄 수 있다.
- 같은 버튼을 여러 번 누르면 중복 등록 요청이 생길 수 있다.

수정:

- 등록 중에는 버튼 안의 아이콘을 회전 로딩 아이콘으로 바꿨다.
- `파일 등록 중`, `폴더 등록 중`처럼 현재 작업을 짧게 보여준다.
- 등록 중에는 파일/폴더 선택 버튼을 비활성화한다.
- 상태 문구를 별도로 보여줘 큰 폴더를 살펴보는 중임을 알 수 있게 했다.

검사 기준:

- 등록 중인 버튼이 명확히 구분되는가
- 사용자가 기다려야 한다는 것을 알 수 있는가
- 버튼 비활성화 상태가 고장처럼 보이지 않는가
- 모바일에서 상태 문구가 버튼과 겹치지 않는가

## 실제 썸네일과 클릭 액션 QA

문제:

- 실제 사진을 등록해도 카드가 그라디언트로만 보여 사진 앱처럼 느껴지지 않았다.
- 앨범보기의 사진을 눌러도 오른쪽 감상 패널이 바뀌지 않아 클릭 가능한 요소처럼 느껴지지 않았다.

수정:

- 이미지 파일은 Tauri의 로컬 파일 URL로 변환해 실제 `<img>`로 보여주도록 했다.
- 모아보기, 시간순, 달력보기, 앨범보기, 감상 패널에서 같은 이미지 표시 방식을 사용하게 했다.
- 앨범보기의 각 사진도 클릭하면 감상 패널의 선택 항목이 바뀌도록 연결했다.

검사 기준:

- 실제 등록한 사진이 카드와 감상 패널에 보이는가
- 앨범 속 사진을 눌렀을 때 선택 항목이 바뀌는가
- 영상/음원처럼 아직 썸네일이 없는 항목도 아이콘으로 구분되는가

## 상세 모달 QA

문제:

- 오른쪽 감상 패널이 항상 떠 있어 사진 목록과 앨범 화면이 좁아졌다.
- 별점, 즐겨찾기, 코멘트는 모든 순간에 필요한 정보가 아니라 사진을 선택했을 때 필요한 정보다.

수정:

- 오른쪽 고정 감상 패널을 제거했다.
- 목록 영역을 전체 폭으로 넓혔다.
- 사진/영상 클릭 시 상세 모달을 띄워 큰 미리보기와 편집 기능을 보여준다.
- 닫기 버튼, 바깥 영역 클릭, `Esc` 키 닫기를 지원한다.

검사 기준:

- 기본 화면에서 사진 목록이 충분히 넓게 보이는가
- 클릭 가능한 사진 카드가 명확한가
- 모달에서 사진, 별점, 코멘트, 즐겨찾기가 한 흐름으로 보이는가
- 모바일에서 모달이 화면 밖으로 넘치지 않는가

## 다중선택 QA

문제:

- 여러 장을 한 번에 삭제하거나 앨범으로 묶는 흐름이 없었다.
- 기본 클릭이 상세보기인지 선택인지 헷갈리면 사용자가 실수하기 쉽다.

수정:

- 우측 상단에 `선택` 버튼을 추가했다.
- 선택 모드에서만 다중선택이 가능하게 했다.
- 모아보기에서는 클릭 선택과 드래그 선택을 모두 지원한다.
- 선택된 개수를 하단 작업 바에 보여주고, `앨범 만들기`, `삭제` 버튼을 배치했다.
- `선택` 버튼은 전역 상단 바가 아니라 `모아보기` 박스 안으로 옮겼다.

검사 기준:

- 선택 모드 진입 여부가 명확한가
- 선택된 카드가 시각적으로 확실히 구분되는가
- 선택하지 않았을 때 작업 버튼이 비활성화되는가
- 삭제가 원본 삭제가 아니라 등록 목록 삭제임이 분명한가

추가 학습 포인트:

- 특정 보기에서만 쓰는 조작은 전역 상단보다 해당 패널 안에 두는 편이 사용자가 기능의 범위를 이해하기 쉽다.

## 등록 목록 비우기 QA

문제:

- 사용자가 테스트로 등록한 사진을 한 번에 정리할 방법이 없었다.
- “제거”가 원본 삭제처럼 느껴지면 위험하다.

수정:

- 설정 화면에 `등록 목록 비우기` 영역을 추가했다.
- 버튼 문구는 `모두 비우기`로 두되, 설명에는 원본 파일이 삭제되지 않는다고 명확히 적었다.
- 실행 전 확인 창을 띄워 실수 클릭을 줄였다.

검사 기준:

- 원본 삭제가 아니라 등록 목록 삭제임이 분명한가
- 항목이 없을 때 버튼이 비활성화되는가
- 삭제 후 선택 패널이 빈 상태로 자연스럽게 바뀌는가

## 빈 앨범 상태 QA

문제:

- 샘플 사진이 있으면 실제 사용자가 등록한 사진인지 데모 데이터인지 헷갈릴 수 있다.
- 샘플을 제거하면 화면이 비어 보일 수 있다.

수정:

- 샘플 미디어 데이터를 제거했다.
- 모아보기, 달력보기, 앨범보기, 시간순, 지난 추억 화면에 빈 상태 안내를 추가했다.
- 오른쪽 감상 패널은 선택 항목이 없을 때 `항목을 선택하세요.`를 보여준다.

검사 기준:

- 첫 실행 시 샘플 파일명이 보이지 않는가
- 빈 화면이 고장처럼 보이지 않는가
- 사용자가 다음 행동인 파일 선택/폴더 선택을 이해할 수 있는가

## 다음 디자인 개선 주제

- 실제 썸네일이 들어왔을 때 색감과 대비 다시 점검
- 빈 상태 화면을 더 따뜻하고 짧게 정리
- 파일이 사라졌을 때 불안하지 않은 안내 문구 설계
- 별점/코멘트 편집 UI를 모바일에서도 쉽게 다듬기
- 앨범보기 페이지 넘김 애니메이션이 과하지 않은지 실제 사용감 확인
