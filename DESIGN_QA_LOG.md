# 디자인 QA 학습 로그

## 미확인 얼굴 그룹 제외

- PC/모바일에서 미확인 그룹 선택 → 얼굴 2개 제외 → 미확인 목록에서 사라짐 → 등록 인물 유지 → 되돌리기 흐름 확인.
- `unknown-exclude-desktop.png` 캡처 직접 확인: 미확인 영역의 선택 도구와 카드 체크 표시, 얼굴 수 표시 검사. 모바일 캡처도 테스트에서 저장.
- 관련 테스트 6개 통과, 기존 2개 생략 및 빌드 성공. 실제 사용자 DB에는 테스트 데이터를 쓰지 않음.

## 등록 인물 우선 배치와 얼굴 제외

- `people-groups-desktop.png`, `people-groups-mobile.png` 캡처를 열어 등록 인물 우선 노출과 상단 선택 버튼, 모바일 버튼 줄바꿈 확인.
- 이름 없는 그룹이 먼저 반환되는 fixture에서도 등록 인물이 먼저 보이는지 검사. 첫 화면 선택 버튼으로 제외 액션 노출 확인.
- 이름 등록·분리·옮기기·동명 합치기·얼굴 제외·되돌리기·재실행 후 유지 관련 테스트 6개 통과, 기존 모바일 추론 2개 생략. 실제 사용자 DB는 변경하지 않음.

## 인물 내부 반려동물 탭 (2026-09-07)

- 별도 메뉴가 없는지, 인물 내부 사람/반려동물 탭과 좌우 키보드 포커스 전환 확인.
- PC/모바일 등록 → 사진 2장 연결/대표 사진 선택 → 상세보기 → 이름 수정/1장 연결 해제 → 새로고침 후 유지 → 등록 삭제 → 원본 3장 유지 흐름 통과.
- `test-results/pet-editor-desktop.png`, `pet-editor-mobile.png`, `pet-list-mobile.png`를 직접 열어 탭과 입력 정렬, 체크 원, 모바일 2열 사진, 닫기·저장 버튼의 가독성 확인.
- 캡처 이미지는 테스트용 대체 사진이며 실제 반려동물 인식 결과가 아님. 최초 테스트의 디코딩 불가능한 이미지 fixture와 대표 사진 select의 접근성 이름을 수정하고 재검사.
- 기존 중립 색상, 공통 선택 체크, 날짜만 노출하는 사진 목록을 재사용. 설명 박스를 추가하지 않음.
- 최종 전체 Playwright 26개 통과/2개 기존 생략. Rust 5개 통과 및 프로덕션 빌드 성공. 브라우저 저장 테스트는 모의 명령, SQL 테스트는 인메모리 DB이며 실제 사용자 DB를 조작하지 않음.

## 얼굴 제외 및 행 드래그

- PC/모바일 모아보기에서 첫 행 한 카드부터 다음 행 같은 열까지 두 번의 포인터 이동만으로 두 행 전체가 선택되는지 검사.
- 얼굴 모아보기 선택·제외, 되돌리기, 재조회 후 제외 유지 확인. `people-exclusion-*` 캡처 저장.
- Rust 테스트는 사진 보존, 인물 숨김, 분석 완료 유지, 제외 얼굴의 비교 기준 제외, 되돌리기, 잘못된 ID 일괄 롤백 검사.
- 전체 브라우저 테스트 24개 통과/2개 기존 생략, Rust 테스트 4개 통과. 실제 사용자 DB 조작은 수행하지 않음.


## 미확인 얼굴 재비교

- PC/모바일 후보 검토 화면 캡처 확인. 체크 전 적용 비활성, 취소 시 유지, 체크한 얼굴만 적용 및 재조회 검사.
- 얼굴 관련 브라우저 테스트 6개 통과, 기존 모바일 중복 실제 추론 2개 생략.
- Rust·SQLite 단위 테스트 3개 통과. 기준 인물 매칭, 확정 얼굴 보호, 같은 사진 중복 제외, 애매한 후보 보류 및 미리보기 비변경 확인.
- 실제 사용자 사진의 인식률과 Tauri 창에서의 전체 흐름은 별도 확인 필요. 캡처는 테스트 데이터 사용.


## 2차 기능 분리 검증

- 전체 테스트 22개 통과, 기존 모바일 중복 추론 검사 2개 생략. 빌드 통과.
- 앨범 넘김 도중 다시 섞기 이후 900ms가 지나도 첫 책장이 유지되는 회귀 검사 추가.
- 최상위 사진 상세창의 방향키·Esc 처리와 배경 앨범 유지 검사 통과.
- 앨범 목록 PC와 앨범 내부 모바일 캡처를 확인하여 CSS 파일 이동 후 기존 배치 유지 확인.
- 첫 실행의 얼굴 인식 네트워크 검사는 늦은 CDN 폰트 요청 때문에 실패. 폰트 완료 후 모델 요청 관찰을 시작하도록 수정하고 전체 재실행 통과.
- Tauri 실제 창 및 Rust 빌드는 이번 프런트엔드 구조 변경 범위에서 미실시.


## 미사용 코드 제거 후 회귀 확인

- 전체 브라우저 테스트 22개 통과, 2개 기존 생략. 디자인 캡처 재생성.
- PC 모아보기와 모바일 앨범 내부 캡처를 확인해 기존 배치 유지 확인.
- 동적 페이지 넘김 클래스, 반응형 규칙, 현재 체크표시 가상 요소는 유지. 제거된 화면의 선택자와 숨겨진 중복 요소만 정리.
- Tauri 실제 창·Rust 빌드는 이번 프런트엔드 정리에서 별도 실행하지 않음.


## 표지 정렬과 키보드 검증

- PC/모바일 표지 캡처 재생성. 제목과 사진을 분리하고 책의 기존 크기·입체감 유지.
- 좌우 방향키 이동, 상세창에서 방향키 입력 시 배경 앨범 유지, Esc로 상세창만 닫기 검사 추가. PC/모바일 2개 테스트 통과.


## 앨범 두께 보강 확인

- 목록 캡처에서 표지와 뒷표지 사이 종이 단면 확인. 내부는 종이 묶음과 하드커버가 분리되어 보이도록 수정.
- PC/모바일 캡처 재생성 및 디자인 테스트 2개 통과. 페이지 이동, 사진 상세 열기, 하단 조작부 비겹침 검사 포함.


## 앨범 전용 개편

- `design-albums-*`, `design-reader-*` PC/모바일 캡처로 사진 중심 표지, 책과 하단 조작부 분리 확인.
- 첫 모바일 캡처의 헤더 버튼 넘침을 발견해 방향과 정렬 수정 후 재검증.
- 책과 페이지 이동 영역의 비겹침, 닫기 버튼의 헤더 내부 배치, 다음 책장 이동과 사진 상세 열기를 자동 검사에 추가.
- 기존 책 표지 라이브러리와 페이지 넘김 구현 유지. 실제 Tauri 창 검사는 별도이며 브라우저의 모의 데이터로 검증.


## 실제 레이아웃 재설계 검증

- 상단 가로 탐색, 제목 옆 검색·등록, 탭 옆 선택·필터, 전체 폭 사진 목록으로 재배치.
- 상세보기는 전체 화면 사진 위주로 구성하고 댓글을 아래로 이동. 댓글 접기 화면은 `test-results/design-detail-focused-*.png`에 별도 저장.
- PC/모바일 상세 캡처에서 즐겨찾기 아이콘이 작게 눌리는 문제 발견. 기존 버튼 패딩을 제거하고 다시 캡처함.
- 구조 좌표·아이콘 크기·가로 넘침 검사와 기존 앱 흐름 포함 18개 통과. 빌드 통과. 테스트용 사진은 DB에 저장되지 않음.
- 브라우저 기반 검증이며 Windows Tauri 실제 창의 별도 검사는 하지 않음. 밝은 상단 메뉴 변형은 시각 비교용이며 실제 사용자 A/B 실험은 아님.

## Apple · Nike · Raycast 조합 개편

- Apple: 밝은 사진 공간과 여백. Nike: 또렷한 대비와 사진 그리드. Raycast: 짙은 메뉴와 간결한 도구 버튼.
- `design-gallery`, `design-selection`, `design-detail`, `design-calendar`, `design-event`, `design-reader`, `design-albums`의 PC/모바일 캡처 저장.
- 실제 캡처 검사 후 모바일의 가운데 제목·등록 버튼 세로 배치·연월 선택의 과도한 높이 수정. 사진은 더 위에서 시작하며 연월과 이전/다음 버튼도 나란히 표시.
- 밝은 탐색 영역 대안은 `design-gallery-light-variant`로 비교. 사용자 A/B 실험은 아니며 시각 검토용 변형임.
- 기존 선택·댓글·일정·앨범·인물 흐름과 가로 넘침 검사 포함 22개 통과. Windows 실제 창 검사는 미실시.
- 장식 이미지는 추가하지 않음. 화면 자산은 사용자가 등록한 사진과 실제 앨범 표지이며 QA 샘플은 테스트에만 존재함.

## 인물 분류 화면

- 인물은 얼굴 미리보기·이름·사진 수로 표시. 인물 안에서는 사진과 날짜, 분류 대상 얼굴을 표시하고 원본명은 노출하지 않음.
- 선택 원 32px 안에 체크를 중앙 배치. 편집 버튼은 아이콘과 짧은 한국어 명칭 사용. 모바일에서는 작업 영역을 줄바꿈하고 사진은 두 열로 표시.
- `test-results/people-edit-desktop.png`, `test-results/people-edit-mobile.png`를 캡처하고 직접 확인. 이름 편집·선택·이동 컨트롤 겹침 없음.
- 실제 검출 결과는 `test-results/people-real-faces.png`로 별도 기록. UI 관리 테스트는 모의 인물 데이터를 사용하므로 분류 정확도 증거로 보지 않음.
- 분석 진행 상태·중단·재개·저장 실패 재시도와 이름 저장·분리·이동·상세 열기 테스트 통과.
- 실제 가족 사진의 연령 변화·가림·옆얼굴 정확도 평가는 추후 필요.

이 문서는 `오래담은` 화면을 제품 디자이너와 QA 관점에서 검토한 기록입니다. 단순 수정 목록이 아니라, 다음 프로젝트에서도 다시 쓸 수 있는 디자인 판단 기준을 남깁니다.

## 디자인 목표

`오래담은`은 사진 관리 도구이지만, 사용자가 느끼는 첫인상은 “파일 관리자”보다 “내 추억을 편하게 꺼내보는 앨범”에 가까워야 합니다.

그래서 화면 원칙은 다음과 같습니다.

- 기본 언어는 한국어로 한다.
- 개발 용어를 화면에 노출하지 않는다.
- 사용자가 바로 사진을 등록하고 볼 수 있게 한다.
- 한국 정서는 과한 장식보다 차분한 색, 여백, 오래된 앨범의 물성으로 표현한다.
- 메뉴는 기능 이름보다 사용자 행동을 기준으로 정한다.

## 디자인 시스템 적용 메모

새 디자인 방향은 따뜻한 크림 배경, 포근한 브라운 포인트, 둥근 카드, 명조 제목을 중심으로 잡았습니다.

적용한 규칙:

- 제목, 앨범명, 날짜는 `ChosunMyeongjo`를 사용한다.
- 본문, 버튼, 메뉴는 `SUIT`를 사용한다.
- 배경은 `#FDFBF7`, 표면은 `#FFFFFF`로 둔다.
- 주요 포인트는 `#B88E68`, 진한 강조는 `#735944`를 사용한다.
- 카드와 컨테이너는 `20px` radius를 기본으로 한다.
- 버튼은 `9999px` radius의 pill 형태로 만든다.
- 사진 프레임은 `1:1` 비율로 맞춘다.
- 그림자는 `rgba(115, 89, 68, 0.06)` 계열로 부드럽게 둔다.

검사 결과:

- 데스크톱에서는 목록 영역과 등록 영역이 충분히 넓게 보인다.
- 모바일에서는 명조 헤드라인이 크지만 겹침 없이 읽힌다.
- 버튼 터치 영역은 충분히 크다.
- 샘플이 없는 빈 상태에서도 화면이 깨지지 않는다.

## 레퍼런스 기반 레이아웃 재구성 메모

참고한 글의 핵심은 “화면을 한 번에 예쁘게 만드는 것”보다 실제 실행 화면을 보고 위계, 간격, 반응형, 클릭 흐름을 반복 검증하는 방식입니다.

이번 변경에서는 그 관점을 현재 앱에 맞춰 적용했습니다.

- 상단을 `소개 문구만 있는 영역`에서 `등록 행동 + 앨범 현황` 영역으로 바꿨다.
- 사용자가 가장 먼저 해야 하는 `파일 선택`, `폴더 선택`을 왼쪽 큰 영역에 두었다.
- 전체 기록, 사진, 영상, 즐겨찾기 수를 오른쪽 요약 카드로 분리했다.
- 아래의 `사진보기` 패널은 실제 작업 공간처럼 더 독립적으로 보이게 했다.
- 모바일에서는 메뉴가 두 줄로 내려와 화면을 많이 차지하지 않도록 가로 스크롤 구조로 바꿨다.

검사 기준:

- 첫 화면에서 다음 행동이 바로 보이는가
- 등록 영역과 사진 보기 영역이 서로 경쟁하지 않는가
- 현황 정보가 기능 설명처럼 길지 않고 숫자로 빠르게 읽히는가
- 모바일에서 메뉴가 사진 보기 흐름을 밀어내지 않는가
- 빈 상태에서도 화면이 고장처럼 보이지 않는가

학습 포인트:

- 좋은 레이아웃은 장식보다 정보의 우선순위를 정리하는 일에 가깝다.
- 사용자가 자주 하는 행동은 크게, 상태 확인은 작고 빠르게 읽히게 둔다.
- 실제 화면 캡처를 보면 코드만 볼 때 놓치는 모바일 높이 문제를 쉽게 발견할 수 있다.

## 사용자가 볼 필요 없는 것

화면에서 제거하거나 숨긴 것:

- `Phase 1`
- `MVP`
- `DB`
- `metadata`
- `등록 대기열`
- A/B 테스트 토글
- 내부 구현 설명
- CSS 클래스명 같은 개발자 용어

이런 정보는 개발자에게는 중요하지만, 사용자가 사진을 보는 데는 방해가 됩니다. 대신 학습 로그와 구현 문서에 남깁니다.

## 메뉴 설계

처음에는 달력과 앨범이 별도 메뉴처럼 보였습니다. 하지만 둘 다 “사진을 보는 방식”입니다.

그래서 사이드 메뉴는 `사진보기`로 통합하고, 내부 탭으로 나눴습니다.

- `모아보기`: 전체 사진을 한 번에 훑어보기
- `달력보기`: 날짜 기준으로 찾아보기
- `앨범보기`: 실제 앨범처럼 넘겨보기

이 구조가 더 자연스러운 이유는 사용자가 “달력 기능을 쓰러 간다”기보다 “사진을 날짜별로 보고 싶다”고 생각하기 때문입니다.

## 달력보기 QA

문제:

- 현재 월 중심 달력만 있으면 오래된 사진을 찾기 어렵다.
- 이전/다음 버튼만 있으면 2020년, 2018년 같은 과거로 가기 번거롭다.

수정:

- 연도 셀렉트 박스를 추가했다.
- 월 셀렉트 박스를 추가했다.
- 이전 달/다음 달 버튼은 빠른 이동용으로 유지했다.

검사 기준:

- 과거 연월을 직접 선택할 수 있는가
- 날짜 칸의 사진 수가 읽히는가
- 모바일에서 셀렉트와 버튼이 눌릴 만큼 충분히 큰가
- 달력 칸 간격이 너무 빽빽하지 않은가

## 앨범보기 QA

문제:

- 카드 목록만으로는 실제 앨범을 보는 감각이 약하다.
- 모든 페이지가 같은 개수의 사진이면 손으로 만든 앨범 느낌이 줄어든다.
- 일반 화면 안의 작은 책장은 실제 앨범을 펼친다는 느낌이 약하다.

수정:

- 좌우 책장 형태의 펼친 앨범 레이아웃을 만들었다.
- 한쪽 페이지마다 사진을 위아래 2장까지 정방향으로 놓이게 구성했다.
- 이전/다음 버튼에 책장 넘김 애니메이션을 추가했다.
- `전체창으로 보기`를 추가해 앨범 감상 중에는 주변 UI보다 책장이 중심이 되게 했다.
- 콜라주식 기울기와 불규칙 배치를 제거해 사진을 더 안정적으로 감상하게 했다.
- 폴라로이드처럼 보이는 두꺼운 여백과 캡션을 줄이고, 일반 인화사진을 붙인 느낌으로 바꿨다.

검사 기준:

- 현재 페이지와 전체 페이지 위치를 알 수 있는가
- 이전/다음 버튼이 명확한가
- 사진이 1장일 때도 비어 보이지 않는가
- 사진이 4장일 때도 답답하지 않은가
- 모바일에서 책장 구조가 깨지지 않는가
- 전체창을 닫는 방법이 명확한가
- 사진이 정방향으로 보이고 클릭을 방해하지 않는가
- 사진 프레임보다 실제 사진 면적이 먼저 보이는가
- 사진은 `4px` 정도만 둥글고, `2px` 흰 테두리로 인화사진 느낌이 나는가
- 다음장으로 넘길 때 페이지가 잠깐 사라져 보이지 않는가
- 하단과 중앙 접힘에서 앨범의 종이 두께감이 느껴지는가
- 하단 페이징이 앨범 그림자나 접힘에 가리지 않는가
- 좌우 페이지의 사진 묶음이 위아래 중앙에 맞는가

## 한국 정서 적용 기준

사용하지 않는 방향:

- 과한 궁궐/전통 문양 장식
- 외국 잡지 같은 scrapbook 분위기
- 영어 메뉴 중심 화면
- 설명이 많은 SaaS 랜딩페이지 구성

사용한 방향:

- 따뜻한 크림색과 차분한 갈색 계열의 배경
- 차분한 갈색, 먹색, 종이색 계열을 보조로 사용
- `오늘 남겨둘 순간`, `지난 추억`처럼 한국어 문장 톤 사용
- 기능보다 감상 경험이 먼저 보이도록 구성

## 화면 QA 체크리스트

- 시각적 위계: 가장 중요한 등록/보기 흐름이 먼저 보이는가
- 간격: 카드, 버튼, 탭 사이가 답답하지 않은가
- 대비: 배경 위 텍스트가 충분히 읽히는가
- 텍스트 가독성: 긴 한국어 문장이 버튼이나 카드 밖으로 밀리지 않는가
- 반응형 레이아웃: 모바일에서도 메뉴와 탭이 사용 가능한가
- hover 상태: 클릭 가능한 요소가 마우스 오버 시 반응하는가
- 모바일 사용성: 손가락으로 누를 영역이 충분한가
- 클릭 가능한 요소의 명확성: 버튼과 정보 텍스트가 구분되는가
- 사용자 흐름의 자연스러움: 등록 후 바로 사진을 볼 수 있는가

## 등록 중 상태 QA

문제:

- 큰 폴더를 등록할 때 화면 변화가 없으면 사용자는 앱이 멈췄다고 느낄 수 있다.
- 같은 버튼을 여러 번 누르면 중복 등록 요청이 생길 수 있다.

수정:

- 등록 중에는 버튼 안의 아이콘을 회전 로딩 아이콘으로 바꿨다.
- `파일 등록 중`, `폴더 등록 중`처럼 현재 작업을 짧게 보여준다.
- 등록 중에는 파일/폴더 선택 버튼을 비활성화한다.
- 상태 문구를 별도로 보여줘 큰 폴더를 살펴보는 중임을 알 수 있게 했다.

검사 기준:

- 등록 중인 버튼이 명확히 구분되는가
- 사용자가 기다려야 한다는 것을 알 수 있는가
- 버튼 비활성화 상태가 고장처럼 보이지 않는가
- 모바일에서 상태 문구가 버튼과 겹치지 않는가

## 실제 썸네일과 클릭 액션 QA

문제:

- 실제 사진을 등록해도 카드가 그라디언트로만 보여 사진 앱처럼 느껴지지 않았다.
- 앨범보기의 사진을 눌러도 오른쪽 감상 패널이 바뀌지 않아 클릭 가능한 요소처럼 느껴지지 않았다.

수정:

- 이미지 파일은 Tauri의 로컬 파일 URL로 변환해 실제 `<img>`로 보여주도록 했다.
- 모아보기, 시간순, 달력보기, 앨범보기, 감상 패널에서 같은 이미지 표시 방식을 사용하게 했다.
- 앨범보기의 각 사진도 클릭하면 감상 패널의 선택 항목이 바뀌도록 연결했다.

검사 기준:

- 실제 등록한 사진이 카드와 감상 패널에 보이는가
- 앨범 속 사진을 눌렀을 때 선택 항목이 바뀌는가
- 영상/음원처럼 아직 썸네일이 없는 항목도 아이콘으로 구분되는가

## 상세 모달 QA

문제:

- 오른쪽 감상 패널이 항상 떠 있어 사진 목록과 앨범 화면이 좁아졌다.
- 별점, 즐겨찾기, 코멘트는 모든 순간에 필요한 정보가 아니라 사진을 선택했을 때 필요한 정보다.

수정:

- 오른쪽 고정 감상 패널을 제거했다.
- 목록 영역을 전체 폭으로 넓혔다.
- 사진/영상 클릭 시 상세 모달을 띄워 큰 미리보기와 편집 기능을 보여준다.
- 닫기 버튼, 바깥 영역 클릭, `Esc` 키 닫기를 지원한다.

검사 기준:

- 기본 화면에서 사진 목록이 충분히 넓게 보이는가
- 클릭 가능한 사진 카드가 명확한가
- 모달에서 사진, 별점, 코멘트, 즐겨찾기가 한 흐름으로 보이는가
- 모바일에서 모달이 화면 밖으로 넘치지 않는가

## 다중선택 QA

문제:

- 여러 장을 한 번에 삭제하거나 앨범으로 묶는 흐름이 없었다.
- 기본 클릭이 상세보기인지 선택인지 헷갈리면 사용자가 실수하기 쉽다.

수정:

- 우측 상단에 `선택` 버튼을 추가했다.
- 선택 모드에서만 다중선택이 가능하게 했다.
- 모아보기에서는 클릭 선택과 드래그 선택을 모두 지원한다.
- 선택된 개수를 하단 작업 바에 보여주고, `앨범 만들기`, `삭제` 버튼을 배치했다.
- `선택` 버튼은 전역 상단 바가 아니라 `모아보기` 박스 안으로 옮겼다.

검사 기준:

- 선택 모드 진입 여부가 명확한가
- 선택된 카드가 시각적으로 확실히 구분되는가
- 선택하지 않았을 때 작업 버튼이 비활성화되는가
- 삭제가 원본 삭제가 아니라 등록 목록 삭제임이 분명한가

추가 학습 포인트:

- 특정 보기에서만 쓰는 조작은 전역 상단보다 해당 패널 안에 두는 편이 사용자가 기능의 범위를 이해하기 쉽다.

## 등록 목록 비우기 QA

문제:

- 사용자가 테스트로 등록한 사진을 한 번에 정리할 방법이 없었다.
- “제거”가 원본 삭제처럼 느껴지면 위험하다.

수정:

- 설정 화면에 `등록 목록 비우기` 영역을 추가했다.
- 버튼 문구는 `모두 비우기`로 두되, 설명에는 원본 파일이 삭제되지 않는다고 명확히 적었다.
- 실행 전 확인 창을 띄워 실수 클릭을 줄였다.

검사 기준:

- 원본 삭제가 아니라 등록 목록 삭제임이 분명한가
- 항목이 없을 때 버튼이 비활성화되는가
- 삭제 후 선택 패널이 빈 상태로 자연스럽게 바뀌는가

## 빈 앨범 상태 QA

문제:

- 샘플 사진이 있으면 실제 사용자가 등록한 사진인지 데모 데이터인지 헷갈릴 수 있다.
- 샘플을 제거하면 화면이 비어 보일 수 있다.

수정:

- 샘플 미디어 데이터를 제거했다.
- 모아보기, 달력보기, 앨범보기, 시간순, 지난 추억 화면에 빈 상태 안내를 추가했다.
- 오른쪽 감상 패널은 선택 항목이 없을 때 `항목을 선택하세요.`를 보여준다.

검사 기준:

- 첫 실행 시 샘플 파일명이 보이지 않는가
- 빈 화면이 고장처럼 보이지 않는가
- 사용자가 다음 행동인 파일 선택/폴더 선택을 이해할 수 있는가

## 다음 디자인 개선 주제

- 실제 썸네일이 들어왔을 때 색감과 대비 다시 점검
- 빈 상태 화면을 더 따뜻하고 짧게 정리
- 파일이 사라졌을 때 불안하지 않은 안내 문구 설계
- 별점/코멘트 편집 UI를 모바일에서도 쉽게 다듬기
- 앨범보기 페이지 넘김 애니메이션이 과하지 않은지 실제 사용감 확인

## 사용자 화면 표시 규칙

## 앨범 관리 화면 검증

## 반복 일정 입력 검증

- `test-results/annual-event-desktop.png`, `test-results/annual-event-mobile.png` 캡처 직접 검사.
- 매년 반복 체크 시 연도 없이 월·일을 나란히 표시. 모바일에서도 라벨, 셀렉트, 등록 버튼이 겹치지 않음.
- PC/모바일 반복 일정 등록·재조회·디데이 토글·삭제 테스트 통과.

### 앨범 관리 검증 내역

- 데스크톱과 모바일에서 수정, 취소, 제목·색상 저장, 사진 제거, 앨범 삭제, 모아보기 원본 목록 유지 검증.
- 전체창 닫기 및 Escape 후 모아보기 활성화 검증. Playwright 전체 14개 통과.
- `test-results/album-editor-desktop.png`, `test-results/album-editor-mobile.png` 캡처를 직접 확인하고 기본 입력창과 버튼 스타일을 수정한 뒤 다시 캡처.
- 제목 입력창 높이 44px, 작업 버튼 최소 높이 42px, 사진 선택 원 32px 및 중앙 체크 표시 적용.
- 브라우저 개발 모드는 사진 자리표시자를 사용함. 실제 Tauri 사진 로딩과 DB 재실행 보존 검증은 미실시.
- 내장 브라우저는 환경 메타데이터 오류로 연결 실패하여 Playwright 테스트 캡처로 확인.

### 원본명 표시 규칙

- 원본 파일명은 사용자가 볼 필요가 없는 내부 정보로 본다.
- 사진 카드, 달력 상세, 앨범보기, 상세 모달에서는 원본 파일명 대신 날짜, 코멘트, 태그처럼 기억을 정리하는 정보만 보여준다.
- 검색에는 내부적으로 파일명을 활용할 수 있지만, 검색창 문구나 화면 텍스트에는 파일명 중심 표현을 쓰지 않는다.
